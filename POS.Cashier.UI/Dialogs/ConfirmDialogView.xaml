<Window x:Class="POS.Cashier.UI.Dialogs.ConfirmDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmation" Height="300" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource MainBgBrush}"
        BorderBrush="{StaticResource HeaderBgBrush}"
        BorderThickness="3"
        FontFamily="Arial">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <Border x:Name="HeaderBorder" Grid.Row="0" Background="{StaticResource CancelBtnBrush}">
            <TextBlock x:Name="TitleTxt" Text="CONFIRM ACTION" Foreground="{StaticResource TextLightBrush}" FontSize="20" FontWeight="Heavy" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource InfoPanelBgBrush}" Padding="30" BorderBrush="{StaticResource HeaderBgBrush}" BorderThickness="0,0,0,2">
            <TextBlock x:Name="MessageTxt" 
                       Text="Are you sure you want to perform this action? This cannot be undone." 
                       Foreground="{StaticResource TextBlackBrush}" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       TextWrapping="Wrap" 
                       TextAlignment="Center" 
                       VerticalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="2" Background="{StaticResource HeaderBgBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelBtn" Click="CancelBtn_Click" Grid.Column="0" Background="{StaticResource NumpadBtnBrush}" Content="NO, GO BACK" FontSize="20" Margin="10,10,5,10"/>

            <Button x:Name="ConfirmBtn" Click="ConfirmBtn_Click" Grid.Column="1" Background="{StaticResource CancelBtnBrush}" Content="YES, I'M SURE" FontSize="20" Margin="5,10,10,10"/>
        </Grid>
    </Grid>
</Window>