<Window x:Class="POS.Cashier.UI.Views.Dialogs.LoyaltyCustomerDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer &amp; Discount Database" Height="720" Width="1450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F0F0F0" FontFamily="Segoe UI" Topmost="True">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#003366" Padding="8" Margin="0,0,0,5" BorderBrush="#808080" BorderThickness="1">
            <TextBlock Text="CUSTOMER &amp; DISCOUNT MANAGEMENT" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Customer Directory &amp; Assigned Discounts" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0">
                <Grid Margin="0,4,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Search (Name/Phone):" FontWeight="Normal" Foreground="Black" FontSize="15" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="SearchTxt" Grid.Column="1" Height="36" FontSize="18" FontWeight="Bold" Background="#FFFFE0" VerticalContentAlignment="Center" Padding="4,0" BorderBrush="#A0A0A0"/>
                        <Button x:Name="SearchBtn" Grid.Column="2" Content="SEARCH" Click="SearchBtn_Click" FontSize="15" FontWeight="Bold" Margin="8,0,0,0" Padding="0"/>
                    </Grid>

                    <DataGrid Grid.Row="1" x:Name="CustomerDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" RowHeight="36" FontSize="15" Background="White"
                              SelectionMode="Single" SelectionChanged="CustomerDataGrid_SelectionChanged" BorderBrush="#A0A0A0" FontWeight="Normal" Foreground="Black">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="4,3"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                                <Setter Property="BorderThickness" Value="0,0,0,0"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Customer Code" Binding="{Binding CustomerCode}" Width="120" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="110"/>
                            <DataGridTextColumn Header="Birthday" Binding="{Binding Birthday, StringFormat=MMM dd}" Width="80"/>

                            <DataGridTextColumn Header="Loyalty Points" Binding="{Binding LoyaltyPoints, StringFormat=\{0:N0\}}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#003366"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="TextAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Active Discount" Binding="{Binding ActiveDiscountName}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#005500"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Applies To" Binding="{Binding DiscountScope}" Width="130" />
                            <DataGridTextColumn Header="Expiry Date" Binding="{Binding DiscountExpiry, StringFormat=yyyy-MM-dd}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#DC3545"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Customer Details &amp; Registration" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10,6,10,0">

                        <TextBlock Text="Loyalty Code (Auto-Generated)" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <TextBox x:Name="RegLoyaltyCodeTxt" Height="36" FontSize="16" FontWeight="Bold" Background="#E5E7EB" Foreground="#4B5563" IsReadOnly="True" VerticalContentAlignment="Center" Padding="6,0" BorderBrush="#A0A0A0" Margin="0,0,0,8" Text="LY-WILL-GENERATE"/>

                        <TextBlock Text="Full Name *" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <TextBox x:Name="RegNameTxt" Height="36" FontSize="16" FontWeight="Bold" Background="#FFFFE0" VerticalContentAlignment="Center" Padding="6,0" BorderBrush="#A0A0A0" Margin="0,0,0,8"/>

                        <TextBlock Text="Phone Number *" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <TextBox x:Name="RegPhoneTxt" Height="36" FontSize="16" FontWeight="Bold" Background="#FFFFE0" VerticalContentAlignment="Center" Padding="6,0" BorderBrush="#A0A0A0" Margin="0,0,0,8"/>

                        <TextBlock Text="Gender *" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <ComboBox x:Name="RegGenderCbo" Height="36" FontSize="15" FontWeight="SemiBold" VerticalContentAlignment="Center" Margin="0,0,0,8" SelectedIndex="0">
                            <ComboBoxItem Content="Select Gender" IsEnabled="False"/>
                            <ComboBoxItem Content="Male"/>
                            <ComboBoxItem Content="Female"/>
                        </ComboBox>

                        <TextBlock Text="Email Address (Optional)" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <TextBox x:Name="RegEmailTxt" Height="36" FontSize="16" FontWeight="Bold" Background="#FFFFE0" VerticalContentAlignment="Center" Padding="6,0" BorderBrush="#A0A0A0" Margin="0,0,0,8"/>

                        <TextBlock Text="Birthday (Optional)" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <DatePicker x:Name="RegBirthDate" Height="36" VerticalContentAlignment="Center" BorderBrush="#A0A0A0" Margin="0,0,0,8"/>

                        <TextBlock Text="Registration Date" FontWeight="Normal" Foreground="Black" FontSize="14" Margin="0,0,0,2"/>
                        <DatePicker x:Name="RegDate" Height="36" VerticalContentAlignment="Center" BorderBrush="#A0A0A0" Margin="0,0,0,12"/>

                        <Border BorderBrush="#A0A0A0" BorderThickness="1" Background="#FFF5F5" Padding="10,8" CornerRadius="4" Margin="0,0,0,15">
                            <CheckBox x:Name="LockCardChk" Content="Lock Loyalty Card (Suspend Account)" Foreground="#DC3545" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="ClearFieldsBtn" Grid.Column="0" Content="CLR" Click="ClearSearchBtn_Click" Background="#FFC107" FontWeight="Bold" FontSize="14" Height="45" Margin="0,0,6,0"/>
                            <Button x:Name="RegisterBtn" Grid.Column="1" Content="REGISTER / UPDATE" Click="RegisterBtn_Click" Background="#E0E0E0" FontWeight="Bold" FontSize="14" Height="45"/>
                        </Grid>

                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="2" Margin="6,4,6,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelBtn" Grid.Column="0" Content="CLOSE" Click="CancelBtn_Click" Background="#E0E0E0" FontWeight="Bold" FontSize="15" Height="45"/>
            <Button x:Name="ApplyBtn" Grid.Column="2" Content="APPLY SELECTED TO SALE" Click="ApplyBtn_Click" Background="#005500" Foreground="White" FontWeight="Bold" FontSize="15" Height="45" IsEnabled="False"/>
        </Grid>
    </Grid>
</Window>