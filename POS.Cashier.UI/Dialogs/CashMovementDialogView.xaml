<Window x:Class="POS.Cashier.UI.Dialogs.CashMovementDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cash Movement" Height="450" Width="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource MainBgBrush}"
        BorderBrush="{StaticResource HeaderBgBrush}"
        BorderThickness="3"
        FontFamily="Arial">

    <Window.Resources>
        <!-- Flat, Zero-Radius Button Style -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource HeaderBgBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <!-- Dynamic Header (Colors and Text will change via C#) -->
        <Border x:Name="HeaderBorder" Grid.Row="0" Background="{StaticResource HeaderBgBrush}">
            <TextBlock x:Name="HeaderTxt" Text="CASH MOVEMENT" Foreground="{StaticResource TextLightBrush}" FontSize="20" FontWeight="Heavy" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT SIDE: Inputs -->
            <Border Grid.Column="0" Background="{StaticResource InfoPanelBgBrush}" Padding="20" BorderBrush="{StaticResource HeaderBgBrush}" BorderThickness="0,0,2,0">
                <StackPanel VerticalAlignment="Center">

                    <TextBlock Text="Amount" Foreground="{StaticResource TextBlackBrush}" FontSize="16" FontWeight="Bold"/>
                    <TextBox x:Name="AmountTxt" Text="0.00" FontSize="42" FontWeight="Heavy" TextAlignment="Right" Foreground="{StaticResource TextDangerBrush}" Margin="0,5,0,25"/>

                    <TextBlock Text="Reason / Remarks" Foreground="{StaticResource TextBlackBrush}" FontSize="16" FontWeight="Bold"/>
                    <TextBox x:Name="ReasonTxt" Height="80" TextWrapping="Wrap" VerticalContentAlignment="Top" AcceptsReturn="True" FontSize="16"/>

                </StackPanel>
            </Border>

            <!-- RIGHT SIDE: Number Pad -->
            <Grid Grid.Column="1" Background="{StaticResource PopupBgBrush}" Margin="5">
                <UniformGrid Columns="3" Rows="4">
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="7" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="8" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="9" FontSize="26"/>

                    <Button Background="{StaticResource NumpadBtnBrush}" Content="4" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="5" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="6" FontSize="26"/>

                    <Button Background="{StaticResource NumpadBtnBrush}" Content="1" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="2" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="3" FontSize="26"/>

                    <Button Background="{StaticResource NumpadBtnBrush}" Content="C" FontSize="22"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="0" FontSize="26"/>
                    <Button Background="{StaticResource NumpadBtnBrush}" Content="." FontSize="26"/>
                </UniformGrid>
            </Grid>
        </Grid>

        <!-- FOOTER: Action Buttons -->
        <Grid Grid.Row="2" Background="{StaticResource HeaderBgBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelBtn" Click="CancelBtn_Click" Grid.Column="0" Background="{StaticResource CancelBtnBrush}" Content="CANCEL" FontSize="20" Margin="10,10,5,10"/>
            <Button x:Name="ConfirmBtn" Click="ConfirmBtn_Click" Grid.Column="1" Background="{StaticResource ExpressBtnBrush}" Content="CONFIRM" FontSize="20" Margin="5,10,10,10"/>
        </Grid>
    </Grid>
</Window>