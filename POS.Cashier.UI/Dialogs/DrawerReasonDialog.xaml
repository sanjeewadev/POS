<Window x:Class="POS.Cashier.UI.Dialogs.DrawerReasonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cash Drawer Reason" Height="330" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F3F4F6" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="12"/><RowDefinition Height="*"/><RowDefinition Height="12"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Background="#0F3D5E" Padding="14"><TextBlock x:Name="TitleText" Foreground="White" FontSize="19" FontWeight="Bold"/></Border>
        <Border Grid.Row="2" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Padding="16">
            <StackPanel>
                <TextBlock Text="Reason" FontWeight="SemiBold"/>
                <ComboBox x:Name="ReasonCombo" Margin="0,6,0,14" Height="34"/>
                <TextBlock Text="Note (required for Other)" FontWeight="SemiBold"/>
                <TextBox x:Name="NoteBox" Margin="0,6,0,0" Height="70" TextWrapping="Wrap" AcceptsReturn="True" MaxLength="500"/>
                <TextBlock x:Name="ErrorText" Foreground="#B91C1C" FontWeight="SemiBold" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Button Content="CANCEL" Click="Cancel_Click" Height="40"/>
            <Button Grid.Column="2" Content="CONTINUE" Click="Continue_Click" Height="40" Background="#0F766E" Foreground="White" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>
