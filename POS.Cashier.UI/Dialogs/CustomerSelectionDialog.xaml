<Window x:Class="POS.Cashier.UI.Dialogs.CustomerSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Customer" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource MainBgBrush}"
        BorderBrush="{StaticResource HeaderBgBrush}"
        BorderThickness="3"
        FontFamily="Arial">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBgBrush}">
            <TextBlock Text="LOYALTY / CUSTOMER ASSIGNMENT" Foreground="{StaticResource TextLightBrush}" FontSize="20" FontWeight="Heavy" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource InfoPanelBgBrush}" Padding="15" BorderBrush="{StaticResource HeaderBgBrush}" BorderThickness="0,0,0,2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtSearch" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" FontSize="24" FontWeight="Bold" Padding="10" VerticalContentAlignment="Center" Background="#F1F5F9" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="2" />
                <Button Grid.Column="1" Content="SEARCH" Background="{StaticResource ActionBtnBrush}" Margin="10,0,0,0" FontSize="20"/>
            </Grid>
        </Border>

        <DataGrid x:Name="dgCustomers" Grid.Row="2" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" Background="White" RowHeight="45" FontSize="16" 
                  BorderThickness="0" SelectionMode="Single" AlternatingRowBackground="#F8FAFC">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource HeaderBgBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource ActionBtnBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Phone No" Binding="{Binding Phone}" Width="150" FontWeight="Heavy"/>
                <DataGridTextColumn Header="Customer Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Loyalty Pts" Binding="{Binding Points}" Width="120" FontWeight="Bold" Foreground="{StaticResource TextHighlightBrush}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Background="{StaticResource HeaderBgBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            <Button Click="CancelBtn_Click" Grid.Column="0" Background="{StaticResource CancelBtnBrush}" Content="CLOSE" FontSize="20" Margin="10,10,5,10"/>
            <Button Grid.Column="1" Background="{StaticResource SystemBtnBrush}" Content="WALK-IN" FontSize="20" Margin="5,10,5,10"/>
            <Button Click="ConfirmBtn_Click" Grid.Column="2" Background="{StaticResource ExpressBtnBrush}" Content="ASSIGN TO BILL" FontSize="20" Margin="5,10,10,10"/>
        </Grid>
    </Grid>
</Window>