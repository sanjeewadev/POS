<Window x:Class="POS.Cashier.UI.Dialogs.LockScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terminal Locked" 
        WindowState="Maximized" 
        WindowStyle="None" 
        Topmost="True" 
        ShowInTaskbar="False"
        Background="#CC000000" 
        FontFamily="Segoe UI, Arial">

    <Window.Resources>
        <Style TargetType="Button" x:Key="NumpadBtn">
            <Setter Property="Background" Value="#2B2B2B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Width="450" Height="650" Background="#111111" CornerRadius="12" BorderBrush="#333333" BorderThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="120"/>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" VerticalAlignment="Center">
                    <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="TERMINAL LOCKED" Foreground="White" FontSize="24" FontWeight="Heavy" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="30,0">
                    <TextBlock x:Name="CashierNameTxt" Text="Current User: Pending..." Foreground="#28A745" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <PasswordBox x:Name="PinBox" PasswordChar="●" FontSize="42" Height="55" Background="#000000" Foreground="White" HorizontalContentAlignment="Center" BorderBrush="#444444" BorderThickness="2" IsEnabled="False"/>
                </StackPanel>

                <UniformGrid Grid.Row="2" Columns="3" Rows="4" Margin="20,10,20,30">
                    <Button Style="{StaticResource NumpadBtn}" Content="7" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="8" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="9" Click="NumpadBtn_Click"/>

                    <Button Style="{StaticResource NumpadBtn}" Content="4" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="5" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="6" Click="NumpadBtn_Click"/>

                    <Button Style="{StaticResource NumpadBtn}" Content="1" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="2" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="3" Click="NumpadBtn_Click"/>

                    <Button Style="{StaticResource NumpadBtn}" Content="C" Background="#DC3545" Click="ClearBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="0" Click="NumpadBtn_Click"/>
                    <Button Style="{StaticResource NumpadBtn}" Content="UNLOCK" Background="#28A745" FontSize="20" Click="UnlockBtn_Click"/>
                </UniformGrid>
            </Grid>
        </Border>
    </Grid>
</Window>