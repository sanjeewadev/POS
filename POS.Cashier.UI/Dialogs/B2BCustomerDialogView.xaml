<Window x:Class="POS.Cashier.UI.Dialogs.B2BCustomerDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wholesale &amp; Credit Accounts" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F0F0F0" FontFamily="Segoe UI" Topmost="True">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#003366" Padding="8" Margin="0,0,0,5" BorderBrush="#808080" BorderThickness="1">
            <TextBlock Text="WHOLESALE &amp; CREDIT ACCOUNT LOOKUP" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="B2B Directory" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Search (Company/VAT/Phone):" FontWeight="Normal" Foreground="Black" FontSize="15" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="SearchTxt" Grid.Column="1" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Height="36" FontSize="18" FontWeight="Bold" Background="#FFFFE0" VerticalContentAlignment="Center" Padding="4,0" BorderBrush="#A0A0A0">
                            <!-- Triggers the search immediately if they hit Enter inside the box -->
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button x:Name="SearchBtn" Grid.Column="2" Content="SEARCH" Command="{Binding SearchCommand}" FontSize="15" FontWeight="Bold" Margin="8,0,0,0" Padding="0"/>
                    </Grid>

                    <DataGrid Grid.Row="1" x:Name="CustomerDataGrid" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" RowHeight="36" FontSize="15" Background="White"
                              SelectionMode="Single" BorderBrush="#A0A0A0" FontWeight="Normal" Foreground="Black">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="4,3"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Customer Code" Binding="{Binding CustomerCode}" Width="120" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="160" FontWeight="Bold" Foreground="#003366"/>
                            <DataGridTextColumn Header="Contact Name" Binding="{Binding FullName}" Width="140"/>

                            <DataGridTextColumn Header="Credit Limit" Binding="{Binding CreditLimit, StringFormat=N2}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Owed Balance" Binding="{Binding CurrentBalance, StringFormat=N2}" Width="120" FontWeight="Bold" Foreground="#8B0000">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="Financial Status" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0" Margin="0,0,0,5" Height="300">
                    <Border Background="#E8E8E8" BorderBrush="#C0C0C0" BorderThickness="1" Padding="10" Margin="5">
                        <Grid VerticalAlignment="Center">

                            <!-- VISIBILITY TOGGLE: No Customer Selected -->
                            <TextBlock Text="Select a customer to view credit status." Foreground="#777" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCustomerSelected}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- VISIBILITY TOGGLE: Customer Selected -->
                            <StackPanel>
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCustomerSelected}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="{Binding DisplayCompanyName}" FontSize="18" FontWeight="Heavy" Foreground="#003366" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,15"/>

                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Credit Limit:" FontSize="15" FontWeight="SemiBold" Foreground="#333"/>
                                    <TextBlock Text="{Binding CreditLimit, StringFormat=Rs. \{0:N2\}}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right"/>
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Current Debt:" FontSize="15" FontWeight="SemiBold" Foreground="#8B0000"/>
                                    <TextBlock Text="{Binding CurrentDebt, StringFormat=Rs. \{0:N2\}}" FontSize="18" FontWeight="Heavy" Foreground="#8B0000" HorizontalAlignment="Right"/>
                                </Grid>

                                <Rectangle Height="2" Fill="#A0A0A0" Margin="0,5,0,10"/>

                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Available Credit:" FontSize="16" FontWeight="Bold" Foreground="#005500"/>

                                    <TextBlock Text="{Binding AvailableCredit, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Heavy" HorizontalAlignment="Right">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <!-- Automatically changes to RED if the account is locked / overdrafted! -->
                                                <Setter Property="Foreground" Value="#005500"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsAccountLocked}" Value="True">
                                                        <Setter Property="Foreground" Value="DarkRed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>

                                <Border Background="#FFCCCC" BorderBrush="#8B0000" BorderThickness="2" Padding="10">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAccountLocked}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding LockReasonText}" Foreground="#8B0000" FontWeight="Heavy" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>

                        </Grid>
                    </Border>
                </GroupBox>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Hooked to ViewModel Commands directly! -->
            <Button Grid.Column="0" Content="CLOSE" Command="{Binding CancelCommand}" Background="#E0E0E0" FontWeight="Bold" FontSize="15" Height="45"/>
            <Button Grid.Column="2" Content="ATTACH TO INVOICE" Command="{Binding AttachCommand}" IsEnabled="{Binding CanAttachToInvoice}" Background="#005500" Foreground="White" FontWeight="Bold" FontSize="15" Height="45"/>
        </Grid>
    </Grid>
</Window>