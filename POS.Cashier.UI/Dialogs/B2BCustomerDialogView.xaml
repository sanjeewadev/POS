<Window x:Class="POS.Cashier.UI.Views.Dialogs.B2BCustomerDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wholesale &amp; Credit Accounts" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F0F0F0" FontFamily="Segoe UI" Topmost="True">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#003366" Padding="8" Margin="0,0,0,5" BorderBrush="#808080" BorderThickness="1">
            <TextBlock Text="WHOLESALE &amp; CREDIT ACCOUNT LOOKUP" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="B2B Directory" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Search (Company/VAT/Phone):" FontWeight="Normal" Foreground="Black" FontSize="15" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="SearchTxt" Grid.Column="1" Height="36" FontSize="18" FontWeight="Bold" Background="#FFFFE0" VerticalContentAlignment="Center" Padding="4,0" BorderBrush="#A0A0A0"/>
                        <Button x:Name="SearchBtn" Grid.Column="2" Content="SEARCH" Click="SearchBtn_Click" FontSize="15" FontWeight="Bold" Margin="8,0,0,0" Padding="0"/>
                    </Grid>

                    <DataGrid Grid.Row="1" x:Name="CustomerDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" RowHeight="36" FontSize="15" Background="White"
                              SelectionMode="Single" SelectionChanged="CustomerDataGrid_SelectionChanged" BorderBrush="#A0A0A0" FontWeight="Normal" Foreground="Black">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="4,3"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Customer Code" Binding="{Binding CustomerCode}" Width="120" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="160" FontWeight="Bold" Foreground="#003366"/>
                            <DataGridTextColumn Header="Contact Name" Binding="{Binding FullName}" Width="140"/>

                            <DataGridTextColumn Header="Credit Limit" Binding="{Binding CreditLimit, StringFormat=N2}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Owed Balance" Binding="{Binding CurrentBalance, StringFormat=N2}" Width="120" FontWeight="Bold" Foreground="#8B0000">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Padding" Value="5,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="Financial Status" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0" Margin="0,0,0,5" Height="300">
                    <Border Background="#E8E8E8" BorderBrush="#C0C0C0" BorderThickness="1" Padding="10" Margin="5">
                        <StackPanel VerticalAlignment="Center">

                            <TextBlock x:Name="NoSelectionTxt" Text="Select a customer to view credit status." Foreground="#777" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0"/>

                            <StackPanel x:Name="FinancialPanel" Visibility="Collapsed">
                                <TextBlock x:Name="DisplayCompanyTxt" Text="COMPANY NAME" FontSize="18" FontWeight="Heavy" Foreground="#003366" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,15"/>

                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Credit Limit:" FontSize="15" FontWeight="SemiBold" Foreground="#333"/>
                                    <TextBlock x:Name="DisplayLimitTxt" Text="Rs. 0.00" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right"/>
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Current Debt:" FontSize="15" FontWeight="SemiBold" Foreground="#8B0000"/>
                                    <TextBlock x:Name="DisplayDebtTxt" Text="Rs. 0.00" FontSize="18" FontWeight="Heavy" Foreground="#8B0000" HorizontalAlignment="Right"/>
                                </Grid>

                                <Rectangle Height="2" Fill="#A0A0A0" Margin="0,5,0,10"/>

                                <Grid Margin="0,0,0,15">
                                    <TextBlock Text="Available Credit:" FontSize="16" FontWeight="Bold" Foreground="#005500"/>
                                    <TextBlock x:Name="DisplayAvailableTxt" Text="Rs. 0.00" FontSize="20" FontWeight="Heavy" Foreground="#005500" HorizontalAlignment="Right"/>
                                </Grid>

                                <Border x:Name="LockWarningBox" Background="#FFCCCC" BorderBrush="#8B0000" BorderThickness="2" Padding="10" Visibility="Collapsed">
                                    <TextBlock x:Name="LockReasonTxt" Text="❌ ACCOUNT LOCKED BY ADMIN" Foreground="#8B0000" FontWeight="Heavy" FontSize="14" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>

                        </StackPanel>
                    </Border>
                </GroupBox>

                <GroupBox Grid.Row="1" Header="Quick Search Input" FontSize="15" FontWeight="Bold" Foreground="#003366" BorderBrush="#A0A0A0">
                    <Border Background="#D4D0C8" Padding="6" Margin="4">
                        <UniformGrid Columns="3" Rows="4">
                            <Button Content="7" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="8" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="9" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="4" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="5" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="6" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="1" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="2" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="3" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="C" FontSize="26" FontWeight="Heavy" Background="#DC3545" Foreground="White" Margin="3" Click="NumpadBtn_Click"/>
                            <Button Content="0" FontSize="26" FontWeight="Heavy" Margin="3" Click="NumpadBtn_Click"/>
                            <Button x:Name="ClearSearchBtn" Content="CLR" FontSize="20" FontWeight="Heavy" Background="#FFC107" Margin="3" Click="ClearSearchBtn_Click" ToolTip="Clear Fields"/>
                        </UniformGrid>
                    </Border>
                </GroupBox>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelBtn" Grid.Column="0" Content="CLOSE" Click="CancelBtn_Click" Background="#E0E0E0" FontWeight="Bold" FontSize="15" Height="45"/>
            <Button x:Name="ApplyBtn" Grid.Column="2" Content="ATTACH TO INVOICE" Click="ApplyBtn_Click" Background="#005500" Foreground="White" FontWeight="Bold" FontSize="15" Height="45" IsEnabled="False"/>
        </Grid>
    </Grid>
</Window>