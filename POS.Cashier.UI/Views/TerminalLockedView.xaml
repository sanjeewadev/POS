<Window x:Class="POS.Cashier.UI.Views.TerminalLockedView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terminal Locked" Height="400" Width="650"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#DC2626" 
        BorderBrush="#7F1D1D"
        BorderThickness="4"
        FontFamily="Arial">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,20">
            <TextBlock Text="🛑 TERMINAL LOCKED" FontSize="42" FontWeight="Heavy" Foreground="White" HorizontalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#991B1B" Padding="20" CornerRadius="5">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="There is currently an active, unclosed shift on this register." FontSize="18" Foreground="#FECACA" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                    <TextBlock Text="Locked To: " FontSize="22" Foreground="White" FontWeight="Bold"/>
                    <TextBlock x:Name="LockedCashierTxt" Text="UNKNOWN" FontSize="22" Foreground="#FCD34D" FontWeight="Heavy"/>
                </StackPanel>

                <TextBlock Text="You cannot open a new shift until the current shift is officially closed." FontSize="16" Foreground="White" TextAlignment="Center" TextWrapping="Wrap" Margin="0,15,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Click="CancelBtn_Click" Grid.Column="0" Background="#111827" Foreground="White" FontSize="18" FontWeight="Bold" Content="CANCEL / GO BACK" Height="50" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Click="OverrideBtn_Click" Grid.Column="2" Background="#F59E0B" Foreground="Black" FontSize="18" FontWeight="Bold" Content="MANAGER OVERRIDE" Height="50" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>