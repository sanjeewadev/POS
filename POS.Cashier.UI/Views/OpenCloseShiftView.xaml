<Window x:Class="POS.Cashier.UI.Views.OpenCloseShiftView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shift Management" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Closing="Window_Closing"
        Background="{StaticResource MainBgBrush}"
        BorderBrush="{StaticResource HeaderBgBrush}"
        BorderThickness="3"
        FontFamily="Arial">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBgBrush}">
            <TextBlock Text="{Binding ShiftModeTitle, FallbackValue='OPEN NEW SHIFT / REGISTER'}" Foreground="{StaticResource TextLightBrush}" FontSize="24" FontWeight="Heavy" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="CASH DENOMINATION COUNTER" FontWeight="Bold" Foreground="{StaticResource TextInactiveBrush}" FontSize="14" Margin="0,0,0,15"/>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Rs. 5000 x" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Count5000, UpdateSourceTrigger=PropertyChanged}" Height="40"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Rs. 1000 x" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Count1000, UpdateSourceTrigger=PropertyChanged}" Height="40"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Rs. 500 x" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Count500, UpdateSourceTrigger=PropertyChanged}" Height="40"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Rs. 100 x" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Count100, UpdateSourceTrigger=PropertyChanged}" Height="40"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Coins/Other:" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding CountCoins, UpdateSourceTrigger=PropertyChanged}" Height="40"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource InfoPanelBgBrush}" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="1" Padding="20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="TOTAL PHYSICAL CASH DECLARED:" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" FontSize="18" HorizontalAlignment="Center"/>
                    <Border Background="White" BorderBrush="{StaticResource HeaderBgBrush}" BorderThickness="2" Margin="0,10,0,30" Padding="15">
                        <TextBlock Text="{Binding DeclaredAmount, StringFormat=Rs. \{0:N2\}, FallbackValue='Rs. 0.00'}" FontSize="48" FontWeight="Heavy" Foreground="{StaticResource TextHighlightBrush}" TextAlignment="Center"/>
                    </Border>

                    <TextBlock Text="Shift Details:" FontWeight="Bold" Foreground="{StaticResource TextInactiveBrush}" FontSize="14" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding CashierName, StringFormat='Cashier: \{0\}'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" Margin="0,5"/>
                    <TextBlock Text="{Binding TerminalNo, StringFormat='Terminal: \{0\}'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" Margin="0,5"/>
                    <TextBlock Text="{Binding CurrentDate, StringFormat='Date: \{0\}'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}" Margin="0,5"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Background="{StaticResource HeaderBgBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Button Click="CancelBtn_Click" Grid.Column="0" Background="{StaticResource CancelBtnBrush}" Content="CANCEL / LOG OFF" Margin="10,15,5,15"/>
            <Button Click="ConfirmBtn_Click" Grid.Column="1" Background="{StaticResource ExpressBtnBrush}" Content="CONFIRM &amp; PROCEED" Margin="5,15,10,15"/>
        </Grid>
    </Grid>
</Window>