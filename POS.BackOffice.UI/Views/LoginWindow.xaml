<Window x:Class="POS.BackOffice.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ERP System Login" Height="320" Width="380" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="ToolWindow"
        Background="#D4D0C8" FontFamily="Segoe UI" FontSize="14">

    <Grid>
        <Border Background="#F0F0F0" BorderBrush="#A0A0A0" BorderThickness="1" 
                Margin="15" Padding="20">
            <StackPanel VerticalAlignment="Center">

                <TextBlock Text="AUTHORIZED ACCESS ONLY" FontWeight="Bold" FontSize="16" Foreground="#003366" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Username:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="28" Padding="5,0" BorderBrush="#808080"/>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Password:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <PasswordBox Grid.Column="1" x:Name="pwdLogin" Height="28" Padding="5,0" BorderBrush="#808080"/>
                </Grid>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="DarkRed" FontWeight="Bold" HorizontalAlignment="Center" Height="20" Margin="0,0,0,10"/>

                <Button Content="SECURE LOGIN" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=pwdLogin}" 
                        Height="38" Background="#005555" Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="1" BorderBrush="#003333"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>