<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.QuotationManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
        <Style TargetType="TextBox" x:Key="ClassicInput">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="ClassicCombo">
            <Setter Property="Height" Value="26"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>
        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>
        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Button" x:Key="ConvertBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="RightElement">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="StatusText">
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsExpired}" Value="True">
                    <Setter Property="Foreground" Value="#A94044"/>
                    <Setter Property="Text" Value="Expired"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Converted">
                    <Setter Property="Foreground" Value="#28A745"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">

        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsGridView}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#A0A0A0" BorderThickness="1" Background="#F0F0F0" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="QUOTATION MANAGER:" Style="{StaticResource HeaderLabel}" VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                        <TextBlock Text="Status:" Style="{StaticResource InlineLabel}"/>
                        <ComboBox ItemsSource="{Binding AvailableStatuses}" SelectedItem="{Binding SelectedStatus}" Width="120" Style="{StaticResource ClassicCombo}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Style="{StaticResource ClassicInput}" ToolTip="Search Quote No or Customer"/>
                        <Button Content="🔍 Search" Command="{Binding SearchCommand}" Style="{StaticResource ClassicBtn}" Margin="10,0,0,0"/>
                    </StackPanel>

                    <Button Grid.Column="4" Content="➕ DRAFT NEW QUOTE" Command="{Binding OpenBuilderCommand}" Style="{StaticResource ActionBtn}" Padding="20,0"/>
                </Grid>
            </Border>

            <DataGrid Grid.Row="1" ItemsSource="{Binding PagedQuotations}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                      RowHeight="30" Background="White" BorderThickness="1" BorderBrush="#A0A0A0" AlternatingRowBackground="#F9F9F9">

                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding ViewAndConvertCommand}" CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </DataGrid.InputBindings>

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#A0A0A0"/>
                    </Style>
                    <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date Created" Binding="{Binding DateCreated, StringFormat=dd-MMM-yyyy}" Width="120" Foreground="#555"/>
                    <DataGridTextColumn Header="Quote No." Binding="{Binding QuoteNo}" Width="140" FontWeight="Bold" Foreground="#005555"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="200"/>
                    <DataGridTextColumn Header="Valid Until" Binding="{Binding ValidUntil, StringFormat=dd-MMM-yyyy}" Width="120" Foreground="#666"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" ElementStyle="{StaticResource StatusText}"/>
                    <DataGridTextColumn Header="Net Amount" Binding="{Binding NetTotal, StringFormat=Rs. \{0:N2\}}" Width="150" ElementStyle="{StaticResource RightElement}" FontWeight="Bold"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="2" Background="#EAEAEA" BorderBrush="#A0A0A0" BorderThickness="1,0,1,1" Padding="10,5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="◀ Prev" Command="{Binding PreviousPageCommand}" Style="{StaticResource ClassicBtn}" Width="80" Margin="0,0,10,0"/>
                    <TextBlock VerticalAlignment="Center" FontWeight="Bold" Foreground="#555" Margin="0,0,10,0">
                        <Run Text="Page "/><Run Text="{Binding CurrentPage}"/><Run Text=" of "/><Run Text="{Binding TotalPages}"/>
                    </TextBlock>
                    <Button Content="Next ▶" Command="{Binding NextPageCommand}" Style="{StaticResource ClassicBtn}" Width="80"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#D4D0C8">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBuilderView}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="#A0A0A0" BorderThickness="1" Background="#003366" Padding="15" Margin="0,0,0,10">
                <Grid>
                    <TextBlock Text="📝 DRAFTING NEW QUOTATION" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <Button Content="X Cancel" Command="{Binding CloseBuilderCommand}" Background="#A94044" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right" Padding="15,5" BorderThickness="0"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" BorderBrush="#A0A0A0" BorderThickness="1" Background="White" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Customer Information" FontWeight="Bold" Foreground="#555" Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="Name:" Width="80" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding ActiveQuote.CustomerName}" Margin="80,0,0,0" Style="{StaticResource ClassicInput}"/>
                        </Grid>
                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="Phone:" Width="80" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding ActiveQuote.CustomerPhone}" Margin="80,0,0,0" Style="{StaticResource ClassicInput}"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Email:" Width="80" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding ActiveQuote.CustomerEmail}" Margin="80,0,0,0" Style="{StaticResource ClassicInput}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" BorderBrush="#A0A0A0" BorderThickness="1" Background="White" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Quote Terms" FontWeight="Bold" Foreground="#555" Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="Valid Until:" Width="80" VerticalAlignment="Center"/>
                            <DatePicker SelectedDate="{Binding ActiveQuote.ValidUntil}" Margin="80,0,0,0" Height="26" BorderBrush="#808080"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Notes:" Width="80" VerticalAlignment="Top" Margin="0,5,0,0"/>
                            <TextBox Text="{Binding ActiveQuote.Notes}" Margin="80,0,0,0" Height="50" AcceptsReturn="True" TextWrapping="Wrap" Style="{StaticResource ClassicInput}"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2" BorderBrush="#A0A0A0" BorderThickness="1" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F0F0F0" Padding="10" BorderBrush="#A0A0A0" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="➕ Add Placeholder Item (For Testing)" Command="{Binding AddPlaceholderItemCommand}" Style="{StaticResource ClassicBtn}"/>
                        </StackPanel>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding BuilderLines}" AutoGenerateColumns="False" CanUserAddRows="False"
                              HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                              RowHeight="30" BorderThickness="0">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding ItemCode}" Width="120" IsReadOnly="True" Foreground="#666"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding ItemDescription}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="80" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="100" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount, UpdateSourceTrigger=PropertyChanged}" Width="100" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, Mode=OneWay, StringFormat=N2}" Width="120" IsReadOnly="True" ElementStyle="{StaticResource RightElement}" FontWeight="Bold"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Background="#EAEAEA" BorderBrush="#A0A0A0" BorderThickness="0,1,0,0" Padding="15">
                        <Grid>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Gross:" Foreground="#666" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding ActiveQuote.GrossTotal, StringFormat=N2}" FontWeight="Bold" Width="100" TextAlignment="Right" Margin="0,0,20,0"/>

                                <TextBlock Text="Discount:" Foreground="#A94044" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding ActiveQuote.TotalDiscount, StringFormat=N2}" Foreground="#A94044" FontWeight="Bold" Width="100" TextAlignment="Right" Margin="0,0,30,0"/>

                                <TextBlock Text="NET TOTAL:" FontSize="16" FontWeight="Bold" Foreground="#005500" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding ActiveQuote.NetTotal, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Bold" Foreground="#005500" Width="150" TextAlignment="Right" Margin="0,0,30,0"/>

                                <Button Content="💾 SAVE QUOTE" Command="{Binding SaveQuoteCommand}" Style="{StaticResource ActionBtn}" Padding="30,0" Height="40" FontSize="14"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Grid Background="#AA000000" Panel.ZIndex="999">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPrintPreviewOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Border Background="#D4D0C8" BorderBrush="#003366" BorderThickness="2" Width="900" Height="700" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="📄 QUOTATION PREVIEW" Style="{StaticResource HeaderLabel}" FontSize="18"/>
                        <Button Content="X" Command="{Binding ClosePrintPreviewCommand}" Width="30" Height="30" HorizontalAlignment="Right" Background="#A94044" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                    </Grid>

                    <Border Grid.Row="1" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" Padding="20">
                        <StackPanel>
                            <Grid Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="PROFORMA INVOICE / QUOTATION" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                                    <TextBlock Text="{Binding ActiveQuote.QuoteNo}" FontSize="16" FontWeight="SemiBold" Foreground="#005555"/>
                                </StackPanel>
                                <StackPanel HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding ActiveQuote.CustomerName}" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding ActiveQuote.CustomerPhone}"/>
                                    <TextBlock Text="{Binding ActiveQuote.DateCreated, StringFormat=Date: dd-MMM-yyyy}" Margin="0,10,0,0" Foreground="#666"/>
                                    <TextBlock Text="{Binding ActiveQuote.ValidUntil, StringFormat=Valid Until: dd-MMM-yyyy}" Foreground="#A94044" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>

                            <DataGrid ItemsSource="{Binding ActiveQuote.Lines}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" BorderBrush="#E0E0E0" Margin="0,0,0,20">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Description" Binding="{Binding ItemDescription}" Width="*"/>
                                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                                    <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=N2}" Width="100"/>
                                    <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat=N2}" Width="120" FontWeight="Bold"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock Text="{Binding ActiveQuote.GrossTotal, StringFormat=Gross: Rs. \{0:N2\}}" TextAlignment="Right" Foreground="#666"/>
                                <TextBlock Text="{Binding ActiveQuote.TotalDiscount, StringFormat=Discount: Rs. \{0:N2\}}" TextAlignment="Right" Foreground="#A94044"/>
                                <TextBlock Text="{Binding ActiveQuote.NetTotal, StringFormat=NET TOTAL: Rs. \{0:N2\}}" TextAlignment="Right" FontSize="22" FontWeight="Bold" Foreground="#005500" Margin="0,5,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Background="#EAEAEA" BorderBrush="#A0A0A0" BorderThickness="1" Padding="15" Margin="0,10,0,0">
                        <Grid>
                            <Button Content="🖨️ PRINT A4" Style="{StaticResource ClassicBtn}" HorizontalAlignment="Left" Width="150" Height="40"/>

                            <Button Content="⚡ CONVERT TO LIVE SALE" Command="{Binding ConvertToLiveSaleCommand}" Style="{StaticResource ConvertBtn}" HorizontalAlignment="Right" Width="250" ToolTip="Deducts stock and records revenue immediately"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>