<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.ReceiptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <!-- Standard Button Style -->
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Post Action Button (Green for Revenue/Money In) -->
        <Style TargetType="Button" x:Key="PostBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Auto-Allocate Button (Blue) -->
        <Style TargetType="Button" x:Key="AutoBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#2B5B84"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C3F5E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Standard Input Style -->
        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <!-- Group Box Border Style -->
        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header (Master Data) -->
            <RowDefinition Height="Auto"/>
            <!-- Payment Entry Bar -->
            <RowDefinition Height="*"/>
            <!-- Data Grid (Invoice Allocation) -->
            <RowDefinition Height="Auto"/>
            <!-- Footer (Settlement Math) -->
        </Grid.RowDefinitions>

        <!-- ========================================== -->
        <!-- ZONE 1: DOCUMENT HEADER (MASTER)           -->
        <!-- ========================================== -->
        <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Margin="0,0,0,15">
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Customer Receipt (Debtor Settlement)" FontSize="20" FontWeight="SemiBold" Foreground="#28A745"/>

                    <Border Grid.Column="1" Background="#FFF3CD" BorderBrush="#FFEEBA" BorderThickness="1" CornerRadius="4" Padding="10,5">
                        <TextBlock Text="Status: DRAFT" FontWeight="Bold" Foreground="#856404"/>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Customer Selection & Outstanding Debt Badge -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Customer / Debtor *" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbCustomer" Style="{StaticResource StandardInput}" IsEditable="True" Margin="0,0,0,5" ToolTip="Selecting a customer automatically loads their unpaid invoices"/>

                        <Border Background="#FDF3F3" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="2" Padding="5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="Total Outstanding Debt:" Foreground="#A94044" FontSize="12" Margin="0,0,10,0"/>
                                <TextBlock x:Name="txtTotalDebt" Text="Rs. 0.00" Foreground="#A94044" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Receipt Date *" Foreground="#666666" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpReceiptDate" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Receipt No." Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtReceiptNo" Style="{StaticResource StandardInput}" Background="#EEEEEE" IsReadOnly="True" Text="[Auto-Generated]"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- ========================================== -->
        <!-- ZONE 2: PAYMENT ENTRY BAR (MONEY IN)       -->
        <!-- ========================================== -->
        <Border Grid.Row="1" Background="#F4FAF6" BorderBrush="#C3E6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Physical Payment Received" FontWeight="SemiBold" Foreground="#28A745" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Payment Method *" Foreground="#333333" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbPaymentMethod" Style="{StaticResource StandardInput}" Margin="0">
                            <ComboBoxItem Content="Cash"/>
                            <ComboBoxItem Content="Cheque" IsSelected="True"/>
                            <ComboBoxItem Content="Bank Transfer"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Total Amount Received *" Foreground="#2B5B84" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtAmountReceived" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Right" Background="#FFFFE0" FontWeight="Bold" Margin="0" Text="0.00"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Ref No. (Cheque / Slip ID)" Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtReferenceNo" Style="{StaticResource StandardInput}" Margin="0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Bank Name (If Cheque)" Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtBankDetails" Style="{StaticResource StandardInput}" Margin="0"/>
                    </StackPanel>

                    <!-- Auto Allocate Feature -->
                    <Button Grid.Column="8" x:Name="btnAutoAllocate" Content="Auto-Allocate" Style="{StaticResource AutoBtn}" Margin="0" VerticalAlignment="Bottom" Height="30" Width="110" ToolTip="Automatically distributes the Received Amount to the oldest unpaid invoices"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- ========================================== -->
        <!-- ZONE 3: INVOICE ALLOCATION GRID            -->
        <!-- ========================================== -->
        <Border Grid.Row="2" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
            <DockPanel>
                <Border DockPanel.Dock="Top" Background="#F8F8F8" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="15,10">
                    <TextBlock Text="Unpaid Invoices for Allocation" FontWeight="SemiBold" Foreground="#333333"/>
                </Border>

                <DataGrid x:Name="dgUnpaidInvoices" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False" 
                          HeadersVisibility="Column" 
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#EEEEEE"
                          BorderThickness="0"
                          RowHeight="40"
                          Background="Transparent">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FFFFFF"/>
                            <Setter Property="Foreground" Value="#666666"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="15,10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#DDDDDD"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Invoice No." Binding="{Binding InvoiceNo}" Width="120" IsReadOnly="True" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Inv Date" Binding="{Binding InvoiceDate, StringFormat=dd/MM/yyyy}" Width="100" IsReadOnly="True"/>

                        <!-- Due Date (Visual Cue for Overdue) -->
                        <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=dd/MM/yyyy}" Width="100" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#A94044"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Original Amt" Binding="{Binding OriginalAmount, StringFormat=N2}" Width="110" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Foreground" Value="#666666"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Outstanding Balance" Binding="{Binding OutstandingBalance, StringFormat=N2}" Width="140" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#A94044"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- THE ONLY EDITABLE COLUMN -->
                        <DataGridTemplateColumn Header="Paying Now (Rs.)" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding AllocatedAmount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" 
                                             Background="#FFFFE0" BorderBrush="#CCCCCC" BorderThickness="1" 
                                             HorizontalContentAlignment="Right" VerticalContentAlignment="Center" 
                                             Padding="5" FontWeight="Bold" Foreground="#28A745"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- ========================================== -->
        <!-- ZONE 4: FOOTER (SETTLEMENT MATH & ACTIONS) -->
        <!-- ========================================== -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Settlement Math Block -->
            <Border Grid.Column="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,20,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Payment Notes / Remarks:" Foreground="#666666" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtRemarks" Height="50" TextWrapping="Wrap" AcceptsReturn="True" BorderBrush="#CCCCCC" BorderThickness="1" Padding="5"/>
                    </StackPanel>

                    <Grid Grid.Column="2" HorizontalAlignment="Right" MinWidth="300">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0" Margin="0,0,0,5">
                            <TextBlock Text="Total Received:" Foreground="#333333"/>
                            <TextBlock x:Name="txtSummaryReceived" Text="Rs. 0.00" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#333333"/>
                        </DockPanel>

                        <DockPanel Grid.Row="1" Margin="0,0,0,10">
                            <TextBlock Text="Total Allocated to Invoices:" Foreground="#28A745"/>
                            <TextBlock x:Name="txtSummaryAllocated" Text="Rs. 0.00" FontWeight="Bold" HorizontalAlignment="Right" Foreground="#28A745"/>
                        </DockPanel>

                        <!-- Advance / Unallocated Credit -->
                        <Border Grid.Row="2" Background="#E2EDF8" BorderBrush="#B6D4FE" BorderThickness="1" Padding="5,2" CornerRadius="2">
                            <DockPanel>
                                <TextBlock Text="Unallocated Balance (Advance):" Foreground="#2B5B84" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtUnallocated" Text="Rs. 0.00" FontWeight="Bold" FontSize="16" Foreground="#2B5B84" HorizontalAlignment="Right" ToolTip="This amount will remain as a positive credit on the customer's account"/>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button Content="Clear Form" Style="{StaticResource StandardBtn}" Background="#777777" Width="100"/>
                <Button Content="POST RECEIPT" Style="{StaticResource PostBtn}" Width="160" Margin="0" ToolTip="Saves payment and clears allocated invoice debts"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>