<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.FloatCashLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBox" x:Key="ClassicInput">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DatePicker" x:Key="ClassicDate">
            <Setter Property="Height" Value="26"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Width" Value="130"/>
        </Style>

        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007777"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Border" x:Key="KpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
        </Style>

        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#005555"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="RightElement">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="RightBoldElement" BasedOn="{StaticResource RightElement}">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="DangerElement" BasedOn="{StaticResource RightBoldElement}">
            <Setter Property="Foreground" Value="#A94044"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="ProfitElement" BasedOn="{StaticResource RightBoldElement}">
            <Setter Property="Foreground" Value="#005500"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="TILL AUDIT FILTERS:" Style="{StaticResource HeaderLabel}" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1" Text="From:" Style="{StaticResource InlineLabel}"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding StartDate}" Style="{StaticResource ClassicDate}" Margin="0,0,15,0"/>

                <TextBlock Grid.Column="3" Text="To:" Style="{StaticResource InlineLabel}"/>
                <DatePicker Grid.Column="4" SelectedDate="{Binding EndDate}" Style="{StaticResource ClassicDate}" Margin="0,0,20,0"/>

                <TextBox Grid.Column="5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" Style="{StaticResource ClassicInput}" Margin="0,0,10,0" ToolTip="Search Cashier or Terminal"/>

                <Button Grid.Column="6" Content="⚡ RUN AUDIT" Command="{Binding GenerateAuditCommand}" Style="{StaticResource ActionBtn}" HorizontalAlignment="Left" Padding="20,0" FontSize="13"/>

                <Button Grid.Column="7" Content="Clear Filters" Command="{Binding ClearFiltersCommand}" Style="{StaticResource ClassicBtn}" Margin="0,0,10,0"/>
                <Button Grid.Column="8" Content="📊 Export Report" Command="{Binding ExportToExcelCommand}" Style="{StaticResource ClassicBtn}"/>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,8">
            <Border Style="{StaticResource KpiCard}">
                <StackPanel>
                    <TextBlock Text="TOTAL EXPECTED CASH (SYSTEM)" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="{Binding TotalExpectedCash, StringFormat=Rs. \{0:N2\}}" FontSize="22" FontWeight="Bold" Foreground="#003366" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}">
                <StackPanel>
                    <TextBlock Text="TOTAL ACTUAL CASH (COUNTED)" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="{Binding TotalActualCash, StringFormat=Rs. \{0:N2\}}" FontSize="22" FontWeight="Bold" Foreground="#555" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" BorderBrush="#005555" Background="#F0F8FF">
                <StackPanel>
                    <TextBlock Text="NET STORE VARIANCE" FontSize="11" FontWeight="Bold" Foreground="#005555"/>
                    <TextBlock Text="{Binding NetVariance, StringFormat=Rs. \{0:N2\}}" FontSize="22" FontWeight="Bold" Foreground="#005555" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" BorderBrush="#A94044" Margin="0">
                <StackPanel>
                    <TextBlock Text="SHIFTS WITH DISCREPANCIES" FontSize="11" FontWeight="Bold" Foreground="#A94044"/>
                    <TextBlock Text="{Binding DiscrepancyCount}" FontSize="22" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0" ToolTip="Number of shifts where Expected Cash did not match Actual Cash"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="White" BorderBrush="#A0A0A0" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Shifts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                      RowHeight="30" Background="White" BorderThickness="0" SelectionMode="Single" AlternatingRowBackground="#F9F9F9">

                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenDrillDownCommand}" CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </DataGrid.InputBindings>

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="6"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#A0A0A0"/>
                    </Style>
                    <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Shift ID" Binding="{Binding ShiftId}" Width="80" ElementStyle="{StaticResource RightBoldElement}" Foreground="#005555"/>
                    <DataGridTextColumn Header="Terminal" Binding="{Binding TerminalNo}" Width="120" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Cashier Name" Binding="{Binding CashierName}" Width="200" FontWeight="Bold"/>

                    <DataGridTextColumn Header="Opened At" Binding="{Binding OpenTime, StringFormat=dd-MMM-yyyy HH:mm}" Width="150" Foreground="#555"/>
                    <DataGridTextColumn Header="Closed At" Binding="{Binding CloseTime, StringFormat=dd-MMM-yyyy HH:mm}" Width="150" Foreground="#555"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" FontWeight="Bold"/>

                    <DataGridTextColumn Header="Expected Cash" Binding="{Binding ExpectedCash, StringFormat=N2}" Width="130" ElementStyle="{StaticResource RightBoldElement}" Foreground="#003366"/>
                    <DataGridTextColumn Header="Actual Counted" Binding="{Binding ActualCash, StringFormat=N2}" Width="130" ElementStyle="{StaticResource RightBoldElement}" Foreground="#555"/>

                    <DataGridTextColumn Header="Variance" Binding="{Binding Variance, StringFormat=N2}" Width="*" ElementStyle="{StaticResource DangerElement}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.RowSpan="3" Background="#AA000000" Panel.ZIndex="999">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDrillDownOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Border Background="#D4D0C8" BorderBrush="#003366" BorderThickness="2" Width="950" Height="650" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="5"/>
                </Border.Effect>
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="🔍 SHIFT CASH MOVEMENT LEDGER" Style="{StaticResource HeaderLabel}" FontSize="16" Margin="0"/>
                        <Button Content="X" Command="{Binding CloseDrillDownCommand}" Width="30" Height="30" HorizontalAlignment="Right" Background="#A94044" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    </Grid>

                    <Border Grid.Row="1" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" Padding="15" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="Terminal: " Foreground="#666" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding SelectedShift.TerminalNo}" FontWeight="Bold" FontSize="16" Foreground="#005555" Margin="0,0,20,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="Cashier: " Foreground="#666" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding SelectedShift.CashierName}" FontWeight="Bold" FontSize="16" Foreground="#333" Margin="0,0,20,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="Calculated Variance: " Foreground="#666" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding SelectedShift.Variance, StringFormat=Rs. \{0:N2\}}" FontWeight="Bold" FontSize="16" Foreground="#A94044" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding ShiftMovements}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                              RowHeight="28" Background="White" BorderThickness="1" BorderBrush="#A0A0A0" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="100" Foreground="#555" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Movement Type" Binding="{Binding MovementType}" Width="150" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Reference No." Binding="{Binding ReferenceNo}" Width="150"/>
                            <DataGridTextColumn Header="Amount (Rs.)" Binding="{Binding Amount, StringFormat=N2}" Width="120" ElementStyle="{StaticResource RightBoldElement}"/>
                            <DataGridTextColumn Header="Remarks / Details" Binding="{Binding Remarks}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>