<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.SalesExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}" FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>
        <Style x:Key="SelectedCellStyle" TargetType="DataGridCell"><Setter Property="Padding" Value="4,1"/><Setter Property="Foreground" Value="Black"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource LightSelectionBrush}"/><Setter Property="Foreground" Value="Black"/></Trigger></Style.Triggers></Style>
        <Style x:Key="CompactButton" TargetType="Button"><Setter Property="Height" Value="27"/><Setter Property="Padding" Value="12,0"/><Setter Property="Margin" Value="4,0,0,0"/></Style>
    </UserControl.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="45*"/><RowDefinition Height="55*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource ActionBackgroundBrush}" Padding="9"><TextBlock Text="SALES EXPLORER" Foreground="White" FontSize="15" FontWeight="Bold"/></Border>
        <Border Grid.Row="1" Margin="0,6" Padding="7" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <WrapPanel>
                <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,4,0"/><DatePicker SelectedDate="{Binding StartDate}" Width="115" Height="25"/>
                <TextBlock Text="To" VerticalAlignment="Center" Margin="8,0,4,0"/><DatePicker SelectedDate="{Binding EndDate}" Width="115" Height="25"/>
                <TextBlock Text="Search" VerticalAlignment="Center" Margin="8,0,4,0"/><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="190" Height="25" Padding="4,2"/>
                <TextBlock Text="Terminal" VerticalAlignment="Center" Margin="8,0,4,0"/><TextBox Text="{Binding TerminalFilter}" Width="80" Height="25" Padding="4,2"/>
                <TextBlock Text="Return" VerticalAlignment="Center" Margin="8,0,4,0"/><ComboBox ItemsSource="{Binding ReturnStatuses}" SelectedItem="{Binding SelectedReturnStatus}" Width="135" Height="25"/>
                <Button Content="Load" Command="{Binding ApplyFiltersCommand}" Style="{StaticResource CompactButton}"/><Button Content="Reset" Command="{Binding ResetCommand}" Style="{StaticResource CompactButton}"/>
            </WrapPanel>
        </Border>
        <UniformGrid Grid.Row="2" Columns="4" Margin="0,0,0,6">
            <Border Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6" Margin="0,0,5,0"><StackPanel><TextBlock Text="Net Sales" Foreground="Gray"/><TextBlock Text="{Binding SummaryNetSales, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6" Margin="0,0,5,0"><StackPanel><TextBlock Text="Returns" Foreground="Gray"/><TextBlock Text="{Binding SummaryReturns, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6" Margin="0,0,5,0"><StackPanel><TextBlock Text="Net After Returns" Foreground="Gray"/><TextBlock Text="{Binding SummaryNetAfterReturns, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6"><StackPanel><TextBlock Text="Gross Profit" Foreground="Gray"/><TextBlock Text="{Binding SummaryGrossProfit, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
        </UniformGrid>
        <DataGrid Grid.Row="3" ItemsSource="{Binding Sales}" SelectedItem="{Binding SelectedSale}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="25" CellStyle="{StaticResource SelectedCellStyle}" AlternatingRowBackground="{DynamicResource DataGridAlternatingRow}" GridLinesVisibility="All">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNo}" Width="120"/><DataGridTextColumn Header="Date" Binding="{Binding TransactionDate, StringFormat=yyyy-MM-dd HH:mm}" Width="135"/><DataGridTextColumn Header="Terminal" Binding="{Binding TerminalNo}" Width="70"/><DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*" MinWidth="150"/><DataGridTextColumn Header="Cashier" Binding="{Binding CashierName}" Width="110"/><DataGridTextColumn Header="Payments" Binding="{Binding PaymentMethods}" Width="140"/><DataGridTextColumn Header="Net" Binding="{Binding NetAmount, StringFormat=N2}" Width="90"/><DataGridTextColumn Header="Returned" Binding="{Binding ReturnedAmount, StringFormat=N2}" Width="90"/><DataGridTextColumn Header="Profit" Binding="{Binding GrossProfit, StringFormat=N2}" Width="90"/><DataGridTextColumn Header="Return Status" Binding="{Binding ReturnStatus}" Width="115"/><DataGridTextColumn Header="Tax Invoice" Binding="{Binding TaxInvoiceNo}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <TabControl Grid.Row="4" Margin="0,6,0,0">
            <TabItem Header="Sale Lines and VAT">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <WrapPanel Grid.Row="0" Margin="5">
                        <TextBlock Text="Invoice:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.InvoiceNo}" Margin="4,0,14,0"/>
                        <TextBlock Text="Tax Invoice:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.TaxInvoiceNo}" Margin="4,0,14,0"/>
                        <TextBlock Text="Gross:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.GrossAmount, StringFormat={}Rs. {0:N2}}" Margin="4,0,14,0"/>
                        <TextBlock Text="Discount:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.TotalDiscount, StringFormat={}Rs. {0:N2}}" Margin="4,0,14,0"/>
                        <TextBlock Text="VAT:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.TotalVatAmount, StringFormat={}Rs. {0:N2}}" Margin="4,0,14,0"/>
                        <TextBlock Text="Net:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.NetAmount, StringFormat={}Rs. {0:N2}}" Margin="4,0,14,0"/>
                        <TextBlock Text="Tax Status:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.TaxSnapshotStatus}" Margin="4,0,0,0"/>
                    </WrapPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedDetails.Lines}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="24" CellStyle="{StaticResource SelectedCellStyle}" GridLinesVisibility="All">
                        <DataGrid.Columns><DataGridTextColumn Header="SKU" Binding="{Binding ItemCode}" Width="95"/><DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/><DataGridTextColumn Header="Type" Binding="{Binding ItemType}" Width="80"/><DataGridTextColumn Header="Qty" Binding="{Binding Qty, StringFormat=N3}" Width="65"/><DataGridTextColumn Header="Returned" Binding="{Binding ReturnedQty, StringFormat=N3}" Width="75"/><DataGridTextColumn Header="Unit" Binding="{Binding UnitPrice, StringFormat=N2}" Width="80"/><DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount, StringFormat=N2}" Width="80"/><DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=N2}" Width="90"/><DataGridTextColumn Header="Tax" Binding="{Binding TaxCategory}" Width="85"/><DataGridTextColumn Header="Rate %" Binding="{Binding TaxRatePercent, StringFormat=N4}" Width="70"/><DataGridTextColumn Header="Taxable" Binding="{Binding TaxableAmount, StringFormat=N2}" Width="85"/><DataGridTextColumn Header="VAT" Binding="{Binding VatAmount, StringFormat=N2}" Width="75"/><DataGridTextColumn Header="Return Status" Binding="{Binding ReturnStatus}" Width="110"/></DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Payments">
                <DataGrid ItemsSource="{Binding SelectedDetails.Payments}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="24" CellStyle="{StaticResource SelectedCellStyle}"><DataGrid.Columns><DataGridTextColumn Header="Type" Binding="{Binding PaymentType}" Width="110"/><DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}" Width="100"/><DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*"/><DataGridTextColumn Header="Entered By" Binding="{Binding EnteredBy}" Width="120"/><DataGridTextColumn Header="Terminal" Binding="{Binding TerminalNo}" Width="80"/></DataGrid.Columns></DataGrid>
            </TabItem>
            <TabItem Header="Credit Notes">
                <DataGrid ItemsSource="{Binding SelectedDetails.CreditNotes}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="24" CellStyle="{StaticResource SelectedCellStyle}"><DataGrid.Columns><DataGridTextColumn Header="Credit Note" Binding="{Binding CreditNoteNo}" Width="140"/><DataGridTextColumn Header="Return Date" Binding="{Binding ReturnDate, StringFormat=yyyy-MM-dd HH:mm}" Width="145"/><DataGridTextColumn Header="Refund" Binding="{Binding RefundAmount, StringFormat=N2}" Width="100"/><DataGridTextColumn Header="Method" Binding="{Binding RefundMethod}" Width="120"/><DataGridTextColumn Header="Authorized By" Binding="{Binding AuthorizedBy}" Width="150"/></DataGrid.Columns></DataGrid>
            </TabItem>
            <TabItem Header="Receipt / Tax Invoice Audit">
                <DataGrid ItemsSource="{Binding SelectedDetails.DocumentAudits}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="24" CellStyle="{StaticResource SelectedCellStyle}"><DataGrid.Columns><DataGridTextColumn Header="Time" Binding="{Binding OccurredAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/><DataGridTextColumn Header="Document" Binding="{Binding DocumentType}" Width="95"/><DataGridTextColumn Header="Number" Binding="{Binding DocumentNumber}" Width="130"/><DataGridTextColumn Header="Event" Binding="{Binding EventType}" Width="105"/><DataGridCheckBoxColumn Header="Success" Binding="{Binding IsSuccessful}" Width="65"/><DataGridTextColumn Header="Copy" Binding="{Binding CopyNumber}" Width="55"/><DataGridTextColumn Header="Performed By" Binding="{Binding PerformedBy}" Width="120"/><DataGridTextColumn Header="Printer" Binding="{Binding PrinterName}" Width="120"/><DataGridTextColumn Header="Error" Binding="{Binding ErrorMessage}" Width="*"/></DataGrid.Columns></DataGrid>
            </TabItem>
        </TabControl>
        <DockPanel Grid.Row="5" Margin="0,5,0,0"><StackPanel Orientation="Horizontal" DockPanel.Dock="Right"><ProgressBar Width="100" Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,8,0"/><Button Content="Previous" Command="{Binding PreviousPageCommand}" IsEnabled="{Binding CanGoPrevious}" Style="{StaticResource CompactButton}"/><TextBlock Text="Page" VerticalAlignment="Center" Margin="10,0,3,0"/><TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center"/><TextBlock Text="of" VerticalAlignment="Center" Margin="3,0"/><TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center"/><Button Content="Next" Command="{Binding NextPageCommand}" IsEnabled="{Binding CanGoNext}" Style="{StaticResource CompactButton}"/></StackPanel><TextBlock Text="No completed sales match the selected filters." Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,12,0"/><TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/></DockPanel>
    </Grid>
</UserControl>
