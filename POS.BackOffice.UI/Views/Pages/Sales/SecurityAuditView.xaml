<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.SecurityAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="AuditCellStyle" TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource LightSelectionBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SummaryBox" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,7">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Security Audit" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}"/>
                <TextBlock Text="Operational events and activity that may require review" Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>
            <TextBlock DockPanel.Dock="Right" Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </DockPanel>

        <Border Grid.Row="1" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="7" Margin="0,0,0,7">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <DatePicker SelectedDate="{Binding StartDate}" Width="125" Height="27"/>
                <TextBlock Text="To" VerticalAlignment="Center" Margin="10,0,4,0"/>
                <DatePicker SelectedDate="{Binding EndDate}" Width="125" Height="27"/>
                <TextBlock Text="Search" VerticalAlignment="Center" Margin="10,0,4,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="235" Height="27" Padding="5,3"/>
                <Button Content="Load" Command="{Binding LoadCommand}" Width="80" Height="27" Margin="8,0,0,0"/>
                <Button Content="Reset" Command="{Binding ResetCommand}" Width="80" Height="27" Margin="5,0,0,0"/>
            </WrapPanel>
        </Border>

        <UniformGrid Grid.Row="2" Rows="1" Columns="7" Margin="0,0,0,7">
            <Border Style="{StaticResource SummaryBox}"><StackPanel><TextBlock Text="Events" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding TotalEventCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SummaryBox}"><StackPanel><TextBlock Text="Failed Logins" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding FailedLoginCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SummaryBox}"><StackPanel><TextBlock Text="Cancelled Carts" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding CancelledCartCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SummaryBox}"><StackPanel><TextBlock Text="Returns" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding CustomerReturnCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SummaryBox}"><StackPanel><TextBlock Text="Approvals" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding ApprovalEventCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SummaryBox}"><StackPanel><TextBlock Text="Drawer Failures" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding DrawerFailureCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SummaryBox}" Margin="0"><StackPanel><TextBlock Text="Activity Review" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding UnusualCashierCount}" FontWeight="Bold"/></StackPanel></Border>
        </UniformGrid>

        <TabControl Grid.Row="3">
            <TabItem Header="Audit Events">
                <Grid>
                    <DataGrid ItemsSource="{Binding Events}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                              CanUserAddRows="False" GridLinesVisibility="Horizontal" RowHeaderWidth="0" CellStyle="{StaticResource AuditCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date / Time" Binding="{Binding OccurredAt, StringFormat={}{0:dd-MMM-yyyy HH:mm}}" Width="145"/>
                            <DataGridTextColumn Header="Event" Binding="{Binding EventType}" Width="150"/>
                            <DataGridTextColumn Header="Level" Binding="{Binding Severity}" Width="90"/>
                            <DataGridTextColumn Header="Actor" Binding="{Binding Actor}" Width="120"/>
                            <DataGridTextColumn Header="Authorized By" Binding="{Binding AuthorizedBy}" Width="120"/>
                            <DataGridTextColumn Header="Terminal" Binding="{Binding TerminalNo}" Width="80"/>
                            <DataGridTextColumn Header="Reference" Binding="{Binding ReferenceNo}" Width="120"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}Rs. {0:N2}}" Width="100"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" MinWidth="220"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Text="No audit events match the selected filters." HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>
            </TabItem>
            <TabItem Header="Cashier Activity">
                <DataGrid ItemsSource="{Binding CashierActivity}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          GridLinesVisibility="Horizontal" RowHeaderWidth="0" CellStyle="{StaticResource AuditCellStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Cashier" Binding="{Binding CashierName}" Width="*" MinWidth="150"/>
                        <DataGridTextColumn Header="Sales" Binding="{Binding CompletedSales}" Width="80"/>
                        <DataGridTextColumn Header="Cancelled" Binding="{Binding CancelledCarts}" Width="90"/>
                        <DataGridTextColumn Header="Returns" Binding="{Binding CustomerReturns}" Width="80"/>
                        <DataGridTextColumn Header="Discounts" Binding="{Binding ManualDiscounts}" Width="85"/>
                        <DataGridTextColumn Header="Overrides" Binding="{Binding PriceOverrides}" Width="85"/>
                        <DataGridTextColumn Header="Free Issues" Binding="{Binding FreeIssues}" Width="85"/>
                        <DataGridTextColumn Header="Return Rate" Binding="{Binding ReturnRatePercent, StringFormat={}{0:N2}%}" Width="90"/>
                        <DataGridTextColumn Header="Review" Binding="{Binding ReviewNote}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <ProgressBar Grid.Row="4" Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
