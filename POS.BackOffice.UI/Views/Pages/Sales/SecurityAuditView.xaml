<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.SecurityAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#A94044"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
        <Style TargetType="DatePicker" x:Key="ClassicDate">
            <Setter Property="Height" Value="26"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Width" Value="130"/>
        </Style>
        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>
        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#A94044"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style TargetType="Border" x:Key="SecurityKpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FFF8F8"/>
            <Setter Property="BorderBrush" Value="#DCA6A8"/>
        </Style>
        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="RightElement">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="HighRiskText">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsHighRisk}" Value="True">
                    <Setter Property="Foreground" Value="#A94044"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock" x:Key="SelfAuthText">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelfAuthorized}" Value="True">
                    <Setter Property="Foreground" Value="#A94044"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🔒 SECURITY &amp; FRAUD AUDIT:" Style="{StaticResource HeaderLabel}" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1" Text="From:" Style="{StaticResource InlineLabel}"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding StartDate}" Style="{StaticResource ClassicDate}" Margin="0,0,15,0"/>

                <TextBlock Grid.Column="3" Text="To:" Style="{StaticResource InlineLabel}"/>
                <DatePicker Grid.Column="4" SelectedDate="{Binding EndDate}" Style="{StaticResource ClassicDate}" Margin="0,0,20,0"/>

                <Button Grid.Column="5" Content="⚡ RUN SECURITY SCAN" Command="{Binding ApplyFiltersCommand}" Style="{StaticResource ActionBtn}" HorizontalAlignment="Left" Padding="20,0"/>

                <Button Grid.Column="6" Content="Reset Filters" Command="{Binding ClearFiltersCommand}" Style="{StaticResource ClassicBtn}" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="5" Margin="-5,0,-5,10">
            <Border Style="{StaticResource SecurityKpiCard}" Background="#FFEEEE" BorderBrush="#A94044" BorderThickness="2">
                <StackPanel>
                    <TextBlock Text="HIGH RISK CASHIERS" FontSize="11" FontWeight="Bold" Foreground="#A94044"/>
                    <TextBlock Text="{Binding HighRiskCashierCount}" FontSize="24" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SecurityKpiCard}">
                <StackPanel>
                    <TextBlock Text="TOTAL VOIDS (COUNT)" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="{Binding TotalVoidCount}" FontSize="20" FontWeight="Bold" Foreground="#333" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SecurityKpiCard}">
                <StackPanel>
                    <TextBlock Text="VOIDED VALUE (RS.)" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="{Binding TotalVoidAmount, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SecurityKpiCard}">
                <StackPanel>
                    <TextBlock Text="TOTAL RETURNS (COUNT)" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="{Binding TotalReturnCount}" FontSize="20" FontWeight="Bold" Foreground="#333" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SecurityKpiCard}">
                <StackPanel>
                    <TextBlock Text="REFUNDED VALUE (RS.)" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="{Binding TotalReturnAmount, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <TabControl Grid.Row="2" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" Padding="0">
            <TabItem Header="👤 Cashier Risk Profiles" FontSize="14" FontWeight="SemiBold">
                <DataGrid ItemsSource="{Binding CashierRiskProfiles}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                          RowHeight="30" BorderThickness="0" AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E0E0E0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#A0A0A0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Cashier Name" Binding="{Binding CashierName}" Width="200" FontWeight="Bold" ElementStyle="{StaticResource HighRiskText}"/>
                        <DataGridTextColumn Header="Total Traffic" Binding="{Binding TotalTransactions}" Width="120" ElementStyle="{StaticResource RightElement}"/>

                        <DataGridTextColumn Header="Void Count" Binding="{Binding VoidCount}" Width="100" ElementStyle="{StaticResource RightElement}"/>
                        <DataGridTextColumn Header="Void Rate %" Binding="{Binding VoidRate, StringFormat=\{0:N2\}%}" Width="120" ElementStyle="{StaticResource RightElement}" FontWeight="Bold"/>

                        <DataGridTextColumn Header="Return Count" Binding="{Binding ReturnCount}" Width="120" ElementStyle="{StaticResource RightElement}"/>
                        <DataGridTextColumn Header="Return Rate %" Binding="{Binding ReturnRate, StringFormat=\{0:N2\}%}" Width="120" ElementStyle="{StaticResource RightElement}" FontWeight="Bold"/>

                        <DataGridCheckBoxColumn Header="FLAG: HIGH RISK" Binding="{Binding IsHighRisk}" Width="150" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="💵 Return Audit Ledger" FontSize="14" FontWeight="SemiBold">
                <DataGrid ItemsSource="{Binding ReturnRecords}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                          RowHeight="28" BorderThickness="0" AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E0E0E0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#A0A0A0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Return Date" Binding="{Binding ReturnDate, StringFormat=dd-MMM-yyyy HH:mm}" Width="140" Foreground="#555"/>
                        <DataGridTextColumn Header="Return No." Binding="{Binding ReturnNo}" Width="150" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Orig. Invoice No." Binding="{Binding OriginalInvoiceNo}" Width="150"/>
                        <DataGridTextColumn Header="Cashier" Binding="{Binding CashierName}" Width="150"/>

                        <DataGridTextColumn Header="Authorized By" Binding="{Binding AuthorizedBy}" Width="150" ElementStyle="{StaticResource SelfAuthText}"/>

                        <DataGridTextColumn Header="Refund Method" Binding="{Binding RefundMethod}" Width="120"/>
                        <DataGridTextColumn Header="Refund Amount" Binding="{Binding RefundAmount, StringFormat=Rs. \{0:N2\}}" Width="140" ElementStyle="{StaticResource RightElement}" FontWeight="Bold" Foreground="#A94044"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="🗑️ Voids &amp; Suspended Ledger" FontSize="14" FontWeight="SemiBold">
                <DataGrid ItemsSource="{Binding VoidRecords}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                          RowHeight="28" BorderThickness="0" AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E0E0E0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#A0A0A0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date &amp; Time" Binding="{Binding TransactionDate, StringFormat=dd-MMM-yyyy HH:mm}" Width="140" Foreground="#555"/>
                        <DataGridTextColumn Header="Invoice No." Binding="{Binding InvoiceNo}" Width="150" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Cashier Name" Binding="{Binding CashierName}" Width="180"/>
                        <DataGridTextColumn Header="Terminal" Binding="{Binding TerminalNo}" Width="100"/>

                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Attempted Amount" Binding="{Binding AttemptedAmount, StringFormat=Rs. \{0:N2\}}" Width="150" ElementStyle="{StaticResource RightElement}" Foreground="#666"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

    </Grid>
</UserControl>