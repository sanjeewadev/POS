<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Sales.ItemSalesAnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}" FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>
        <Style x:Key="SelectedCellStyle" TargetType="DataGridCell"><Setter Property="Padding" Value="4,1"/><Setter Property="Foreground" Value="Black"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource LightSelectionBrush}"/><Setter Property="Foreground" Value="Black"/></Trigger></Style.Triggers></Style>
        <Style x:Key="CompactButton" TargetType="Button"><Setter Property="Height" Value="27"/><Setter Property="Padding" Value="12,0"/><Setter Property="Margin" Value="4,0,0,0"/></Style>
        <Style x:Key="KpiBorder" TargetType="Border"><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="7"/><Setter Property="Margin" Value="0,0,5,0"/></Style>
    </UserControl.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource ActionBackgroundBrush}" Padding="9"><TextBlock Text="ITEM SALES ANALYSIS" Foreground="White" FontSize="15" FontWeight="Bold"/></Border>
        <Border Grid.Row="1" Margin="0,6" Padding="7" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <WrapPanel>
                <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,4,0"/><DatePicker SelectedDate="{Binding StartDate}" Width="120" Height="25"/>
                <TextBlock Text="To" VerticalAlignment="Center" Margin="10,0,4,0"/><DatePicker SelectedDate="{Binding EndDate}" Width="120" Height="25"/>
                <TextBlock Text="Item / SKU / Category" VerticalAlignment="Center" Margin="10,0,4,0"/><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="230" Height="25" Padding="4,2"/>
                <Button Content="Load" Command="{Binding LoadCommand}" Style="{StaticResource CompactButton}"/><Button Content="Reset" Command="{Binding ResetCommand}" Style="{StaticResource CompactButton}"/>
                <Button Content="Save PDF" Command="{Binding ExportPdfCommand}" Width="86" Height="27" Margin="5,0,0,0" ToolTip="Manager or Administrator access required."/>
                <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Width="90" Height="27" Margin="5,0,0,0" ToolTip="Manager or Administrator access required."/>
            </WrapPanel>
        </Border>
        <UniformGrid Grid.Row="2" Columns="8" Margin="0,0,0,6">
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Gross Sales" Foreground="Gray"/><TextBlock Text="{Binding GrossSales, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Discounts" Foreground="Gray"/><TextBlock Text="{Binding Discounts, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Returns" Foreground="Gray"/><TextBlock Text="{Binding ReturnValue, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Net Sales" Foreground="Gray"/><TextBlock Text="{Binding NetSales, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Gross Profit" Foreground="Gray"/><TextBlock Text="{Binding GrossProfit, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Sold Qty" Foreground="Gray"/><TextBlock Text="{Binding SoldQuantity, StringFormat={}{0:#,##0.###}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}"><StackPanel><TextBlock Text="Returned Qty" Foreground="Gray"/><TextBlock Text="{Binding ReturnedQuantity, StringFormat={}{0:#,##0.###}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource KpiBorder}" Margin="0"><StackPanel><TextBlock Text="Slow / Non-Selling" Foreground="Gray"/><TextBlock Text="{Binding SlowOrNonSellingCount}" FontWeight="Bold"/></StackPanel></Border>
        </UniformGrid>
        <TabControl Grid.Row="3">
            <TabItem Header="All Items and Services">
                <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="25" CellStyle="{StaticResource SelectedCellStyle}" AlternatingRowBackground="{DynamicResource DataGridAlternatingRow}" GridLinesVisibility="All">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Rank}" Width="45"/>
                        <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="90"/>
                        <DataGridTextColumn Header="SKU" Binding="{Binding SkuCode}" Width="105"/>
                        <DataGridTextColumn Header="Item / Service" Binding="{Binding ItemName}" Width="*" MinWidth="180"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding ItemType}" Width="85"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="110"/>
                        <DataGridTextColumn Header="Sold" Binding="{Binding SoldQuantity, StringFormat={}{0:#,##0.###}}" Width="70"/>
                        <DataGridTextColumn Header="Returned" Binding="{Binding ReturnedQuantity, StringFormat={}{0:#,##0.###}}" Width="75"/>
                        <DataGridTextColumn Header="Net Qty" Binding="{Binding NetQuantity, StringFormat={}{0:#,##0.###}}" Width="75"/>
                        <DataGridTextColumn Header="Net Sales" Binding="{Binding NetSales, StringFormat=N2}" Width="90"/>
                        <DataGridTextColumn Header="Net Cost" Binding="{Binding NetCost, StringFormat=N2}" Width="85"/>
                        <DataGridTextColumn Header="Profit" Binding="{Binding GrossProfit, StringFormat=N2}" Width="85"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding StockDisplay}" Width="75"/>
                        <DataGridTextColumn Header="Last Sale" Binding="{Binding LastSaleDate, StringFormat=yyyy-MM-dd}" Width="95"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Top Sellers">
                <DataGrid ItemsSource="{Binding TopSellers}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="25" CellStyle="{StaticResource SelectedCellStyle}">
                    <DataGrid.Columns><DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="*"/><DataGridTextColumn Header="Type" Binding="{Binding ItemType}" Width="90"/><DataGridTextColumn Header="Net Qty" Binding="{Binding NetQuantity, StringFormat={}{0:#,##0.###}}" Width="90"/><DataGridTextColumn Header="Net Sales" Binding="{Binding NetSales, StringFormat=N2}" Width="110"/><DataGridTextColumn Header="Profit" Binding="{Binding GrossProfit, StringFormat=N2}" Width="110"/></DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Slow / Non-Selling Stock">
                <DataGrid ItemsSource="{Binding SlowOrNonSellingItems}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="25" CellStyle="{StaticResource SelectedCellStyle}">
                    <DataGrid.Columns><DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="*"/><DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="140"/><DataGridTextColumn Header="Current Stock" Binding="{Binding StockDisplay}" Width="110"/><DataGridTextColumn Header="Last Sale" Binding="{Binding LastSaleDate, StringFormat=yyyy-MM-dd}" Width="110"/></DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Selected Item Transactions">
                <DataGrid ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="25" CellStyle="{StaticResource SelectedCellStyle}">
                    <DataGrid.Columns><DataGridTextColumn Header="Date" Binding="{Binding TransactionDate, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/><DataGridTextColumn Header="Type" Binding="{Binding TransactionType}" Width="110"/><DataGridTextColumn Header="Document" Binding="{Binding DocumentNo}" Width="130"/><DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/><DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat={}{0:#,##0.###}}" Width="90"/><DataGridTextColumn Header="Value" Binding="{Binding Value, StringFormat=N2}" Width="110"/></DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        <Grid Grid.Row="4" Margin="2,5,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="No item or service activity matches the selected filters." Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>
            <ProgressBar Grid.Column="1" Width="120" Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
