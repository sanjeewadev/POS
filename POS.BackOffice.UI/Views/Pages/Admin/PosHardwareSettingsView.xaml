<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Admin.PosHardwareSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1050" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SaveBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DiagBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#17A2B8"/>
            <Setter Property="BorderBrush" Value="#17A2B8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2F3F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#6F42C1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5936A2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Local POS Hardware &amp; Peripheral Configuration" FontSize="22" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,15"/>

            <Border Background="#F9F5FF" BorderBrush="#E5D6FA" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Enterprise Cloud Sync" FontWeight="Bold" Foreground="#6F42C1"/>
                        <TextBlock Text="Download pre-configured hardware settings for this specific workstation from the central database." Foreground="#666666" FontSize="12"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,15,0">
                        <TextBlock Text="Target Workstation ID" FontSize="11" Foreground="#666666" Margin="0,0,0,2"/>
                        <TextBox Width="150" Height="30" Text="MAIN-TILL-01" BorderBrush="#CCCCCC" BorderThickness="1" Padding="5"/>
                    </StackPanel>

                    <Button Grid.Column="2" Content="⬇️ Pull Configuration Profile" Style="{StaticResource ActionBtn}" Margin="0" Height="30"/>
                </Grid>
            </Border>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
            <StackPanel>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="1. Workstation Identity (Local Machine)" FontSize="16" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Terminal / Machine Name *" Foreground="#333333" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtTerminalName" Style="{StaticResource StandardInput}" Text="MAIN-TILL-01" FontWeight="Bold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Physical Branch Location *" Foreground="#333333" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbLocation" Style="{StaticResource StandardInput}">
                                    <ComboBoxItem Content="Main Store" IsSelected="True"/>
                                    <ComboBoxItem Content="Warehouse"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Hardware Profile Type" Foreground="#666666" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbProfileType" Style="{StaticResource StandardInput}" Background="#F8F8F8">
                                    <ComboBoxItem Content="Front-End POS (Till)" IsSelected="True"/>
                                    <ComboBoxItem Content="Back-Office Admin"/>
                                    <ComboBoxItem Content="Kitchen / Dispatch Terminal"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="2. Receipt Printer &amp; Cash Drawer Engine" FontSize="16" FontWeight="SemiBold" Foreground="#2B5B84" Margin="0,0,0,15"/>

                        <Border Background="#F4FAF6" BorderBrush="#C3E6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Printer Connection Mode *" Foreground="#28A745" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbPrinterMode" Style="{StaticResource StandardInput}">
                                        <ComboBoxItem Content="Windows Spooler (USB/Driver)" IsSelected="True"/>
                                        <ComboBoxItem Content="Direct IP / Network (LAN)"/>
                                        <ComboBoxItem Content="Raw COM / Serial Port"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Target Device / IP Address / COM Port *" Foreground="#333333" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbTargetPrinter" Style="{StaticResource StandardInput}" IsEditable="True" Text="EPSON TM-T82 Receipt">
                                        <ComboBoxItem Content="EPSON TM-T82 Receipt"/>
                                        <ComboBoxItem Content="Xprinter XP-80"/>
                                        <ComboBoxItem Content="Send to OneNote"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Paper Width" Foreground="#666666" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbPaperWidth" Style="{StaticResource StandardInput}">
                                        <ComboBoxItem Content="80mm (Standard)" IsSelected="True"/>
                                        <ComboBoxItem Content="58mm (Compact)"/>
                                        <ComboBoxItem Content="A4 Sheet"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Background="#FFF8EB" BorderBrush="#FDE0A6" BorderThickness="1" CornerRadius="4" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <CheckBox x:Name="chkEnableDrawer" Content="Enable Cash Drawer Kick" FontWeight="Bold" Foreground="#D97706" IsChecked="True"/>
                                    <TextBlock Text="Fires via RJ11 through receipt printer." FontSize="11" Foreground="#666666" Margin="20,2,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Printer Brand (Sets Default Hex/Dec Code)" Foreground="#333333" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbDrawerBrand" Style="{StaticResource StandardInput}">
                                        <ComboBoxItem Content="Epson / Generic ESC/POS" IsSelected="True"/>
                                        <ComboBoxItem Content="Star Micronics"/>
                                        <ComboBoxItem Content="Citizen"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Kick Code Override" Foreground="#666666" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtKickCode" Style="{StaticResource StandardInput}" Text="27,112,0,25,250" ToolTip="Decimal or Hex payload sent to printer"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="3. Input Peripherals (Scanners &amp; Weighing Scales)" FontSize="16" FontWeight="SemiBold" Foreground="#6F42C1" Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="Barcode Scanner Logic" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,10"/>

                                    <TextBlock Text="Scan Suffix Action *" Foreground="#666666" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbScanSuffix" Style="{StaticResource StandardInput}">
                                        <ComboBoxItem Content="Auto-Enter / Add Item Instantly" IsSelected="True"/>
                                        <ComboBoxItem Content="Tab to Next Field"/>
                                        <ComboBoxItem Content="Do Nothing (Wait for manual Enter)"/>
                                    </ComboBox>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15">
                                <StackPanel>
                                    <CheckBox x:Name="chkEnableScale" Content="Enable Serial Weighing Scale (COM)" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,10" IsChecked="False"/>

                                    <Grid IsEnabled="{Binding ElementName=chkEnableScale, Path=IsChecked}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="5"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="5"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Port" Foreground="#666666" FontSize="11"/>
                                            <TextBox Text="COM3" Style="{StaticResource StandardInput}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Baud" Foreground="#666666" FontSize="11"/>
                                            <TextBox Text="9600" Style="{StaticResource StandardInput}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="Parity" Foreground="#666666" FontSize="11"/>
                                            <TextBox Text="Even" Style="{StaticResource StandardInput}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="4. Customer Facing Displays &amp; Payment Terminals" FontSize="16" FontWeight="SemiBold" Foreground="#D97706" Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15">
                                <StackPanel>
                                    <CheckBox x:Name="chkEnablePoleDisplay" Content="Enable Customer Line Display (VFD)" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,10" IsChecked="True"/>

                                    <TextBlock Text="COM Port" Foreground="#666666" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtPoleCom" Text="COM4" Style="{StaticResource StandardInput}"/>

                                    <TextBlock Text="Idle / Welcome Message" Foreground="#666666" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtPoleWelcome" Text="Welcome to Apex!" Style="{StaticResource StandardInput}" MaxLength="20"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Background="#E2EDF8" BorderBrush="#B6D4FE" BorderThickness="1" CornerRadius="4" Padding="15">
                                <StackPanel>
                                    <CheckBox x:Name="chkEnableEftpos" Content="Enable Integrated Card Terminal (EFTPOS)" FontWeight="SemiBold" Foreground="#2B5B84" Margin="0,0,0,10" IsChecked="False"/>

                                    <Grid IsEnabled="{Binding ElementName=chkEnableEftpos, Path=IsChecked}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Terminal IP Address" Foreground="#666666" Margin="0,0,0,5"/>
                                            <TextBox Text="192.168.1.50" Style="{StaticResource StandardInput}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="TCP Port" Foreground="#666666" Margin="0,0,0,5"/>
                                            <TextBox Text="8000" Style="{StaticResource StandardInput}"/>
                                        </StackPanel>
                                    </Grid>
                                    <TextBlock Text="Pushes final total directly to the card machine to prevent manual keying errors." FontSize="11" Foreground="#2B5B84" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="20" Margin="-20,15,-20,-20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="IT Diagnostics:" Foreground="#666666" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="🖨️ Test Print" Style="{StaticResource DiagBtn}" Width="100"/>
                    <Button Content="💵 Kick Drawer" Style="{StaticResource DiagBtn}" Width="110"/>
                    <Button Content="📺 Test Display" Style="{StaticResource DiagBtn}" Width="110" Margin="0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Discard Changes" Style="{StaticResource StandardBtn}" Background="White" Foreground="#666666" BorderBrush="#CCCCCC" BorderThickness="1" Width="140"/>
                    <Button Content="SAVE LOCAL HARDWARE PROFILE" Style="{StaticResource SaveBtn}" Width="260" Margin="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>