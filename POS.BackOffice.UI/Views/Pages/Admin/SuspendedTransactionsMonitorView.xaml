<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Admin.SuspendedTransactionsMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}" FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>
        <Style x:Key="SelectedCellStyle" TargetType="DataGridCell"><Setter Property="Padding" Value="4,1"/><Setter Property="Foreground" Value="Black"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource LightSelectionBrush}"/><Setter Property="Foreground" Value="Black"/></Trigger></Style.Triggers></Style>
        <Style x:Key="CompactButton" TargetType="Button"><Setter Property="Height" Value="27"/><Setter Property="Padding" Value="12,0"/><Setter Property="Margin" Value="4,0,0,0"/></Style>
    </UserControl.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="3*"/><RowDefinition Height="2*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource ActionBackgroundBrush}" Padding="9"><TextBlock Text="PERSISTENT CART / SUSPENDED TRANSACTION HISTORY" Foreground="White" FontSize="15" FontWeight="Bold"/></Border>
        <Border Grid.Row="1" Margin="0,6" Padding="7" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <WrapPanel>
                <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,4,0"/><DatePicker SelectedDate="{Binding StartDate}" Width="120" Height="25"/>
                <TextBlock Text="To" VerticalAlignment="Center" Margin="10,0,4,0"/><DatePicker SelectedDate="{Binding EndDate}" Width="120" Height="25"/>
                <TextBlock Text="Status" VerticalAlignment="Center" Margin="10,0,4,0"/><ComboBox ItemsSource="{Binding Statuses}" SelectedItem="{Binding SelectedStatus}" Width="110" Height="25"/>
                <TextBlock Text="Search" VerticalAlignment="Center" Margin="10,0,4,0"/><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="210" Height="25" Padding="4,2"/>
                <Button Content="Load" Command="{Binding LoadCommand}" Style="{StaticResource CompactButton}"/><Button Content="Reset" Command="{Binding ResetCommand}" Style="{StaticResource CompactButton}"/>
            </WrapPanel>
        </Border>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="25" CellStyle="{StaticResource SelectedCellStyle}" AlternatingRowBackground="{DynamicResource DataGridAlternatingRow}" GridLinesVisibility="All">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cart Reference" Binding="{Binding ReferenceNo}" Width="155"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="85"/>
                <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="135"/>
                <DataGridTextColumn Header="Terminal" Binding="{Binding TerminalNo}" Width="75"/>
                <DataGridTextColumn Header="Cashier" Binding="{Binding CashierName}" Width="120"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*" MinWidth="150"/>
                <DataGridTextColumn Header="Lines" Binding="{Binding ItemCount}" Width="60"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding TotalQuantity, StringFormat=N3}" Width="70"/>
                <DataGridTextColumn Header="Value" Binding="{Binding NetTotal, StringFormat=N2}" Width="90"/>
                <DataGridTextColumn Header="Recalls" Binding="{Binding RecallCount}" Width="65"/>
                <DataGridTextColumn Header="Completed Invoice" Binding="{Binding CompletedInvoiceNo}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="3" Margin="0,6,0,0" Padding="6" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Created:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.Session.CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Margin="4,0,14,0"/>
                    <TextBlock Text="Held:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.Session.HeldAt, StringFormat=yyyy-MM-dd HH:mm}" Margin="4,0,14,0"/>
                    <TextBlock Text="Recalled:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.Session.RecalledAt, StringFormat=yyyy-MM-dd HH:mm}" Margin="4,0,14,0"/>
                    <TextBlock Text="Cancelled:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.Session.CancelledAt, StringFormat=yyyy-MM-dd HH:mm}" Margin="4,0,14,0"/>
                    <TextBlock Text="Reason:" FontWeight="Bold"/><TextBlock Text="{Binding SelectedDetails.Session.CancellationReasonText}" Margin="4,0,0,0"/>
                </WrapPanel>
                <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedDetails.Lines}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeight="24" CellStyle="{StaticResource SelectedCellStyle}" GridLinesVisibility="All">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding LineNumber}" Width="45"/>
                        <DataGridTextColumn Header="Line Type" Binding="{Binding LineType}" Width="100"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat=N3}" Width="80"/>
                        <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=N2}" Width="95"/>
                        <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=N2}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
        <Grid Grid.Row="4" Margin="2,5,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="No persistent cart sessions match the selected filters." Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>
            <ProgressBar Grid.Column="1" Width="120" Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
