<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Admin.AccessControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SaveBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="OutlineBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2B5B84"/>
            <Setter Property="BorderBrush" Value="#2B5B84"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2EDF8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="CheckBox" x:Key="PermCheck">
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="LayoutTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource GroupBoxBorder}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F8F8F8" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="15">
                    <StackPanel>
                        <TextBlock Text="System Roles" FontSize="18" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,10"/>
                        <TextBox x:Name="txtSearchRoles" Style="{StaticResource StandardInput}" Margin="0" ToolTip="Search Roles"/>
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="1" x:Name="dgRoles" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          HeadersVisibility="None" 
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#EEEEEE"
                          BorderThickness="0"
                          RowHeight="50"
                          Background="Transparent"
                          SelectionMode="Single">

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center" Margin="15,0">
                                        <TextBlock Text="{Binding RoleName}" FontWeight="Bold" FontSize="14" Foreground="#2B5B84"/>
                                        <TextBlock Text="{Binding ActiveUsersCount, StringFormat='👤 {0} Active Users'}" Foreground="#666666" FontSize="11" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="2" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="15">
                    <StackPanel>
                        <Button Content="+ Create Custom Role" Style="{StaticResource StandardBtn}" Background="#2B5B84" Margin="0,0,0,10" Width="Auto"/>
                        <Button Content="Clone Selected Role" Style="{StaticResource OutlineBtn}" Margin="0" Width="Auto"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource GroupBoxBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="Role Name *" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtRoleName" Style="{StaticResource StandardInput}" Text="Store Manager" FontWeight="Bold" FontSize="16" Height="35"/>

                            <TextBlock Text="Role Description" Foreground="#666666" Margin="0,5,0,5"/>
                            <TextBox x:Name="txtRoleDescription" Text="Branch supervisor capable of overriding discounts and managing localized inventory, but restricted from altering system settings." TextWrapping="Wrap" AcceptsReturn="True" Height="45" BorderBrush="#CCCCCC" BorderThickness="1" Padding="5"/>
                        </StackPanel>

                        <Border Grid.Column="1" Background="#FFF3CD" BorderBrush="#FFEEBA" BorderThickness="1" CornerRadius="4" Padding="15" VerticalAlignment="Top">
                            <StackPanel>
                                <TextBlock Text="🔒 Core System Role" FontWeight="Bold" Foreground="#856404"/>
                                <TextBlock Text="This role is critical to system operation." Foreground="#856404" FontSize="11" Margin="0,2,0,0"/>
                                <TextBlock Text="It cannot be deleted." Foreground="#856404" FontSize="11"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Security &amp; Authorization Matrix" FontSize="16" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

                        <Border Background="#F4FAF6" BorderBrush="#C3E6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="A. POS &amp; Front Desk Operations" FontWeight="Bold" Foreground="#28A745" Margin="0,0,0,10"/>

                                <CheckBox Content="Can access POS Sales Terminal" Style="{StaticResource PermCheck}" IsChecked="True"/>

                                <Grid Margin="0,5,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" Content="Can apply manual line discounts" Style="{StaticResource PermCheck}" IsChecked="True"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="Threshold (Max %):" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBox Width="60" Text="10.0" HorizontalContentAlignment="Center" Background="#FDF3F3" BorderBrush="#F5C6CB" BorderThickness="1" Padding="2" ToolTip="Maximum discount percentage this role can give without Manager Override"/>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,5,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" Content="Can VOID saved invoices" Style="{StaticResource PermCheck}" IsChecked="True"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="Threshold (Max Rs.):" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBox Width="100" Text="25000.00" HorizontalContentAlignment="Right" Background="#FDF3F3" BorderBrush="#F5C6CB" BorderThickness="1" Padding="2" ToolTip="Maximum invoice value this role can void without higher approval"/>
                                    </StackPanel>
                                </Grid>

                                <CheckBox Content="Can process Customer Returns / Refunds" Style="{StaticResource PermCheck}" IsChecked="True"/>
                                <CheckBox Content="Can perform End of Day (Z-Read) Cash Audit" Style="{StaticResource PermCheck}" IsChecked="True"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#E2EDF8" BorderBrush="#B6D4FE" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="B. Inventory &amp; Supply Chain" FontWeight="Bold" Foreground="#2B5B84" Margin="0,0,0,10"/>

                                <CheckBox Content="Can view item Cost Prices and Supplier Margins" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can add/edit Products and Categories" Style="{StaticResource PermCheck}" IsChecked="True"/>
                                <CheckBox Content="Can perform Inventory Count / Stock Adjustments" Style="{StaticResource PermCheck}" IsChecked="True"/>

                                <Grid Margin="0,5,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" Content="Can create/approve Purchase Orders" Style="{StaticResource PermCheck}" IsChecked="True"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="PO Limit (Max Rs.):" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <TextBox Width="100" Text="100000.00" HorizontalContentAlignment="Right" BorderBrush="#CCCCCC" BorderThickness="1" Padding="2"/>
                                    </StackPanel>
                                </Grid>

                                <CheckBox Content="Can post Goods Received Notes (GRN)" Style="{StaticResource PermCheck}" IsChecked="True"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#FFF8EB" BorderBrush="#FDE0A6" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="C. Accounting &amp; Finance" FontWeight="Bold" Foreground="#D97706" Margin="0,0,0,10"/>

                                <CheckBox Content="Can view global Gross Profit and Revenue Analytics" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can view Cash Book and Ledger Balances" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can post Supplier Payment Vouchers" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can post Operational Expense Vouchers" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can perform Bank Reconciliation / Deposits" Style="{StaticResource PermCheck}"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#F9F5FF" BorderBrush="#E5D6FA" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="D. Administration &amp; IT Controls" FontWeight="Bold" Foreground="#6F42C1" Margin="0,0,0,10"/>

                                <CheckBox Content="Can manage User Accounts and Reset Passwords" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can modify global System Settings (Taxes, Branches)" Style="{StaticResource PermCheck}"/>
                                <CheckBox Content="Can view Immutable System Audit Logs" Style="{StaticResource PermCheck}"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="0,15,0,0" Margin="0,10,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="⚠️ Changes to permissions apply immediately upon the next user login." Foreground="#A94044" FontWeight="SemiBold"/>
                            <TextBlock Text="Reducing threshold limits may affect currently pending transactions created by these users." Foreground="#666666" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Discard Changes" Style="{StaticResource OutlineBtn}" Background="White" Width="140"/>
                            <Button Content="SAVE PERMISSIONS" Style="{StaticResource SaveBtn}" Width="180" Margin="0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>