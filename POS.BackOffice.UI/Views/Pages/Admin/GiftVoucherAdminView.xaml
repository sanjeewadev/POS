<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Admin.GiftVoucherAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200"
             Background="#F5F5F5" FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="ActionBtn">
            <Setter Property="Background" Value="#6F42C1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="32"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5936A2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DangerBtn">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox" x:Key="InputBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="💳 Gift Voucher Management" FontSize="22" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#F9F5FF" BorderBrush="#E5D6FA" BorderThickness="1" Padding="10,5" CornerRadius="3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏭 CREATE NEW BATCH" FontWeight="Bold" Foreground="#6F42C1" VerticalAlignment="Center" Margin="0,0,15,0"/>

                            <TextBlock Text="Qty:" Foreground="#555" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding GenerateCount}" Width="60" Style="{StaticResource InputBox}" Margin="0,0,15,0" HorizontalContentAlignment="Center"/>

                            <TextBlock Text="Face Value (Rs.):" Foreground="#555" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding GenerateAmount}" Width="100" Style="{StaticResource InputBox}" Margin="0,0,15,0" FontWeight="Bold"/>

                            <Button Content="⚡ Generate Barcodes" Command="{Binding GenerateBatchCommand}" Style="{StaticResource ActionBtn}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Status Filter:" Foreground="#555" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <ComboBox SelectedValue="{Binding SelectedFilter}" SelectedValuePath="Content" Width="150" Height="30" VerticalContentAlignment="Center" Margin="0,0,15,0">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Inactive" ToolTip="Not yet sold"/>
                        <ComboBoxItem Content="Active" ToolTip="Sold and holding a balance"/>
                        <ComboBoxItem Content="Exhausted" ToolTip="Fully spent"/>
                        <ComboBoxItem Content="Expired"/>
                        <ComboBoxItem Content="Blocked"/>
                    </ComboBox>

                    <Button Content="📑 Export to Printer" Command="{Binding ExportBarcodesCommand}" Height="30" Background="#E2EDF8" Foreground="#2B5B84" BorderThickness="1" BorderBrush="#2B5B84" Padding="15,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Vouchers}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0" BorderThickness="1" BorderBrush="#A0A0A0" 
                  RowHeight="32" Background="White" AlternatingRowBackground="#F9F9F9">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2B3C38"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,8"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Barcode (12-Digit)" Binding="{Binding Barcode}" Width="160" FontWeight="Bold"/>

                <DataGridTextColumn Header="Face Value" Binding="{Binding InitialAmount, StringFormat=Rs. \{0:N2\}}" Width="120" Foreground="#555"/>
                <DataGridTextColumn Header="Current Balance" Binding="{Binding CurrentBalance, StringFormat=Rs. \{0:N2\}}" Width="130" FontWeight="Bold" Foreground="#1E7E34"/>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" FontWeight="SemiBold"/>

                <DataGridTextColumn Header="Created On" Binding="{Binding CreatedDate, StringFormat=dd-MMM-yyyy HH:mm}" Width="150" Foreground="#666"/>
                <DataGridTextColumn Header="Activated On" Binding="{Binding ActivationDate, StringFormat=dd-MMM-yyyy}" Width="110" Foreground="#666"/>
                <DataGridTextColumn Header="Expires On" Binding="{Binding ExpiryDate, StringFormat=dd-MMM-yyyy}" Width="110" Foreground="#A94044"/>

                <DataGridTemplateColumn Header="Emergency Action" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="BLOCK / KILL" 
                    Command="{Binding DataContext.BlockVoucherCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                    CommandParameter="{Binding Id}" 
                    HorizontalAlignment="Left">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource DangerBtn}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                <Setter Property="Visibility" Value="Hidden"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Exhausted">
                                                <Setter Property="Visibility" Value="Hidden"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Blocked">
                                                <Setter Property="Visibility" Value="Hidden"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>