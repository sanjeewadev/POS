<UserControl x:Class="POS.BackOffice.UI.Views.Pages.InventoryOperations.PriceChangeHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="850"
             d:DesignWidth="1400"
             Loaded="UserControl_Loaded"
             Background="#D4D0C8"
             FontFamily="Segoe UI"
             FontSize="12">
    <UserControl.Resources>
        <Style x:Key="Group" TargetType="Border"><Setter Property="Background" Value="#F0F0F0"/><Setter Property="BorderBrush" Value="#999999"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="8"/></Style>
        <Style x:Key="Input" TargetType="TextBox"><Setter Property="Height" Value="27"/><Setter Property="Padding" Value="5,0"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="BorderBrush" Value="#808080"/></Style>
        <Style x:Key="Combo" TargetType="ComboBox"><Setter Property="Height" Value="27"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="BorderBrush" Value="#808080"/></Style>
        <Style x:Key="Btn" TargetType="Button"><Setter Property="Height" Value="29"/><Setter Property="Padding" Value="12,0"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Background" Value="#E0E0E0"/><Setter Property="BorderBrush" Value="#777777"/></Style>
        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource Btn}"><Setter Property="Background" Value="#005555"/><Setter Property="Foreground" Value="White"/><Setter Property="BorderBrush" Value="#003333"/></Style>
        <Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="#003366"/><Setter Property="Foreground" Value="White"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Padding" Value="5"/></Style>
        <Style TargetType="DataGridCell"><Setter Property="Padding" Value="5,0"/><Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="BorderThickness" Value="0"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#D0E8E8"/><Setter Property="Foreground" Value="Black"/></Trigger></Style.Triggers></Style>
        <Style x:Key="RightText" TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="VerticalAlignment" Value="Center"/></Style>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#D0E8E8"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Group}">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,7">
                    <TextBlock Text="PRICE CHANGE HISTORY" FontSize="14" FontWeight="Bold" Foreground="#003366"/>
                    <TextBlock Text="One row per saved operation" Margin="14,1,0,0" Foreground="#666666"/>
                    <Button DockPanel.Dock="Right" Content="REFRESH" Command="{Binding RefreshCommand}" Style="{StaticResource Btn}"/>
                </DockPanel>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="230"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="95"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="125"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="125"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="115"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="115"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="80"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Input}" ToolTip="Change number, item, SKU, barcode, batch, action or document"/>
                    <ComboBox Grid.Column="2" ItemsSource="{Binding PriceLevelFilters}" SelectedItem="{Binding SelectedPriceLevelFilter}" Style="{StaticResource Combo}"/>
                    <ComboBox Grid.Column="4" ItemsSource="{Binding ChangeSourceFilters}" SelectedItem="{Binding SelectedChangeSourceFilter}" Style="{StaticResource Combo}"/>
                    <ComboBox Grid.Column="6" ItemsSource="{Binding ChangedByFilters}" SelectedItem="{Binding SelectedChangedByFilter}" Style="{StaticResource Combo}"/>
                    <DatePicker Grid.Column="8" SelectedDate="{Binding DateFrom}" Height="27"/>
                    <DatePicker Grid.Column="10" SelectedDate="{Binding DateTo}" Height="27"/>
                    <ComboBox Grid.Column="12" ItemsSource="{Binding RowLimitOptions}" SelectedItem="{Binding SelectedRowLimit}" Style="{StaticResource Combo}"/>
                    <Button Grid.Column="14" Content="SEARCH" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryBtn}"/>
                    <Button Grid.Column="16" Content="CLEAR" Command="{Binding ClearFiltersCommand}" Style="{StaticResource Btn}"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Group}" Padding="0">
            <DataGrid ItemsSource="{Binding Operations}" SelectedItem="{Binding SelectedOperation, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" RowHeight="28" AlternatingRowBackground="#F8F8F8" SelectionMode="Single" SelectionUnit="FullRow" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Changed At" Binding="{Binding ChangedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="130"/>
                    <DataGridTextColumn Header="Change No" Binding="{Binding DisplayChangeNo}" Width="105"/>
                    <DataGridTextColumn Header="Item / Variant" Binding="{Binding ItemVariantSummary}" Width="240"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding ChangeSource}" Width="110"/>
                    <DataGridTextColumn Header="Master Change" Binding="{Binding MasterChangeSummary}" Width="180"/>
                    <DataGridTextColumn Header="Batch Overrides" Binding="{Binding BatchOverrideSummary}" Width="105" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Changed By" Binding="{Binding ChangedBy}" Width="120"/>
                    <DataGridTextColumn Header="Source Document" Binding="{Binding SourceDocumentNo}" Width="120"/>
                    <DataGridTextColumn Header="Details" Binding="{Binding DetailRowCount}" Width="70" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding ChangeReason}" Width="260"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource Group}" Padding="0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#E5E5E5" Padding="8,6">
                    <DockPanel LastChildFill="True">
                        <Button DockPanel.Dock="Right" Content="CLOSE DETAILS" Command="{Binding CloseDetailsCommand}" Style="{StaticResource Btn}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="OPERATION DETAILS" FontWeight="Bold" Foreground="#005555"/>
                            <TextBlock Text="{Binding SelectedOperation.DisplayChangeNo, TargetNullValue=Select an operation}" Margin="12,0,0,0" FontWeight="Bold" Foreground="#003366"/>
                            <TextBlock Text="{Binding SelectedOperation.ChangeSource, StringFormat=Source: {0}}" Margin="16,0,0,0" Foreground="#555555"/>
                            <TextBlock Text="{Binding SelectedOperation.ChangedBy, StringFormat=User: {0}}" Margin="16,0,0,0" Foreground="#555555"/>
                        </StackPanel>
                    </DockPanel>
                </Border>
                <DataGrid Grid.Row="1" ItemsSource="{Binding OperationDetails}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" RowHeight="27" AlternatingRowBackground="#F8F8F8" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Level" Binding="{Binding PriceLevel}" Width="65"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding ChangeAction}" Width="145"/>
                        <DataGridTextColumn Header="Item / Variant" Binding="{Binding ItemVariantDisplay}" Width="210"/>
                        <DataGridTextColumn Header="SKU" Binding="{Binding SkuCode}" Width="105"/>
                        <DataGridTextColumn Header="Batch" Binding="{Binding BatchNo}" Width="100"/>
                        <DataGridTextColumn Header="Old Source" Binding="{Binding OldPriceSource}" Width="105"/>
                        <DataGridTextColumn Header="New Source" Binding="{Binding NewPriceSource}" Width="105"/>
                        <DataGridTextColumn Header="Old Min" Binding="{Binding OldMinimumPrice, StringFormat=N2}" Width="75" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="New Min" Binding="{Binding NewMinimumPrice, StringFormat=N2}" Width="75" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="Old Retail" Binding="{Binding OldRetailPrice, StringFormat=N2}" Width="80" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="New Retail" Binding="{Binding NewRetailPrice, StringFormat=N2}" Width="80" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="Old W/S" Binding="{Binding OldWholesalePrice, StringFormat=N2}" Width="80" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="New W/S" Binding="{Binding NewWholesalePrice, StringFormat=N2}" Width="80" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="Old Max" Binding="{Binding OldMaximumPrice, StringFormat=N2}" Width="75" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="New Max" Binding="{Binding NewMaximumPrice, StringFormat=N2}" Width="75" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="Cost" Binding="{Binding EffectiveCost, StringFormat=N2}" Width="75" ElementStyle="{StaticResource RightText}"/>
                        <DataGridTextColumn Header="Document" Binding="{Binding SourceDocumentNo}" Width="105"/>
                        <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="260"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="6" Style="{StaticResource Group}" Padding="7,5">
            <DockPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#444444"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock Text="Operations: " FontWeight="Bold"/><TextBlock Text="{Binding TotalOperations}" Margin="0,0,14,0"/>
                    <TextBlock Text="Master: " FontWeight="Bold"/><TextBlock Text="{Binding MasterOperationCount}" Margin="0,0,14,0"/>
                    <TextBlock Text="Batch: " FontWeight="Bold"/><TextBlock Text="{Binding BatchOperationCount}" Margin="0,0,14,0"/>
                    <TextBlock Text="Latest: " FontWeight="Bold"/><TextBlock Text="{Binding LatestChangedText}"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
