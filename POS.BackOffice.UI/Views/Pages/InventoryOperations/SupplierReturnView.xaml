<UserControl x:Class="POS.BackOffice.UI.Views.Pages.InventoryOperations.SupplierReturnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBox" x:Key="ClassicInput">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="ClassicCombo">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DatePicker" x:Key="ClassicDate">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>

        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
        </Style>

        <Style TargetType="Button" x:Key="ReturnBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#A94044"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#822E31"/>
        </Style>

        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#005555"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell" x:Key="CreditCell" BasedOn="{StaticResource BaseCell}">
            <Setter Property="Background" Value="#FEF2F2"/>
            <Setter Property="Foreground" Value="#991B1B"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="520"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ======================================================== -->
        <!-- LEFT COLUMN: INTAKE & INVOICE HISTORY                    -->
        <!-- ======================================================== -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ZONE 1: DOCUMENT HEADER -->
            <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
                <StackPanel>
                    <TextBlock Text="1. DEBIT NOTE HEADER" Style="{StaticResource HeaderLabel}"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Target Supplier *" Style="{StaticResource InlineLabel}" Foreground="#991B1B"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="CompanyName" Style="{StaticResource ClassicCombo}" Background="#FFFFE0" FontWeight="Bold"/>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Link to GRN/Inv:" Style="{StaticResource InlineLabel}"/>
                        <ComboBox Grid.Column="1" IsEditable="True" ItemsSource="{Binding SupplierInvoices}" SelectedItem="{Binding SelectedInvoice}" DisplayMemberPath="SupplierInvoiceNo" Style="{StaticResource ClassicCombo}" ToolTip="Select historical invoice to pull exact unit costs"/>
                        <Button Grid.Column="3" Content="LOAD" Command="{Binding LoadInvoiceCommand}" Style="{StaticResource ActionBtn}"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Return Date *" Style="{StaticResource InlineLabel}"/>
                        <DatePicker Grid.Column="1" SelectedDate="{Binding ReturnDate}" Style="{StaticResource ClassicDate}"/>

                        <TextBlock Grid.Column="3" Text="Auth By *" Style="{StaticResource InlineLabel}"/>
                        <TextBox Grid.Column="4" Text="{Binding AuthorizedBy}" Style="{StaticResource ClassicInput}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- ZONE 2: RAPID ENTRY MATRIX -->
            <Border Grid.Row="1" Style="{StaticResource ClassicGroupBox}" Background="#E6F2F2" BorderBrush="#005555">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="2. HISTORICAL INVOICE MATRIX" Style="{StaticResource HeaderLabel}" Foreground="#005555"/>
                    <TextBlock DockPanel.Dock="Top" Text="Enter the quantity you wish to return into the highlighted column below." Foreground="#555" FontStyle="Italic" Margin="0,0,0,10"/>

                    <Button DockPanel.Dock="Bottom" Content="⚡ ADD TO DEBIT NOTE" Command="{Binding AddMatrixCommand}" Style="{StaticResource ActionBtn}" Background="#0066CC" BorderBrush="#004C99" Height="36" Margin="0,10,0,0" FontSize="13"/>

                    <DataGrid ItemsSource="{Binding ActiveMatrixVariants}" AutoGenerateColumns="False" CanUserAddRows="False" 
                              HeadersVisibility="Column" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" 
                              RowHeight="32" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0"
                              SelectionMode="Single" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E5E5E5"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="6"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Item Details" Binding="{Binding Description}" Width="*" IsReadOnly="True" FontWeight="SemiBold" CellStyle="{StaticResource BaseCell}"/>
                            <DataGridTextColumn Header="Variant" Binding="{Binding VariantDescription}" Width="100" IsReadOnly="True" Foreground="#555" CellStyle="{StaticResource BaseCell}"/>

                            <DataGridTextColumn Header="Hist. Cost" Binding="{Binding HistoricalCost, StringFormat=N2}" Width="80" IsReadOnly="True" Foreground="#005500" CellStyle="{StaticResource BaseCell}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="Return Qty *" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding ReturnQty, UpdateSourceTrigger=PropertyChanged}" 
                                                 HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                                                 FontWeight="Bold" Background="#FFFFE0" BorderBrush="#A0A0A0" BorderThickness="1" Margin="2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>

        <!-- ======================================================== -->
        <!-- RIGHT COLUMN: DEBIT NOTE CART & FINANCIALS               -->
        <!-- ======================================================== -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#003366" Padding="15,12" BorderBrush="#002244" BorderThickness="1" Margin="0,0,0,10">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="PENDING DEBIT NOTE / RETURN CART" Foreground="White" FontSize="15" FontWeight="Bold"/>
                    </StackPanel>
                    <Border Background="#FFD700" Padding="15,4" HorizontalAlignment="Right">
                        <TextBlock Text="DRAFT STATUS" FontWeight="Bold" Foreground="Black" FontSize="12"/>
                    </Border>
                </Grid>
            </Border>

            <!-- THE RETURN LINES DATAGRID -->
            <Border Grid.Row="1" Style="{StaticResource ClassicGroupBox}" Padding="0">
                <DataGrid x:Name="dgReturnLines" ItemsSource="{Binding ReturnLines}" SelectedItem="{Binding SelectedLine}" 
                          AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal" IsReadOnly="True"
                          HorizontalGridLinesBrush="#E0E0E0" BorderThickness="0" RowHeight="34" Background="White" AlternatingRowBackground="#F9F9F9">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F0F0F0"/>
                            <Setter Property="Foreground" Value="#333"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#A0A0A0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="90" Foreground="#666" CellStyle="{StaticResource BaseCell}"/>
                        <DataGridTextColumn Header="Description &amp; Variant" Binding="{Binding Description}" Width="*" FontWeight="SemiBold" CellStyle="{StaticResource BaseCell}"/>

                        <DataGridTextColumn Header="Return Qty" Binding="{Binding ReturnQty}" Width="80" FontWeight="Bold" Foreground="#005500" CellStyle="{StaticResource BaseCell}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Unit Cost" Binding="{Binding HistoricalCost, StringFormat=N2}" Width="90" Foreground="#666" CellStyle="{StaticResource BaseCell}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Credit Value" Binding="{Binding CreditValue, StringFormat=N2}" Width="110" CellStyle="{StaticResource CreditCell}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Drop" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✖" Command="{Binding DataContext.RemoveLineCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}" Background="#A94044" Foreground="White" BorderThickness="0" Cursor="Hand" Width="26" Height="26" FontWeight="Bold"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- ZONE 4: FINANCIAL SETTLEMENT -->
            <Border Grid.Row="2" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" Margin="0,0,0,0" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="340"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Remarks / Dispatch Notes:" Style="{StaticResource InlineLabel}" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="70" Style="{StaticResource ClassicInput}"/>
                    </StackPanel>

                    <Rectangle Grid.Column="1" Width="1" Fill="#E0E0E0" Margin="7,0"/>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Text="FINANCIAL DEDUCTIONS" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Restocking Fee (-)" Style="{StaticResource InlineLabel}" Foreground="#991B1B"/>
                            <TextBox Grid.Column="1" Text="{Binding RestockingFee, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right" Background="#FEF2F2" FontWeight="Bold" Foreground="#991B1B" ToolTip="Supplier penalty for returning stock"/>
                        </Grid>
                    </StackPanel>

                    <Rectangle Grid.Column="3" Width="1" Fill="#E0E0E0" Margin="7,0"/>

                    <StackPanel Grid.Column="4" VerticalAlignment="Center">
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="GROSS RETURN VALUE:" Foreground="#666" FontSize="13" FontWeight="Bold"/>
                            <TextBlock Text="{Binding GrossCredit, StringFormat=Rs. \{0:N2\}}" HorizontalAlignment="Right" FontSize="14" FontWeight="Bold" Foreground="#333"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="RESTOCKING PENALTY:" Foreground="#991B1B" FontSize="13" FontWeight="Bold"/>
                            <TextBlock Text="{Binding RestockingFee, StringFormat=- Rs. \{0:N2\}}" HorizontalAlignment="Right" Foreground="#991B1B" FontSize="14" FontWeight="Bold"/>
                        </Grid>

                        <Rectangle Height="2" Fill="#333" Margin="0,0,0,10"/>

                        <Grid Margin="0,0,0,15">
                            <TextBlock Text="TOTAL NET CREDIT:" Foreground="#003366" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding NetCredit, StringFormat=Rs. \{0:N2\}}" Foreground="#005500" FontSize="28" FontWeight="ExtraBold" HorizontalAlignment="Right" ToolTip="Exact amount deducted from Supplier Ledger"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Clear" Command="{Binding ClearCommand}" Style="{StaticResource ClassicBtn}" Height="40"/>
                            <Button Grid.Column="2" Content="POST DEBIT NOTE" Command="{Binding PostReturnCommand}" Style="{StaticResource ReturnBtn}" Height="40" FontSize="14" ToolTip="Warning: Reduces physical stock and posts Debit Note to ledger"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>