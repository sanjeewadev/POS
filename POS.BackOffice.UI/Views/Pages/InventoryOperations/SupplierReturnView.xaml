<UserControl x:Class="POS.BackOffice.UI.Views.Pages.InventoryOperations.SupplierReturnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PostBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#D97706"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B45309"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DeleteBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#A94044"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C25357"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Margin="0,0,0,15">
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Supplier Return (Return Outwards)" FontSize="20" FontWeight="SemiBold" Foreground="#A94044"/>

                    <Border Grid.Column="1" Background="#FFF3CD" BorderBrush="#FFEEBA" BorderThickness="1" CornerRadius="4" Padding="10,5">
                        <TextBlock Text="Status: DRAFT" FontWeight="Bold" Foreground="#856404"/>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="GRN Reference *" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbGrnReference" Style="{StaticResource StandardInput}" ToolTip="Select original delivery to lock in correct purchase prices"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Supplier *" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbSupplier" Style="{StaticResource StandardInput}" DisplayMemberPath="CompanyName" SelectedValuePath="Id"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Return Note No." Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtReturnNumber" Style="{StaticResource StandardInput}" Background="#EEEEEE" IsReadOnly="True" Text="[Auto-Generated]"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Return From Location *" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbLocation" Style="{StaticResource StandardInput}" DisplayMemberPath="LocationName" SelectedValuePath="Id"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Reason for Return *" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbReturnReason" Style="{StaticResource StandardInput}">
                            <ComboBoxItem Content="Damaged in Transit"/>
                            <ComboBoxItem Content="Expired / Short Expiry"/>
                            <ComboBoxItem Content="Wrong Item Delivered"/>
                            <ComboBoxItem Content="Overstock / Unsold Agreement"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Return Date *" Foreground="#666666" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpReturnDate" Style="{StaticResource StandardInput}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#FDF3F3" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Scan / Search Product" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbProductSearch" Style="{StaticResource StandardInput}" IsEditable="True" Margin="0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Available Stock" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtCurrentStock" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Center" Background="#EEEEEE" IsReadOnly="True" Margin="0" ToolTip="Maximum quantity you can return"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Return Qty *" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtReturnQty" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Center" Background="#FFFFE0" Margin="0"/>
                </StackPanel>

                <StackPanel Grid.Column="6">
                    <TextBlock Text="Unit Cost (Rs.)" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtUnitCost" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Right" Margin="0" Background="#EEEEEE" IsReadOnly="True" ToolTip="Locked to original GRN purchase price"/>
                </StackPanel>

                <Button Grid.Column="8" x:Name="btnAddItem" Content="Add to Return" Style="{StaticResource StandardBtn}" Background="#A94044" Margin="0" VerticalAlignment="Bottom" Height="30" Width="120"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
            <DataGrid x:Name="dgReturnLines" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      BorderThickness="0"
                      RowHeight="40"
                      Background="Transparent">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F8F8"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="120" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>

                    <DataGridTextColumn Header="Return Qty" Binding="{Binding ReturnQty}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#A94044"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Debit Amount" Binding="{Binding LineTotal, StringFormat=N2}" Width="150" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Action" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="X" Background="#A94044" Foreground="White" BorderThickness="0" Cursor="Hand" Width="30" Height="25" FontWeight="Bold"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FDF3F3" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,20,0" HorizontalAlignment="Left" MinWidth="400">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Remarks / Notes:" Foreground="#666666" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtRemarks" Height="40" TextWrapping="Wrap" AcceptsReturn="True" BorderBrush="#CCCCCC" BorderThickness="1" Padding="5" ToolTip="Physical proof or courier details"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Text="TOTAL DEBIT NOTE:" Foreground="#A94044" FontSize="14" FontWeight="Bold"/>
                        <TextBlock x:Name="txtTotalDebit" Text="Rs. 0.00" FontWeight="Bold" FontSize="26" Foreground="#333333" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Clear Form" Style="{StaticResource StandardBtn}" Background="#777777" Width="100"/>
                <Button Content="Save Draft" Style="{StaticResource StandardBtn}" Width="110"/>
                <Button Content="POST RETURN" Style="{StaticResource PostBtn}" Width="160" Margin="0" ToolTip="Deducts inventory and creates a Debit Note in Accounts Payable"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>