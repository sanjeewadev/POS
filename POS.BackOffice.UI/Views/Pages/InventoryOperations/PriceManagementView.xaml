<UserControl x:Class="POS.BackOffice.UI.Views.Pages.InventoryOperations.PriceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:POS.BackOffice.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="850"
             d:DesignWidth="1400"
             Background="#D4D0C8"
             FontFamily="Segoe UI"
             FontSize="12">
    <UserControl.Resources>
        <conv:SmartQuantityConverter x:Key="SmartQuantityConverter"/>
        <Style x:Key="GroupBox" TargetType="Border">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#9A9A9A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="Input" TargetType="TextBox">
            <Setter Property="Height" Value="27"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style x:Key="Combo" TargetType="ComboBox">
            <Setter Property="Height" Value="27"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style x:Key="Button" TargetType="Button">
            <Setter Property="Height" Value="29"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#777777"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource Button}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource Button}">
            <Setter Property="Background" Value="#8B1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#5C1010"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#003366"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D0E8E8"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RightText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#D0E8E8"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GroupBox}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,7">
                    <TextBlock Text="PRICE MANAGEMENT" FontSize="14" FontWeight="Bold" Foreground="#003366"/>
                    <TextBlock Text="Master prices and exact batch overrides" Margin="14,1,0,0" Foreground="#666666"/>
                    <Button DockPanel.Dock="Right" Content="REFRESH" Command="{Binding LoadDataCommand}" Style="{StaticResource Button}"/>
                </DockPanel>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="145"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Input}" ToolTip="Item, SKU, barcode, batch number or internal batch barcode"/>
                    <ComboBox Grid.Column="2" ItemsSource="{Binding ItemTypeFilters}" SelectedItem="{Binding SelectedItemTypeFilter}" Style="{StaticResource Combo}"/>
                    <ComboBox Grid.Column="4" ItemsSource="{Binding TrackingFilters}" SelectedItem="{Binding SelectedTrackingFilter}" Style="{StaticResource Combo}"/>
                    <ComboBox Grid.Column="6" ItemsSource="{Binding CategoryFilters}" SelectedItem="{Binding SelectedCategoryFilter}" Style="{StaticResource Combo}"/>
                    <Button Grid.Column="8" Content="CLEAR FILTERS" Command="{Binding ClearFiltersCommand}" Style="{StaticResource Button}"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource GroupBox}" Padding="0">
            <DataGrid ItemsSource="{Binding PricingItems}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      RowHeight="28"
                      AlternatingRowBackground="#F8F8F8"
                      GridLinesVisibility="All"
                      FrozenColumnCount="3"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item / Variant" Binding="{Binding DisplayDescription}" Width="220"/>
                    <DataGridTextColumn Header="SKU" Binding="{Binding SkuCode}" Width="115"/>
                    <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="125"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding ItemTypeText}" Width="80"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="120"/>
                    <DataGridTextColumn Header="Tracking" Binding="{Binding TrackingText}" Width="115"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding InventoryStockOnHand, Converter={StaticResource SmartQuantityConverter}, ConverterParameter=Display, TargetNullValue=—}" Width="75" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Avg Cost" Binding="{Binding MovingAverageCost, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Last Cost" Binding="{Binding LastLandedCost, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Minimum" Binding="{Binding MinimumPrice, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Retail" Binding="{Binding RetailPrice, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Wholesale" Binding="{Binding WholesalePrice, StringFormat=N2}" Width="90" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Maximum" Binding="{Binding MaximumPrice, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                    <DataGridTextColumn Header="Batch Price Status" Binding="{Binding BatchPriceStatus}" Width="135"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource GroupBox}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="SELECTED ITEM — MASTER PRICES" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,7"/>
                    <Grid Grid.Row="1" IsEnabled="{Binding HasSelectedItem}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="95"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Item" Style="{StaticResource Label}" Margin="0,0,0,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedItem.DisplayDescription, TargetNullValue=Select an item}" FontWeight="Bold" TextTrimming="CharacterEllipsis" Margin="0,0,0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="SKU / Type" Style="{StaticResource Label}" Margin="0,0,0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"><Run Text="{Binding SelectedItem.SkuCode}"/><Run Text="  |  "/><Run Text="{Binding SelectedItem.ItemTypeText}"/></TextBlock>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Minimum" Style="{StaticResource Label}" Margin="0,0,0,5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedItem.MinimumPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource Input}" Margin="0,0,0,5" HorizontalContentAlignment="Right"/>
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Retail" Style="{StaticResource Label}" Margin="0,0,0,5"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedItem.RetailPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource Input}" Margin="0,0,0,5" HorizontalContentAlignment="Right"/>
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Wholesale" Style="{StaticResource Label}" Margin="0,0,0,5"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedItem.WholesalePrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource Input}" Margin="0,0,0,5" HorizontalContentAlignment="Right"/>
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Maximum" Style="{StaticResource Label}" Margin="0,0,0,5"/>
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding SelectedItem.MaximumPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource Input}" Margin="0,0,0,5" HorizontalContentAlignment="Right"/>
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Tracking" Style="{StaticResource Label}"/>
                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SelectedItem.TrackingText}" Foreground="#555555"/>
                    </Grid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Content="RESET" Command="{Binding ResetMasterPricesCommand}" Style="{StaticResource Button}" Margin="0,0,6,0"/>
                        <Button Content="SAVE MASTER PRICES" Command="{Binding SaveMasterPricesCommand}" Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource GroupBox}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,6">
                        <TextBlock Text="SELECTED ITEM — BATCH PRICES" FontWeight="Bold" Foreground="#005555"/>
                        <TextBlock DockPanel.Dock="Right" Text="{Binding BatchPanelMessage}" Foreground="#666666" FontStyle="Italic"/>
                    </DockPanel>
                    <Grid Grid.Row="1" IsEnabled="{Binding CanShowBatchEditor}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="235"/>
                        </Grid.ColumnDefinitions>
                        <DataGrid Grid.Column="0" ItemsSource="{Binding ActiveBatches}" SelectedItem="{Binding SelectedBatch, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" RowHeight="27" HeadersVisibility="Column" AlternatingRowBackground="#F8F8F8" SelectionMode="Single" SelectionUnit="FullRow" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Batch" Binding="{Binding BatchNo}" Width="105"/>
                                <DataGridTextColumn Header="Internal Barcode" Binding="{Binding InternalBatchBarcode}" Width="120"/>
                                <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryDisplayText}" Width="88"/>
                                <DataGridTextColumn Header="Qty" Binding="{Binding CurrentStock, Converter={StaticResource SmartQuantityConverter}, ConverterParameter=Display}" Width="65" ElementStyle="{StaticResource RightText}"/>
                                <DataGridTextColumn Header="Cost" Binding="{Binding CostPrice, StringFormat=N2}" Width="75" ElementStyle="{StaticResource RightText}"/>
                                <DataGridTextColumn Header="Eff. Retail" Binding="{Binding EffectiveRetailPrice, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                                <DataGridTextColumn Header="Eff. W/S" Binding="{Binding EffectiveWholesalePrice, StringFormat=N2}" Width="85" ElementStyle="{StaticResource RightText}"/>
                                <DataGridTextColumn Header="Source" Binding="{Binding PriceSourceText}" Width="105"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Border Grid.Column="2" Background="#E8E8E8" BorderBrush="#AAAAAA" BorderThickness="1" Padding="8" IsEnabled="{Binding HasSelectedBatch}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding SelectedBatch.BatchNo, TargetNullValue=Select a batch}" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,7"/>
                                <TextBlock Grid.Row="1" Text="{Binding SelectedBatch.PriceSourceText}" Foreground="#555555" Margin="0,0,0,7"/>
                                <TextBlock Grid.Row="2" Text="Override Retail" Style="{StaticResource Label}"/>
                                <TextBox Grid.Row="3" Text="{Binding SelectedBatch.OverrideRetailPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource Input}" HorizontalContentAlignment="Right" Margin="0,3,0,7"/>
                                <TextBlock Grid.Row="4" Text="Override Wholesale" Style="{StaticResource Label}"/>
                                <StackPanel Grid.Row="5">
                                    <TextBox Text="{Binding SelectedBatch.OverrideWholesalePrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource Input}" HorizontalContentAlignment="Right" Margin="0,3,0,8"/>
                                    <Button Content="{Binding SelectedBatch.OverrideActionText, TargetNullValue=SET BATCH PRICE OVERRIDE}" Command="{Binding SaveBatchOverrideCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,5"/>
                                    <Button Content="REMOVE BATCH PRICE OVERRIDE" Command="{Binding RemoveBatchOverrideCommand}" Style="{StaticResource DangerButton}" IsEnabled="{Binding CanRemoveSelectedOverride}" Margin="0,0,0,5"/>
                                    <Button Content="RESET" Command="{Binding ResetBatchOverrideCommand}" Style="{StaticResource Button}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="6" Style="{StaticResource GroupBox}" Padding="7,5">
            <DockPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#444444"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock Text="Items: " FontWeight="Bold"/><TextBlock Text="{Binding TotalItems}" Margin="0,0,14,0"/>
                    <TextBlock Text="Stock: " FontWeight="Bold"/><TextBlock Text="{Binding StockItemCount}" Margin="0,0,14,0"/>
                    <TextBlock Text="Services: " FontWeight="Bold"/><TextBlock Text="{Binding ServiceItemCount}" Margin="0,0,14,0"/>
                    <TextBlock Text="Batch Overrides: " FontWeight="Bold"/><TextBlock Text="{Binding BatchOverrideCount}"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
