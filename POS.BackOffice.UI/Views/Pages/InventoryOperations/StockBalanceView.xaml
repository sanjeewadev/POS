<UserControl x:Class="POS.BackOffice.UI.Views.Pages.InventoryOperations.StockBalanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1300"
             Background="#F5F5F5">

    <UserControl.Resources>
        <!-- Standard Button Style -->
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Excel Export Button (Green) -->
        <Style TargetType="Button" x:Key="ExcelBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Print Button (Blue) -->
        <Style TargetType="Button" x:Key="PrintBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#2B5B84"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C3F5E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Standard Input Style -->
        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <!-- Group Box Border Style -->
        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Advanced Filtering Engine -->
            <RowDefinition Height="*"/>
            <!-- Inventory Data Grid -->
            <RowDefinition Height="Auto"/>
            <!-- Financial Footer & Actions -->
        </Grid.RowDefinitions>

        <!-- ========================================== -->
        <!-- ZONE 1: ADVANCED FILTERING ENGINE          -->
        <!-- ========================================== -->
        <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Stock Balance &amp; Inventory Valuation" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Search (Item Code / Name)" Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtSearch" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Filter by Location" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbLocation" Style="{StaticResource StandardInput}" DisplayMemberPath="LocationName" SelectedValuePath="Id"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Filter by Category" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbCategory" Style="{StaticResource StandardInput}" DisplayMemberPath="CategoryName" SelectedValuePath="Id"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Filter by Supplier" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbSupplier" Style="{StaticResource StandardInput}" DisplayMemberPath="CompanyName" SelectedValuePath="Id"/>
                    </StackPanel>

                    <Button Grid.Column="8" x:Name="btnRefresh" Content="Refresh Data" Style="{StaticResource StandardBtn}" Margin="0" VerticalAlignment="Bottom" Height="30" Width="100"/>
                </Grid>

                <WrapPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <CheckBox x:Name="chkHideZero" Content="Hide Zero Stock Items" IsChecked="True" Foreground="#333333" Cursor="Hand" Margin="0,0,25,0" ToolTip="Removes out-of-stock items from the view"/>
                    <CheckBox x:Name="chkNegativeOnly" Content="Show Negative Stock Only" IsChecked="False" Foreground="#A94044" FontWeight="SemiBold" Cursor="Hand" ToolTip="Audit tool to find items that sold below 0"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- ========================================== -->
        <!-- ZONE 2: INVENTORY GRID (REPORTING VIEW)    -->
        <!-- ========================================== -->
        <Border Grid.Row="1" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
            <DataGrid x:Name="dgStockBalance" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      IsReadOnly="True"
                      HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      BorderThickness="0"
                      RowHeight="35"
                      Background="Transparent"
                      SelectionMode="Single">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F8F8"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E2EBE9"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="100"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2.5*"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding LocationName}" Width="1.5*"/>
                    <DataGridTextColumn Header="UOM" Binding="{Binding Uom}" Width="70"/>

                    <DataGridTextColumn Header="Qty On Hand" Binding="{Binding QtyOnHand}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#2B5B84"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat=N2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Total Cost Value" Binding="{Binding TotalCostValue, StringFormat=N2}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#2B3C38"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Unit Retail" Binding="{Binding UnitRetail, StringFormat=N2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Total Retail Value" Binding="{Binding TotalRetailValue, StringFormat=N2}" Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#2B5B84"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ========================================== -->
        <!-- ZONE 3: FINANCIAL FOOTER & ACTIONS         -->
        <!-- ========================================== -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Counters -->
                <ColumnDefinition Width="*"/>
                <!-- Financials -->
                <ColumnDefinition Width="Auto"/>
                <!-- Actions -->
            </Grid.ColumnDefinitions>

            <!-- Basic Counters -->
            <Border Grid.Column="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15,10" Margin="0,0,15,0">
                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Total Line Items: " Foreground="#666666" FontSize="13"/>
                        <TextBlock x:Name="txtTotalItems" Text="0" FontWeight="Bold" Foreground="#333333" FontSize="14"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Total Physical Qty: " Foreground="#666666" FontSize="13"/>
                        <TextBlock x:Name="txtTotalQty" Text="0" FontWeight="Bold" Foreground="#2B5B84" FontSize="14"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Financial Valuation -->
            <Border Grid.Column="1" Background="#E2EBE9" BorderBrush="#B2D1C9" BorderThickness="1" CornerRadius="4" Padding="20,10" Margin="0,0,15,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                        <TextBlock Text="Total Inventory Asset Value (Cost):" Foreground="#2B3C38" FontSize="12"/>
                        <TextBlock x:Name="txtTotalCostValuation" Text="Rs. 0.00" FontWeight="Bold" FontSize="22" Foreground="#2B3C38"/>
                    </StackPanel>

                    <!-- Divider Line -->
                    <Border Grid.Column="1" Background="#B2D1C9" Margin="0,5"/>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="Projected Revenue Value (Retail):" Foreground="#2B5B84" FontSize="12"/>
                        <TextBlock x:Name="txtTotalRetailValuation" Text="Rs. 0.00" FontWeight="Bold" FontSize="22" Foreground="#2B5B84"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Clear Filters" Style="{StaticResource StandardBtn}" Background="#777777" Width="110"/>
                <Button Content="Export to Excel" Style="{StaticResource ExcelBtn}" Width="140"/>
                <Button Content="Print Report" Style="{StaticResource PrintBtn}" Width="130" Margin="0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>