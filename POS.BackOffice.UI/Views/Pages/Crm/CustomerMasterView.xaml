<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Crm.CustomerMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PointsBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#D97706"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B45309"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Background" Value="#FAFAFA"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="GroupHeader">
            <Setter Property="Foreground" Value="#2B3C38"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,20,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Customer Master Profile" FontSize="18" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

                    <Border Background="#FFF8EB" BorderBrush="#FDE0A6" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Customer Status &amp; KPIs" Foreground="#D97706" FontWeight="Bold" Margin="0,0,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="10"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="Tier" Foreground="#666666" FontSize="11" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="txtTierBadge" Text="Standard" FontWeight="Bold" FontSize="14" Foreground="#333333" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="Points" Foreground="#666666" FontSize="11" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="txtPoints" Text="0" FontWeight="Bold" FontSize="18" Foreground="#D97706" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="Life Spend" Foreground="#666666" FontSize="11" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="txtLifeSpend" Text="Rs. 0" FontWeight="Bold" FontSize="14" Foreground="#2B5B84" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <Border Grid.Row="2" Grid.ColumnSpan="3" Background="#FDF3F3" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="2" Padding="10,5">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="Outstanding Debt:" Foreground="#A94044" FontSize="12" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                        <TextBlock x:Name="txtOutstandingDebt" Text="Rs. 0.00" Foreground="#A94044" FontSize="14" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource GroupBoxBorder}">
                        <StackPanel>
                            <TextBlock Text="1. Personal Details" Style="{StaticResource GroupHeader}"/>

                            <TextBlock Text="Mobile Number (ID) *" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtMobile" Style="{StaticResource StandardInput}" MaxLength="15" Background="#F0F8FF" ToolTip="Used to identify the customer at the checkout"/>

                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="First Name *" Foreground="#666666" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtFirstName" Style="{StaticResource StandardInput}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Last Name" Foreground="#666666" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtLastName" Style="{StaticResource StandardInput}"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Email Address" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtEmail" Style="{StaticResource StandardInput}"/>

                            <TextBlock Text="Date of Birth" Foreground="#666666" Margin="0,0,0,5"/>
                            <DatePicker x:Name="dpDob" Style="{StaticResource StandardInput}" ToolTip="Used for automated birthday discounts"/>

                            <TextBlock Text="Postal Address" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtAddress" Style="{StaticResource StandardInput}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource GroupBoxBorder}">
                        <StackPanel>
                            <TextBlock Text="2. B2B Corporate Identity" Style="{StaticResource GroupHeader}" Foreground="#6F42C1"/>

                            <TextBlock Text="Company / Trade Name" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtCompanyName" Style="{StaticResource StandardInput}" ToolTip="Leave blank for standard retail customers"/>

                            <TextBlock Text="Tax / VAT Registration No." Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtTaxNo" Style="{StaticResource StandardInput}" ToolTip="Required for commercial wholesale invoicing"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource GroupBoxBorder}" Background="#F4FAF6" BorderBrush="#C3E6CB">
                        <StackPanel>
                            <TextBlock Text="3. Financial Rules &amp; Pricing" Style="{StaticResource GroupHeader}" Foreground="#28A745"/>

                            <TextBlock Text="Assigned Price Tier *" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cmbPriceTier" Style="{StaticResource StandardInput}" Background="#FFFFE0">
                                <ComboBoxItem Content="Standard Retail" IsSelected="True"/>
                                <ComboBoxItem Content="Wholesale Level 1 (Bulk)"/>
                                <ComboBoxItem Content="Wholesale Level 2 (Distributor)"/>
                                <ComboBoxItem Content="VIP / Staff Discount"/>
                            </ComboBox>

                            <TextBlock Text="Store Credit Limit (Rs.) *" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtCreditLimit" Style="{StaticResource StandardInput}" Text="0.00" HorizontalContentAlignment="Right" Background="#FDF3F3" ToolTip="Maximum allowed unpaid debt before the POS system blocks credit sales"/>
                        </StackPanel>
                    </Border>

                    <CheckBox x:Name="chkIsActive" Content="Active Customer Account" IsChecked="True" Foreground="#333333" Cursor="Hand" Margin="0,0,0,20"/>

                    <WrapPanel Orientation="Horizontal">
                        <Button Content="Save Profile" Style="{StaticResource StandardBtn}" Background="#2B5B84" Width="100"/>
                        <Button Content="Clear" Style="{StaticResource StandardBtn}" Width="80" Background="#777777"/>
                        <Button Content="Adjust Points" Style="{StaticResource PointsBtn}" Width="120" ToolTip="Manually add or remove points (Manager Only)"/>
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="180"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Search Customers:" VerticalAlignment="Center" Foreground="#666666" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" x:Name="txtSearch" Padding="8,5" ToolTip="Search by Mobile, Name, Company, or Email..." Margin="0"/>

                    <ComboBox Grid.Column="3" x:Name="cmbFilterTier" Padding="5" ToolTip="Filter by Price Tier">
                        <ComboBoxItem Content="All Tiers" IsSelected="True"/>
                        <ComboBoxItem Content="Standard Retail"/>
                        <ComboBoxItem Content="Wholesale Level 1"/>
                    </ComboBox>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
                <DataGrid x:Name="dgCustomers" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False" 
                          IsReadOnly="True" 
                          HeadersVisibility="Column" 
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#EEEEEE"
                          BorderThickness="0"
                          RowHeight="38"
                          Background="Transparent"
                          SelectionMode="Single">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F8F8"/>
                            <Setter Property="Foreground" Value="#333333"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#DDDDDD"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mobile No." Binding="{Binding Mobile}" Width="120" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Customer Name" Binding="{Binding FullName}" Width="160"/>
                        <DataGridTextColumn Header="Company (B2B)" Binding="{Binding CompanyName}" Width="140" Foreground="#666666"/>
                        <DataGridTextColumn Header="Price Tier" Binding="{Binding PriceTier}" Width="110" Foreground="#2B5B84"/>

                        <DataGridTextColumn Header="Debt (Rs.)" Binding="{Binding OutstandingDebt, StringFormat=N2}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Foreground" Value="#A94044"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70">
                            <DataGridCheckBoxColumn.ElementStyle>
                                <Style TargetType="CheckBox">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="IsHitTestVisible" Value="False"/>
                                </Style>
                            </DataGridCheckBoxColumn.ElementStyle>
                        </DataGridCheckBoxColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Row="3" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Background="#E2EBE9" Padding="10">
                        <TextBlock Text="Recent Purchase History (Selected Customer)" FontWeight="SemiBold" Foreground="#2B3C38"/>
                    </Border>

                    <DataGrid x:Name="dgPurchaseHistory" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" 
                              CanUserDeleteRows="False" 
                              IsReadOnly="True" 
                              HeadersVisibility="Column" 
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#EEEEEE"
                              BorderThickness="0"
                              RowHeight="30"
                              Background="Transparent">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="Foreground" Value="#666666"/>
                                <Setter Property="Padding" Value="10,5"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,0"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Invoice No." Binding="{Binding InvoiceNumber}" Width="120" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Location" Binding="{Binding LocationName}" Width="*"/>

                            <DataGridTextColumn Header="Amount Spent" Binding="{Binding AmountSpent, StringFormat=N2}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Points Earned" Binding="{Binding PointsEarned}" Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="#28A745"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>