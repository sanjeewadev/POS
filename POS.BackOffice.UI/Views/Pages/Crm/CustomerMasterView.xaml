<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Crm.CustomerMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBox" x:Key="ClassicInput">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="ClassicCombo">
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007777"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#003366" Padding="10" BorderBrush="#002244" BorderThickness="1" Margin="0,0,0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="CUSTOMER MASTER (CRM)" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <WrapPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Filter Type:" Foreground="#A0C0E0" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox SelectedItem="{Binding SelectedTypeFilter}" Style="{StaticResource ClassicCombo}" Width="120" Margin="0,0,15,0">
                        <System:String>All Customers</System:String>
                        <System:String>Retail</System:String>
                        <System:String>Wholesale</System:String>
                    </ComboBox>

                    <TextBlock Text="Search (Name/Phone):" Foreground="#A0C0E0" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ClassicInput}" Width="180" Margin="0,0,15,0"/>

                    <Button Content="[ F5 - REFRESH ]" Command="{Binding RefreshDataCommand}" Style="{StaticResource ClassicBtn}" Background="#D0E4F5" BorderBrush="#003366" Foreground="#003366" Height="24" Padding="10,0"/>
                </WrapPanel>

                <Button Grid.Column="2" Content="+ NEW CUSTOMER" Command="{Binding AddNewCustomerCommand}" Style="{StaticResource ClassicBtn}" Background="#059669" Foreground="White" BorderBrush="#047857" Height="28"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding CustomersList}" SelectedItem="{Binding SelectedCustomer}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="All" 
                  HorizontalGridLinesBrush="#C0C0C0" VerticalGridLinesBrush="#D0D0D0" 
                  RowHeight="26" HeadersVisibility="Column" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" 
                  SelectionMode="Single" AlternatingRowBackground="#F9F9F9" IsReadOnly="True" Margin="0,0,0,5">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#E0E0E0"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="#A0A0A0"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding CustomerCode}" Width="100" FontWeight="Bold"/>
                <DataGridTextColumn Header="Customer / Company Name" Binding="{Binding FullName}" Width="*" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Type" Binding="{Binding CustomerType}" Width="100"/>

                <DataGridTextColumn Header="Credit Limit Rs." Binding="{Binding CreditLimit, StringFormat=N2}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="0,0,4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Current Balance Rs." Binding="{Binding CurrentBalance, StringFormat=N2}" Width="130" FontWeight="Bold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="0,0,4,0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCreditLocked}" Value="True">
                                    <Setter Property="Foreground" Value="#8B0000"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridCheckBoxColumn Header="Locked" Binding="{Binding IsCreditLocked}" Width="60">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Style="{StaticResource ClassicGroupBox}" VerticalAlignment="Stretch" Padding="0">
            <Grid IsEnabled="{Binding IsCustomerSelected}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#E0E0E0" BorderBrush="#A0A0A0" BorderThickness="0,0,0,1" Padding="8,4">
                    <TextBlock Text="SELECTED CUSTOMER PROFILE" Style="{StaticResource HeaderLabel}" Margin="0"/>
                </Border>

                <Grid Grid.Row="1" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="1. DEMOGRAPHICS" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,10"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Account Code:" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="{Binding InputCustomerCode}" IsReadOnly="True" Style="{StaticResource ClassicInput}" Background="#E5E5E5" FontWeight="Bold"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Full Name *" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="{Binding InputFullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ClassicInput}" FontWeight="Bold" Background="#FFFFE0"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Phone *" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="{Binding InputPhone}" Style="{StaticResource ClassicInput}" Background="#FFFFE0" Margin="0,0,5,0"/>
                            <TextBlock Grid.Column="2" Text="Email:" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="3" Text="{Binding InputEmail}" Style="{StaticResource ClassicInput}"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Address:" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="{Binding InputAddress}" Style="{StaticResource ClassicInput}"/>
                        </Grid>

                        <Rectangle Height="1" Fill="#C0C0C0" Margin="0,5,0,10"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Company:" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="{Binding InputCompanyName}" Style="{StaticResource ClassicInput}" ToolTip="For Wholesale / B2B Clients"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="VAT/BR No:" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="{Binding InputVatNumber}" Style="{StaticResource ClassicInput}"/>
                        </Grid>
                    </StackPanel>

                    <Rectangle Grid.Column="1" Width="1" Fill="#A0A0A0" Margin="9,0"/>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="2. POS SETTINGS &amp; LOYALTY" FontWeight="Bold" Foreground="#005500" Margin="0,0,0,10"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Customer Type *" Style="{StaticResource InlineLabel}"/>
                            <ComboBox Grid.Column="1" SelectedItem="{Binding InputCustomerType}" Style="{StaticResource ClassicCombo}" FontWeight="Bold">
                                <System:String>Retail</System:String>
                                <System:String>Wholesale</System:String>
                            </ComboBox>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Loyalty Group:" Style="{StaticResource InlineLabel}"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableLoyaltyGroups}" SelectedItem="{Binding InputLoyaltyGroup}" DisplayMemberPath="GroupName" Style="{StaticResource ClassicCombo}" ToolTip="Select a group to auto-apply discounts at checkout"/>
                        </Grid>

                        <Rectangle Height="1" Fill="#C0C0C0" Margin="0,5,0,15"/>

                        <CheckBox Content="Account is Active (Can purchase items)" IsChecked="{Binding InputIsActive}" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>

                        <Button Content="💾 SAVE CUSTOMER PROFILE" Command="{Binding SaveCustomerCommand}" Style="{StaticResource ActionBtn}" Height="35" Margin="0,20,0,0"/>
                    </StackPanel>

                    <Rectangle Grid.Column="3" Width="1" Fill="#A0A0A0" Margin="9,0"/>

                    <Border Grid.Column="4" Background="#F8F8F8" BorderBrush="#C0C0C0" BorderThickness="1" Padding="15">
                        <StackPanel>
                            <TextBlock Text="3. FINANCIAL CONTROLS" FontWeight="Bold" Foreground="#8B0000" Margin="0,0,0,15"/>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Credit Limit Rs." Style="{StaticResource InlineLabel}"/>
                                <TextBox Grid.Column="1" Text="{Binding InputCreditLimit, StringFormat=N2}" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right" FontWeight="Bold"/>
                            </Grid>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Credit Terms:" Style="{StaticResource InlineLabel}"/>
                                <TextBox Grid.Column="1" Text="{Binding InputCreditDays}" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Center" ToolTip="Number of days to pay"/>
                            </Grid>

                            <Rectangle Height="2" Fill="#A0A0A0" Margin="0,5,0,15"/>

                            <TextBlock Text="CURRENT BALANCE:" FontSize="13" FontWeight="Bold" Foreground="#555" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding SelectedCustomer.CurrentBalance, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Heavy" Foreground="#8B0000" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                            <CheckBox Content="🚨 LOCK CREDIT ACCOUNT" IsChecked="{Binding InputIsCreditLocked}" FontWeight="Heavy" Foreground="#8B0000" ToolTip="Check this to instantly prevent cashier from finalizing any credit sales for this customer." Margin="0,0,0,15" HorizontalAlignment="Center">
                                <CheckBox.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                </CheckBox.LayoutTransform>
                            </CheckBox>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>