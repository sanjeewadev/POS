<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Crm.CustomerLedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="1400"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PostBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#1E7E34"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#155D27"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="OutlineBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2B5B84"/>
            <Setter Property="BorderBrush" Value="#2B5B84"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2EDF8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="GroupHeader">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="480"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}">
                <StackPanel>
                    <TextBlock Text="Customer Account &amp; Identity" Style="{StaticResource GroupHeader}"/>

                    <TextBlock Text="Search Customer *" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" x:Name="cmbCustomer" Style="{StaticResource StandardInput}" IsEditable="True" Margin="0">
                            <ComboBoxItem Content="CUST-0042: Apex Enterprises" IsSelected="True"/>
                            <ComboBoxItem Content="CUST-0105: John Doe"/>
                        </ComboBox>
                        <Button Grid.Column="1" Content="Load" Style="{StaticResource StandardBtn}" Background="#2B5B84" Margin="10,0,0,0" Height="30" Padding="15,0"/>
                    </Grid>

                    <Border Background="#F8F8F8" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Apex Enterprises" FontWeight="Bold" FontSize="16" Foreground="#333333"/>
                                <TextBlock Text="Mobile: +94 77 123 4567" Foreground="#666666" FontSize="11" Margin="0,2,0,0"/>
                                <TextBlock Text="Tier: Wholesale Level 1" Foreground="#6F42C1" FontSize="11" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="Credit Limit" Foreground="#666666" FontSize="11" HorizontalAlignment="Right"/>
                                <TextBlock Text="Rs. 500,000.00" FontWeight="Bold" FontSize="14" Foreground="#2B5B84"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource GroupBoxBorder}" Background="#F4FAF6" BorderBrush="#C3E6CB" Margin="0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock Text="Process Incoming Payment" Style="{StaticResource GroupHeader}" Foreground="#1E7E34"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Payment Date *" Foreground="#666666" Margin="0,0,0,5"/>
                                <DatePicker x:Name="dpPaymentDate" Style="{StaticResource StandardInput}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Receipt Amount (Rs.) *" Foreground="#1E7E34" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtReceiptAmount" Style="{StaticResource StandardInput}" Text="35000.00" HorizontalContentAlignment="Right" FontWeight="Bold" Background="#E8F4F8" Foreground="#2B5B84"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Method *" Foreground="#666666" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbMethod" Style="{StaticResource StandardInput}">
                                    <ComboBoxItem Content="Bank Transfer" IsSelected="True"/>
                                    <ComboBoxItem Content="Cheque"/>
                                    <ComboBoxItem Content="Cash"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Ref No. (Txn/Cheque ID)" Foreground="#666666" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtRefNo" Style="{StaticResource StandardInput}" Text="BOC-TXN-99812"/>
                            </StackPanel>
                        </Grid>

                        <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox x:Name="chkAutoAllocate" Content="Smart Auto-Allocate (Pay oldest invoices first)" FontWeight="SemiBold" Foreground="#2B5B84" IsChecked="False" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Manual Invoice Allocation Grid" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                    </StackPanel>

                    <Button DockPanel.Dock="Bottom" Content="POST PAYMENT &amp; UPDATE LEDGER" Style="{StaticResource PostBtn}" Height="45" Margin="0,15,0,0"/>

                    <DataGrid x:Name="dgAllocation" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" 
                              HeadersVisibility="Column" 
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#EEEEEE"
                              BorderThickness="1"
                              BorderBrush="#DDDDDD"
                              RowHeight="35"
                              Background="White"
                              IsEnabled="{Binding ElementName=chkAutoAllocate, Path=IsChecked, Converter={StaticResource InverseBooleanConverter}}">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F8F8"/>
                                <Setter Property="Foreground" Value="#333333"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="5,0"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Invoice No" Binding="{Binding InvoiceNo}" Width="100" IsReadOnly="True" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Due" Binding="{Binding Outstanding, StringFormat=N2}" Width="*" IsReadOnly="True" Foreground="#A94044"/>

                            <DataGridTemplateColumn Header="Apply Amt (Rs.)" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding AppliedAmount, UpdateSourceTrigger=PropertyChanged}" 
                                                 Background="#FFFFE0" BorderBrush="#CCCCCC" BorderThickness="1" 
                                                 HorizontalContentAlignment="Right" VerticalContentAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="15"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Total Outstanding Balance" Foreground="#666666" FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock Text="Rs. 135,000.00" FontWeight="Bold" FontSize="26" Foreground="#A94044"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="Available Credit Limit" Foreground="#666666" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            <TextBlock Text="Rs. 365,000.00" FontWeight="Bold" FontSize="26" Foreground="#28A745" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>

                    <Rectangle Grid.Row="1" Height="1" Fill="#EEEEEE" Margin="0,7"/>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Current (0-30 Days)" Foreground="#666666" FontSize="11"/>
                            <TextBlock Text="Rs. 85,000.00" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="31-60 Days Overdue" Foreground="#666666" FontSize="11"/>
                            <TextBlock Text="Rs. 40,000.00" FontWeight="Bold" Foreground="#D97706"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="61-90 Days Overdue" Foreground="#666666" FontSize="11"/>
                            <TextBlock Text="Rs. 10,000.00" FontWeight="Bold" Foreground="#A94044"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3">
                            <TextBlock Text="90+ Days Overdue" Foreground="#666666" FontSize="11"/>
                            <TextBlock Text="Rs. 0.00" FontWeight="Bold" Foreground="#C82333"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Background="#F8F8F8" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="15">
                        <TextBlock Text="Chronological Statement of Account" FontWeight="SemiBold" Foreground="#333333"/>
                    </Border>

                    <DataGrid x:Name="dgStatement" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" 
                              CanUserDeleteRows="False" 
                              IsReadOnly="True" 
                              HeadersVisibility="Column" 
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#EEEEEE"
                              BorderThickness="0"
                              RowHeight="40"
                              Background="Transparent">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="Foreground" Value="#666666"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,0"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding DocDate, StringFormat=dd/MMM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Document Ref" Binding="{Binding Reference}" Width="130" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Description / Remarks" Binding="{Binding Description}" Width="*"/>

                            <DataGridTextColumn Header="Debit (Increase Debt)" Binding="{Binding Debit, StringFormat=N2}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Foreground" Value="#A94044"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Credit (Decrease Debt)" Binding="{Binding Credit, StringFormat=N2}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Foreground" Value="#1E7E34"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Running Balance" Binding="{Binding Balance, StringFormat=N2}" Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#333333"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Send SMS Reminder" Style="{StaticResource OutlineBtn}" Width="160"/>
                <Button Content="🖨️ Export PDF Statement" Style="{StaticResource StandardBtn}" Background="#666666" Width="200" Margin="0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>