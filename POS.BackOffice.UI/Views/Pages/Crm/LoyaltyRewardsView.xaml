<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Crm.LoyaltyRewardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1200" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SaveBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="OutlineBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2B5B84"/>
            <Setter Property="BorderBrush" Value="#2B5B84"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2EDF8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="GroupHeader">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Loyalty &amp; Rewards Engine Configuration" FontSize="22" FontWeight="Bold" Foreground="#333333"/>
            <TextBlock Text="Manage point accrual, tier gamification, and financial liability limits." FontSize="12" Foreground="#666666" Margin="0,2,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
            <StackPanel>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="1. Global Program Settings" Style="{StaticResource GroupHeader}" Foreground="#2B5B84"/>

                        <CheckBox x:Name="chkEnableLoyalty" Content="Enable POS Loyalty &amp; Rewards Engine" FontWeight="Bold" Foreground="#28A745" Margin="0,0,0,15" IsChecked="True">
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                            </CheckBox.LayoutTransform>
                        </CheckBox>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Program Name *" Foreground="#333333" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtProgramName" Style="{StaticResource StandardInput}" Text="Apex VIP Rewards" ToolTip="Prints on customer receipts"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Enrollment Logic" Foreground="#666666" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbEnrollment" Style="{StaticResource StandardInput}">
                                    <ComboBoxItem Content="Auto-Enroll on Profile Creation" IsSelected="True"/>
                                    <ComboBoxItem Content="Explicit Opt-In Required"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Sign-Up Bonus Points" Foreground="#D97706" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtSignupBonus" Style="{StaticResource StandardInput}" Text="50" HorizontalContentAlignment="Center" Background="#FFF8EB"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="2. Accrual Engine (Earning Points)" Style="{StaticResource GroupHeader}" Foreground="#1E7E34"/>

                        <Border Background="#F4FAF6" BorderBrush="#C3E6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Customer spends Rs." VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBox Grid.Column="2" Text="100.00" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Right" FontWeight="Bold" Margin="0"/>
                                <TextBlock Grid.Column="4" Text="to earn" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBox Grid.Column="6" Text="1" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Center" FontWeight="Bold" Foreground="#1E7E34" Margin="0"/>

                                <TextBlock Grid.Column="8" Text="Rounding:" VerticalAlignment="Center" Foreground="#666666"/>
                                <ComboBox Grid.Column="10" Style="{StaticResource StandardInput}" Margin="0">
                                    <ComboBoxItem Content="Round Down (Floor) - Ex: Rs. 199 = 1 Pt" IsSelected="True"/>
                                    <ComboBoxItem Content="Standard Math - Ex: Rs. 150 = 2 Pts"/>
                                </ComboBox>
                            </Grid>
                        </Border>

                        <TextBlock Text="Multiplier Events (Bonus Rules)" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <CheckBox Content="Enable Birthday Multiplier" IsChecked="True" Foreground="#333333" Margin="0,0,0,5"/>
                                <ComboBox Style="{StaticResource StandardInput}">
                                    <ComboBoxItem Content="2x Points during Birthday Month" IsSelected="True"/>
                                    <ComboBoxItem Content="2x Points on Birthday Week"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <CheckBox Content="Enable Happy Hour / Specific Day Bonus" Foreground="#333333" Margin="0,0,0,5"/>
                                <ComboBox Style="{StaticResource StandardInput}" IsEnabled="False">
                                    <ComboBoxItem Content="2x Points on Tuesdays"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="3. Redemption Engine (Spending Points)" Style="{StaticResource GroupHeader}" Foreground="#6F42C1"/>

                        <Border Background="#F9F5FF" BorderBrush="#E5D6FA" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="1 Point =" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBox Grid.Column="2" Text="1" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Center" FontWeight="Bold" Margin="0" IsReadOnly="True" Background="#EEEEEE"/>
                                <TextBlock Grid.Column="4" Text="Rs. Discount of" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBox Grid.Column="6" Text="1.00" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Right" FontWeight="Bold" Foreground="#6F42C1" Margin="0"/>
                            </Grid>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Minimum Redemption Threshold" Foreground="#666666" Margin="0,0,0,5"/>
                                <TextBox Style="{StaticResource StandardInput}" Text="500 Points" ToolTip="Customer must have at least this many points to spend them"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Maximum Redemption Limit" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <ComboBox Style="{StaticResource StandardInput}" Background="#FDF3F3">
                                    <ComboBoxItem Content="Max 50% of Total Bill Amount" IsSelected="True"/>
                                    <ComboBoxItem Content="Max 100% of Total Bill Amount"/>
                                    <ComboBoxItem Content="Fixed Max Limit (e.g., Rs. 5000 per tx)"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <TextBlock Text="4. Margin Protection &amp; Exclusions" Style="{StaticResource GroupHeader}" Foreground="#A94044"/>
                        <TextBlock Text="Define scenarios where customers DO NOT earn points to protect business profit margins." FontSize="11" Foreground="#666666" Margin="0,-10,0,15"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <CheckBox Content="Exclude items already on Sale / Manual Discount" FontWeight="SemiBold" Foreground="#333333" IsChecked="True" Margin="0,0,20,0"/>
                            <CheckBox Content="Exclude Wholesale / Tier 1 &amp; Tier 2 Customers" FontWeight="SemiBold" Foreground="#333333" IsChecked="True"/>
                        </StackPanel>

                        <TextBlock Text="Excluded Product Categories (Legal / Margin Restrictions)" Foreground="#666666" Margin="0,0,0,5"/>
                        <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Background="#F8F8F8" Padding="10" MinHeight="80">
                            <WrapPanel>
                                <Border Background="#E2EBE9" BorderBrush="#B2D1C9" BorderThickness="1" CornerRadius="12" Padding="10,4" Margin="0,0,5,5">
                                    <TextBlock Text="Tobacco &amp; Cigarettes ✕" FontSize="11" Foreground="#2B3C38"/>
                                </Border>
                                <Border Background="#E2EBE9" BorderBrush="#B2D1C9" BorderThickness="1" CornerRadius="12" Padding="10,4" Margin="0,0,5,5">
                                    <TextBlock Text="Pharmaceuticals ✕" FontSize="11" Foreground="#2B3C38"/>
                                </Border>
                                <Button Content="+ Add Category Exclusions" Background="Transparent" Foreground="#2B5B84" BorderThickness="0" Cursor="Hand" Margin="5,0,0,5" Padding="5,0" FontWeight="SemiBold"/>
                            </WrapPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="5. Automated Tier Gamification" Style="{StaticResource GroupHeader}" Foreground="#D97706" Margin="0,0,0,2"/>
                                <TextBlock Text="Automatically upgrade customers based on their Lifetime Spend." FontSize="11" Foreground="#666666"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="+ Add Tier" Style="{StaticResource OutlineBtn}" Height="30" Padding="10,0"/>
                        </Grid>

                        <DataGrid AutoGenerateColumns="False" 
                                  CanUserAddRows="False" 
                                  HeadersVisibility="Column" 
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#EEEEEE"
                                  BorderThickness="1"
                                  BorderBrush="#DDDDDD"
                                  RowHeight="35"
                                  Background="White">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F8F8F8"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tier Name" Binding="{Binding TierName}" Width="150" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Entry Threshold (Lifetime Spend Rs.)" Binding="{Binding EntryThreshold}" Width="250"/>
                                <DataGridTextColumn Header="Earning Multiplier" Binding="{Binding Multiplier}" Width="150" Foreground="#1E7E34" FontWeight="SemiBold"/>
                                <DataGridTextColumn Header="Perk Description" Binding="{Binding Description}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GroupBoxBorder}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="6. Point Liability &amp; Expiration" Style="{StaticResource GroupHeader}"/>

                        <TextBlock Text="Point Expiration Logic *" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox Style="{StaticResource StandardInput}" Width="400" HorizontalAlignment="Left">
                            <ComboBoxItem Content="Inactivity Expiry: 12 Months without purchase" IsSelected="True"/>
                            <ComboBoxItem Content="Rolling Expiry: 12 Months from earning date"/>
                            <ComboBoxItem Content="Points Never Expire"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0" Padding="20" Margin="-20,15,-20,-20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="WARNING: Changing Conversion Values applies instantly to all future checkout sessions." Foreground="#A94044" FontWeight="Bold"/>
                    <TextBlock Text="Historical point balances remain unchanged. Changes to earning logic are not retroactive." Foreground="#666666" FontSize="11" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Discard Changes" Style="{StaticResource StandardBtn}" Background="White" Foreground="#666666" BorderBrush="#CCCCCC" BorderThickness="1" Width="140"/>
                    <Button Content="SAVE LOYALTY RULES" Style="{StaticResource SaveBtn}" Width="220" Margin="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>