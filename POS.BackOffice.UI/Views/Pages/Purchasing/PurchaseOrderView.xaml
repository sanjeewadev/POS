<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Purchasing.PurchaseOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>

        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBox" x:Key="ClassicInput">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="ClassicCombo">
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DatePicker" x:Key="ClassicDate">
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>

        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007777"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#005555"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell" x:Key="FrozenCell" BasedOn="{StaticResource BaseCell}">
            <Setter Property="Background" Value="#F5F5F5"/>
        </Style>
        <Style TargetType="DataGridCell" x:Key="SohCell" BasedOn="{StaticResource BaseCell}">
            <Setter Property="Background" Value="#F0FFF0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="480"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
                <StackPanel>
                    <TextBlock Text="1. PURCHASE ORDER SETUP" Style="{StaticResource HeaderLabel}" Margin="0,0,0,5"/>

                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Supplier *" Style="{StaticResource InlineLabel}"/>
                        <ComboBox Grid.Column="1" IsEditable="True" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="CompanyName" Style="{StaticResource ClassicCombo}"/>
                    </Grid>

                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Terms:" Style="{StaticResource InlineLabel}"/>
                        <ComboBox Grid.Column="1" Style="{StaticResource ClassicCombo}" Margin="0,0,10,0">
                            <ComboBoxItem Content="30 Days Credit" IsSelected="True"/>
                            <ComboBoxItem Content="Cash on Delivery"/>
                            <ComboBoxItem Content="Advance Payment"/>
                        </ComboBox>

                        <TextBlock Grid.Column="2" Text="Cr. Days:" Style="{StaticResource InlineLabel}"/>
                        <TextBox Grid.Column="3" Text="30" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Order Date:" Style="{StaticResource InlineLabel}"/>
                        <DatePicker Grid.Column="1" SelectedDate="{Binding OrderDate}" Style="{StaticResource ClassicDate}" Margin="0,0,10,0"/>

                        <TextBlock Grid.Column="2" Text="Exp. Del:" Style="{StaticResource InlineLabel}" Foreground="#005500"/>
                        <DatePicker Grid.Column="3" SelectedDate="{Binding ExpectedDate}" Style="{StaticResource ClassicDate}"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Requisitioner:" Style="{StaticResource InlineLabel}"/>
                        <TextBox Grid.Column="1" Text="{Binding CurrentUser}" IsReadOnly="True" Background="#E5E5E5" Style="{StaticResource ClassicInput}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource ClassicGroupBox}" VerticalAlignment="Stretch">
                <StackPanel>
                    <TextBlock Text="2. ITEM ENTRY CONSOLE" Style="{StaticResource HeaderLabel}" Foreground="#005555" Margin="0,0,0,5"/>

                    <Border Background="#334155" Padding="8" BorderBrush="#1E293B" BorderThickness="1" Margin="0,0,0,10">
                        <StackPanel>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Barcode:" Foreground="#DEFF9A" Style="{StaticResource InlineLabel}"/>
                                <TextBox Grid.Column="1" Text="{Binding ScanBarcode}" Height="24" FontSize="12" FontWeight="Bold" ToolTip="Scan direct variant or standard item here"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="65"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Code:" Foreground="White" Style="{StaticResource InlineLabel}"/>
                                <ComboBox Grid.Column="1" IsEditable="True" Height="24" Margin="0,0,10,0"/>

                                <TextBlock Grid.Column="2" Text="Name:" Foreground="White" Style="{StaticResource InlineLabel}"/>
                                <ComboBox Grid.Column="3" IsEditable="True" Height="24" ToolTip="Select a standard item OR a Matrix Parent"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="ORDER PARAMETERS" FontSize="10" FontWeight="Bold" Foreground="#666" Margin="0,0,0,5"/>

                    <Border Background="#F0F8FF" BorderBrush="#005555" BorderThickness="1" Padding="8" Margin="0,0,0,8" Visibility="Visible">
                        <StackPanel>
                            <TextBlock Text="⚡ MATRIX PURCHASING" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,5"/>

                            <DataGrid Height="120" ItemsSource="{Binding ActiveMatrixVariants}" SelectedItem="{Binding SelectedMatrixVariant}"
                                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                      HeadersVisibility="Column" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" 
                                      RowHeight="24" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                                      FrozenColumnCount="2" SelectionMode="Single">
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#E5E5E5"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="4"/>
                                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                        <Setter Property="BorderBrush" Value="#A0A0A0"/>
                                    </Style>
                                </DataGrid.Resources>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Variant Attributes" Binding="{Binding VariantDescription}" Width="130" CellStyle="{StaticResource FrozenCell}"/>
                                    <DataGridTextColumn Header="Current SOH" Binding="{Binding CurrentSOH}" Width="80" FontWeight="Bold" Foreground="#005500" CellStyle="{StaticResource SohCell}">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Order Qty" Binding="{Binding OrderQty}" Width="80" FontWeight="Bold">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="Exp. Cost Rs." Binding="{Binding ExpectedCost, StringFormat=N2}" Width="95">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <Border Background="#FFF7ED" BorderBrush="#D97706" BorderThickness="1" Padding="6" Margin="0,4,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="55"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding SelectedMatrixVariant.VariantDescription}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" FontWeight="Bold" Foreground="#333"/>

                                    <TextBlock Grid.Column="1" Text="Ord Qty:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0" FontSize="11"/>
                                    <TextBox Grid.Column="2" Text="{Binding SelectedMatrixVariant.OrderQty}" Style="{StaticResource ClassicInput}" FontWeight="Bold" Background="#E0FFFF" HorizontalContentAlignment="Center"/>

                                    <TextBlock Grid.Column="3" Text="Cost:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0" FontSize="11"/>
                                    <TextBox Grid.Column="4" Text="{Binding SelectedMatrixVariant.ExpectedCost}" Style="{StaticResource ClassicInput}" Background="#FFFFE0" HorizontalContentAlignment="Right"/>

                                    <Button Grid.Column="5" Content="UPDATE ROW" Command="{Binding UpdateMatrixRowCommand}" Style="{StaticResource ClassicBtn}" Height="24" Margin="5,0,0,0"/>
                                </Grid>
                            </Border>

                            <Grid Margin="0,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Tax Code:" Style="{StaticResource InlineLabel}"/>
                                <ComboBox Grid.Column="1" Style="{StaticResource ClassicCombo}" Margin="0,0,10,0">
                                    <ComboBoxItem Content="Exempt" IsSelected="True"/>
                                    <ComboBoxItem Content="VAT 18%"/>
                                </ComboBox>

                                <TextBlock Grid.Column="3" Text="UOM:" Style="{StaticResource InlineLabel}"/>
                                <ComboBox Grid.Column="4" Style="{StaticResource ClassicCombo}">
                                    <ComboBoxItem Content="PCS" IsSelected="True"/>
                                    <ComboBoxItem Content="BOX"/>
                                </ComboBox>
                            </Grid>

                            <Button Content="⚡ ADD ENTIRE MATRIX TO PO" Command="{Binding AddMatrixCommand}" Style="{StaticResource ActionBtn}" Background="#0066CC" BorderBrush="#004C99" Height="30" Margin="0,10,0,0" FontSize="13"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Visibility="Collapsed">
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Order Qty:" Style="{StaticResource InlineLabel}" Foreground="#005555"/>
                            <TextBox Grid.Column="1" Text="100" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Center" FontWeight="Bold" Background="#E0FFFF"/>

                            <TextBlock Grid.Column="3" Text="UOM:" Style="{StaticResource InlineLabel}"/>
                            <ComboBox Grid.Column="4" Style="{StaticResource ClassicCombo}">
                                <ComboBoxItem Content="PCS" IsSelected="True"/>
                                <ComboBoxItem Content="BOX"/>
                            </ComboBox>
                        </Grid>

                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Exp. Cost:" Style="{StaticResource InlineLabel}"/>
                            <TextBox Grid.Column="1" Text="145.00" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right" Background="#FFFFE0"/>

                            <TextBlock Grid.Column="3" Text="Est Disc:" Style="{StaticResource InlineLabel}" Foreground="DarkRed"/>
                            <TextBox Grid.Column="4" Text="0.00" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Tax Code:" Style="{StaticResource InlineLabel}"/>
                            <ComboBox Grid.Column="1" Style="{StaticResource ClassicCombo}">
                                <ComboBoxItem Content="Exempt" IsSelected="True"/>
                                <ComboBoxItem Content="VAT 18%"/>
                            </ComboBox>
                        </Grid>

                        <Button Content="ADD TO ORDER GRID" Command="{Binding AddItemCommand}" Style="{StaticResource ActionBtn}" Height="34" FontSize="13"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#003366" Padding="10" BorderBrush="#002244" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="PO NO:" Foreground="#A0C0E0" FontSize="14" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock Text="PO-2026-0045" Foreground="White" FontSize="18" FontWeight="Bold"/>
                    </StackPanel>
                    <Border Grid.Column="2" Background="#FFD700" Padding="15,4">
                        <TextBlock Text="DRAFT / ESTIMATE" FontWeight="Bold" Foreground="Black" FontSize="12"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="#E5E5E5" Padding="5" BorderBrush="#A0A0A0" BorderThickness="1,0,1,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Selected Row Actions:" VerticalAlignment="Center" FontWeight="Bold" Foreground="#666" Margin="0,0,10,0"/>
                    <Button Content="📋 Show Price History" Style="{StaticResource ClassicBtn}" Height="24" Margin="0,0,5,0"/>
                    <Button Content="🗑️ Remove Item" Style="{StaticResource ClassicBtn}" Foreground="DarkRed" Height="24"/>
                </StackPanel>
            </Border>

            <DataGrid Grid.Row="2" x:Name="dgPoLines" ItemsSource="{Binding PoLines}" SelectedItem="{Binding SelectedLine}" 
                      AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="All" IsReadOnly="True"
                      HorizontalGridLinesBrush="#C0C0C0" VerticalGridLinesBrush="#D0D0D0" 
                      RowHeight="26" HeadersVisibility="Column" Background="White" BorderBrush="#A0A0A0" BorderThickness="1,0,1,0" 
                      SelectionMode="Single" AlternatingRowBackground="#F9F9F9">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="BorderBrush" Value="#A0A0A0"/>
                    </Style>
                    <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="85" Foreground="#666"/>
                    <DataGridTextColumn Header="Variant Attributes" Binding="{Binding VariantDescription}" Width="110" Foreground="#444"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="UOM" Binding="{Binding Uom}" Width="50"/>

                    <DataGridTextColumn Header="SOH" Binding="{Binding SOH}" Width="45" Foreground="#005500">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Order Qty" Binding="{Binding OrderQty}" Width="70" FontWeight="Bold">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Exp. Unit Cost" Binding="{Binding ExpectedCost, StringFormat=N2}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Est. Disc" Binding="{Binding LineDiscount, StringFormat=N2}" Width="65" Foreground="DarkRed">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Tax Code" Binding="{Binding TaxCode}" Width="70" Foreground="#666"/>

                    <DataGridTextColumn Header="Est. Line Total" Binding="{Binding LineTotal, StringFormat=N2}" Width="110" FontWeight="Bold">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="3" Background="#E0FFFF" BorderBrush="#005555" BorderThickness="1,1,1,1" Padding="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="✏️ EDIT SELECTED LINE:" FontWeight="Bold" Foreground="#005555" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding SelectedLine.Description}" VerticalAlignment="Center" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedLine.Description}"/>

                    <TextBlock Grid.Column="2" Text="Ord Qty:" VerticalAlignment="Center" Margin="10,0,5,0" FontSize="11"/>
                    <TextBox Grid.Column="3" Text="{Binding SelectedLine.OrderQty}" Width="60" Style="{StaticResource ClassicInput}" FontWeight="Bold" HorizontalContentAlignment="Center"/>

                    <TextBlock Grid.Column="4" Text="Exp Cost:" VerticalAlignment="Center" Margin="10,0,5,0" FontSize="11"/>
                    <TextBox Grid.Column="5" Text="{Binding SelectedLine.ExpectedCost, StringFormat=N2}" Width="80" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right"/>

                    <TextBlock Grid.Column="6" Text="Disc:" VerticalAlignment="Center" Margin="10,0,5,0" FontSize="11"/>
                    <TextBox Grid.Column="7" Text="{Binding SelectedLine.LineDiscount, StringFormat=N2}" Width="60" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right"/>

                    <Button Grid.Column="8" Content="UPDATE LINE" Command="{Binding UpdateLineCommand}" Style="{StaticResource ActionBtn}" Height="24" Padding="15,0" Margin="10,0,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="4" Background="#D4D0C8" BorderBrush="#A0A0A0" BorderThickness="1" Margin="0,5,0,0" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Special Instructions / Supplier Notes:" Style="{StaticResource InlineLabel}" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Remarks}" TextWrapping="Wrap" AcceptsReturn="True" Height="75" Style="{StaticResource ClassicInput}"/>
                    </StackPanel>

                    <Rectangle Grid.Column="1" Width="1" Fill="#A0A0A0" Margin="7,0"/>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Text="GLOBAL ESTIMATES" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Est. Disc (-)" Style="{StaticResource InlineLabel}" Foreground="DarkRed"/>
                            <TextBox Grid.Column="1" Text="{Binding GlobalBillDiscount, StringFormat=N2}" Style="{StaticResource ClassicInput}" HorizontalContentAlignment="Right" Background="#FEF2F2"/>
                        </Grid>
                        <TextBlock Text="(Applied to total order value)" FontSize="10" Foreground="#888" Margin="0,5,0,0" FontStyle="Italic"/>
                    </StackPanel>

                    <Rectangle Grid.Column="3" Width="1" Fill="#A0A0A0" Margin="7,0"/>

                    <StackPanel Grid.Column="4" VerticalAlignment="Center">
                        <Grid Margin="0,0,0,4">
                            <TextBlock Text="EST. SUBTOTAL:" Foreground="#555" FontSize="12" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Subtotal, StringFormat=Rs. \{0:N2\}}" HorizontalAlignment="Right" FontSize="12" FontWeight="Bold"/>
                        </Grid>
                        <Grid Margin="0,0,0,4">
                            <TextBlock Text="EST. DISCOUNTS:" Foreground="DarkRed" FontSize="12" FontWeight="Bold"/>
                            <TextBlock Text="{Binding TotalDiscountAmount, StringFormat=- Rs. \{0:N2\}}" HorizontalAlignment="Right" Foreground="DarkRed" FontSize="12" FontWeight="Bold"/>
                        </Grid>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="EST. TAX (VAT):" Foreground="#555" FontSize="12" FontWeight="Bold"/>
                            <TextBlock Text="{Binding TotalTaxAmount, StringFormat=+ Rs. \{0:N2\}}" HorizontalAlignment="Right" FontSize="12" FontWeight="Bold"/>
                        </Grid>

                        <Rectangle Height="2" Fill="#333" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="TOTAL EST. VALUE:" Foreground="#003366" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding NetPayable, StringFormat=Rs. \{0:N2\}}" Foreground="#003366" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Save Draft" Style="{StaticResource ClassicBtn}" Height="36"/>
                            <Button Grid.Column="2" Content="Print PO" Style="{StaticResource ClassicBtn}" Height="36"/>
                            <Button Grid.Column="4" Content="APPROVE &amp; SEND" Background="#005500" Foreground="White" FontWeight="Bold" BorderBrush="#003300" BorderThickness="1" Height="36" Cursor="Hand"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>