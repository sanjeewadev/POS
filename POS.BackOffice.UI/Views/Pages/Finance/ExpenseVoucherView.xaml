<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Finance.ExpenseVoucherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PostBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#6F42C1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5936A2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="UploadBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#2B5B84"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C3F5E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Margin="0,0,0,15">
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="General Expense Voucher (Operational Costs)" FontSize="20" FontWeight="SemiBold" Foreground="#6F42C1"/>

                    <Border Grid.Column="1" Background="#FFF3CD" BorderBrush="#FFEEBA" BorderThickness="1" CornerRadius="4" Padding="10,5">
                        <TextBlock Text="Status: DRAFT" FontWeight="Bold" Foreground="#856404"/>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Cost Center / Department *" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbCostCenter" Style="{StaticResource StandardInput}" Margin="0,0,0,5" ToolTip="Which department is incurring this cost?">
                            <ComboBoxItem Content="Main Branch - Sales Floor" IsSelected="True"/>
                            <ComboBoxItem Content="Central Warehouse"/>
                            <ComboBoxItem Content="Admin &amp; Back Office"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Expense Date *" Foreground="#666666" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpExpenseDate" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Approved By (Manager) *" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtApprovedBy" Style="{StaticResource StandardInput}" ToolTip="Name or PIN of the manager authorizing this expense"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Voucher No." Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtVoucherNo" Style="{StaticResource StandardInput}" Background="#EEEEEE" IsReadOnly="True" Text="[Auto-Generated]"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#F4FAF6" BorderBrush="#C3E6CB" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Add Expense Item (Chart of Accounts)" FontWeight="SemiBold" Foreground="#28A745" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="GL Category *" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbExpenseCategory" Style="{StaticResource StandardInput}" Margin="0">
                            <ComboBoxItem Content="Utility Bills (Electricity/Water)"/>
                            <ComboBoxItem Content="Office Stationery &amp; Printing"/>
                            <ComboBoxItem Content="Repairs &amp; Maintenance"/>
                            <ComboBoxItem Content="Staff Meals &amp; Welfare"/>
                            <ComboBoxItem Content="Transport &amp; Travel"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Payee Name *" Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtPayee" Style="{StaticResource StandardInput}" Margin="0" ToolTip="e.g., Ceylon Electricity Board or John (Plumber)"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Description / Narration *" Foreground="#666666" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtDescription" Style="{StaticResource StandardInput}" Margin="0" ToolTip="e.g., Fixing the broken AC in the main lobby"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Amount (Rs.) *" Foreground="#A94044" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtAmount" Style="{StaticResource StandardInput}" HorizontalContentAlignment="Right" Background="#FFFFE0" FontWeight="Bold" Margin="0" Text="0.00"/>
                    </StackPanel>

                    <Button Grid.Column="8" x:Name="btnAddExpense" Content="Add to List" Style="{StaticResource StandardBtn}" Background="#28A745" Margin="0" VerticalAlignment="Bottom" Height="30" Width="100"/>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
            <DataGrid x:Name="dgExpenseLines" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      IsReadOnly="True"
                      HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      BorderThickness="0"
                      RowHeight="40"
                      Background="Transparent">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F8F8"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="GL Category" Binding="{Binding Category}" Width="180" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Payee" Binding="{Binding Payee}" Width="150" Foreground="#666666"/>
                    <DataGridTextColumn Header="Description / Narration" Binding="{Binding Description}" Width="*"/>

                    <DataGridTextColumn Header="Amount (Rs.)" Binding="{Binding Amount, StringFormat=N2}" Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#A94044"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Del" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="X" Background="#A94044" Foreground="White" BorderThickness="0" Cursor="Hand" Width="25" Height="25" FontWeight="Bold"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F9F5FF" BorderBrush="#E5D6FA" BorderThickness="1" CornerRadius="4" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="5" Text="Payment Source &amp; Documentation" FontWeight="SemiBold" Foreground="#6F42C1" Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1" Grid.Column="0">
                        <TextBlock Text="Payment Method *" Foreground="#333333" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbPaymentMethod" Style="{StaticResource StandardInput}" Margin="0">
                            <ComboBoxItem Content="Cash (Petty Cash)" IsSelected="True"/>
                            <ComboBoxItem Content="Cheque"/>
                            <ComboBoxItem Content="Company Credit Card"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2">
                        <TextBlock Text="Paying From (Source) *" Foreground="#A94044" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbSourceAccount" Style="{StaticResource StandardInput}" Margin="0" Background="#FFFFE0" ToolTip="Which bank account or safe is this money leaving from?">
                            <ComboBoxItem Content="Main Shop Petty Cash" IsSelected="True"/>
                            <ComboBoxItem Content="BOC Current Account"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="4" VerticalAlignment="Bottom">
                        <Button Content="📎 Attach Receipt / Bill" Style="{StaticResource UploadBtn}" Height="30" Margin="0" ToolTip="Upload a scanned image or PDF of the physical receipt for tax auditing"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="TOTAL VOUCHER AMOUNT:" Foreground="#333333" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="txtTotalExpense" Text="Rs. 0.00" FontWeight="Bold" FontSize="24" Foreground="#6F42C1" HorizontalAlignment="Right"/>
                    </DockPanel>

                    <TextBlock Grid.Row="1" Text="Ensure all attachments are uploaded before posting. This action will deduct funds from the selected source account." TextWrapping="Wrap" Foreground="#666666" FontSize="11" FontStyle="Italic"/>

                    <Button Grid.Row="2" Content="POST EXPENSE VOUCHER" Style="{StaticResource PostBtn}" Height="45" Margin="0,15,0,0" ToolTip="Records the expense and deducts cash from the source account"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>