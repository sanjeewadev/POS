<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Finance.SupplierClaimsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}"
             FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>
        <Style x:Key="ClassicButton" TargetType="Button">
            <Setter Property="Height" Value="27"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{DynamicResource ButtonDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBoxBorderBrush}"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource LightSelectionBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="145"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="8" Margin="0,0,0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="95"/><ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="55"/><ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="75"/><ColumnDefinition Width="125"/>
                    <ColumnDefinition Width="75"/><ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="From" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding StartDate}" Height="26"/>
                <TextBlock Grid.Column="2" Text="To" VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold"/>
                <DatePicker Grid.Column="3" SelectedDate="{Binding EndDate}" Height="26"/>
                <TextBlock Grid.Column="4" Text="Status" VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="5" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" Height="26"/>
                <TextBlock Grid.Column="6" Text="Supplier" VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="7" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}"
                          DisplayMemberPath="DisplayText" Height="26"/>
                <TextBox Grid.Column="8" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="26" Margin="8,0,0,0" VerticalContentAlignment="Center" ToolTip="Invoice, claim, item, SKU or promotion"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="7" Margin="0,0,0,5">
            <DockPanel LastChildFill="False">
                <Button Content="Load" Command="{Binding LoadClaimsCommand}" Style="{StaticResource ClassicButton}"/>
                <Button Content="Submit" Command="{Binding SubmitCommand}" IsEnabled="{Binding CanSubmit}" Style="{StaticResource ClassicButton}"/>
                <Button Content="Settle" Click="Settle_Click" IsEnabled="{Binding CanSettle}" Style="{StaticResource ClassicButton}"/>
                <Button Content="Reject" Click="Reject_Click" IsEnabled="{Binding CanReject}" Style="{StaticResource ClassicButton}"/>
                <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Style="{StaticResource ClassicButton}"/>
                <Button Content="Save PDF" Command="{Binding ExportPdfCommand}" Style="{StaticResource ClassicButton}" ToolTip="Manager or Administrator access required."/>
                <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColorHex}" Margin="12,5,0,0" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Claims}" SelectedItem="{Binding SelectedClaim}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow"
                  RowHeight="26" GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  Background="{DynamicResource WhiteBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1" Margin="0,0,0,5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat=yyyy-MM-dd}" Width="88"/>
                <DataGridTextColumn Header="Claim" Binding="{Binding ClaimReferenceNo}" Width="125"/>
                <DataGridTextColumn Header="Status" Binding="{Binding ClaimStatus}" Width="80"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="150"/>
                <DataGridTextColumn Header="Promotion" Binding="{Binding SupplierPromotionReference}" Width="120"/>
                <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNo}" Width="105"/>
                <DataGridTextColumn Header="Item" Binding="{Binding ItemDescription}" Width="*" MinWidth="180"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding SkuCode}" Width="100"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat=N3}" Width="65"/>
                <DataGridTextColumn Header="Returned" Binding="{Binding ReturnedQuantity, StringFormat=N3}" Width="72"/>
                <DataGridTextColumn Header="Net Qty" Binding="{Binding NetClaimQuantity, StringFormat=N3}" Width="70"/>
                <DataGridTextColumn Header="Claim" Binding="{Binding ClaimValue, StringFormat=N2}" Width="85"/>
                <DataGridTextColumn Header="Reduction" Binding="{Binding ClaimValueReduction, StringFormat=N2}" Width="82"/>
                <DataGridTextColumn Header="Net Claim" Binding="{Binding NetClaimValue, StringFormat=N2}" Width="88"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="5"/><ColumnDefinition Width="3*"/></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <TextBlock Text="SELECTED CLAIM" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}"/>
                    <Grid Grid.Row="1" Margin="0,6,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Text="Rule / reason" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="{Binding SelectedClaim.FreeIssueRuleName}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Row="1" Text="Applied by" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedClaim.FreeIssueAppliedBy}" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="2" Text="Approved by" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,4,0,0"><Run Text="{Binding SelectedClaim.FreeApprovedBy}"/><Run Text="  "/><Run Text="{Binding SelectedClaim.FreeApprovedRole}"/></TextBlock>
                        <TextBlock Grid.Row="3" Text="Remarks" FontWeight="SemiBold" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedClaim.Remarks}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="8">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <TextBlock Text="SUPPLIER / PROMOTION SUMMARY" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding GroupedSummaries}" AutoGenerateColumns="False" IsReadOnly="True"
                              RowHeight="24" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Margin="0,5,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="*"/>
                            <DataGridTextColumn Header="Promotion" Binding="{Binding PromotionReference}" Width="130"/>
                            <DataGridTextColumn Header="Claims" Binding="{Binding ClaimCount}" Width="55"/>
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat=N3}" Width="65"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding ClaimValue, StringFormat=N2}" Width="85"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="4" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="8" Margin="0,5,0,0">
            <WrapPanel>
                <TextBlock Text="Claims:" FontWeight="SemiBold"/><TextBlock Text="{Binding TotalClaimCount}" Margin="4,0,18,0"/>
                <TextBlock Text="Qty:" FontWeight="SemiBold"/><TextBlock Text="{Binding TotalQuantity, StringFormat=N3}" Margin="4,0,18,0"/>
                <TextBlock Text="Returned:" FontWeight="SemiBold"/><TextBlock Text="{Binding ReturnedQuantity, StringFormat=N3}" Margin="4,0,18,0"/>
                <TextBlock Text="Draft:" FontWeight="SemiBold"/><TextBlock Text="{Binding DraftCount}" Margin="4,0,12,0"/>
                <TextBlock Text="Submitted:" FontWeight="SemiBold"/><TextBlock Text="{Binding SubmittedCount}" Margin="4,0,12,0"/>
                <TextBlock Text="Settled:" FontWeight="SemiBold"/><TextBlock Text="{Binding SettledCount}" Margin="4,0,12,0"/>
                <TextBlock Text="Rejected:" FontWeight="SemiBold"/><TextBlock Text="{Binding RejectedCount}" Margin="4,0,18,0"/>
                <TextBlock Text="Net claim value:" FontWeight="Bold"/><TextBlock Text="{Binding NetClaimValue, StringFormat='Rs. {0:N2}'}" FontWeight="Bold" Margin="5,0,0,0"/>
            </WrapPanel>
        </Border>
    </Grid>
</UserControl>
