<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Finance.SupplierLedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="850"
             d:DesignWidth="1400"
             Background="#D4D0C8"
             FontFamily="Segoe UI"
             FontSize="12">

    <UserControl.Resources>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#D0E8E8"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#E4F0F0"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black"/>

        <Style TargetType="{x:Type TextBlock}" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="{x:Type TextBlock}" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="{x:Type TextBox}" x:Key="ClassicInput">
            <Setter Property="Height" Value="27"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style TargetType="{x:Type ComboBox}" x:Key="ClassicCombo">
            <Setter Property="Height" Value="27"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style TargetType="{x:Type DatePicker}" x:Key="ClassicDate">
            <Setter Property="Height" Value="27"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style TargetType="{x:Type Button}" x:Key="ClassicBtn">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.55"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type Button}" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007777"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type Button}" x:Key="PayBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1E7E34"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type Button}" x:Key="QuickFillBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Height" Value="27"/>
        </Style>

        <Style TargetType="{x:Type Border}" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="{x:Type Border}" x:Key="KpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="13,9"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
        </Style>

        <Style TargetType="{x:Type DataGridCell}" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D0E8E8"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type DataGridCell}" x:Key="FrozenCell" BasedOn="{StaticResource BaseCell}">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="{x:Type TextBlock}" x:Key="RightText">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="{x:Type TextBlock}" x:Key="RightBoldText" BasedOn="{StaticResource RightText}">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="{x:Type TextBlock}" x:Key="CenterText">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="{x:Type TextBox}" x:Key="CopyableGridTextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="IBeam"/>
            <Setter Property="Focusable" Value="True"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="620"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="360" MinWidth="320" MaxWidth="440"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="165"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="95"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="SUPPLIER ACCOUNT:" Style="{StaticResource HeaderLabel}" Margin="0" VerticalAlignment="Center"/>

                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding Suppliers}"
                              SelectedItem="{Binding SelectedSupplier, Mode=TwoWay}"
                              DisplayMemberPath="DisplayText"
                              TextSearch.TextPath="DisplayText"
                              IsTextSearchEnabled="True"
                              IsEditable="True"
                              Style="{StaticResource ClassicCombo}"
                              Background="#FFFFE0"
                              FontWeight="Bold"
                              Height="30"/>

                    <Button Grid.Column="3" Content="LOAD LEDGER" Command="{Binding LoadLedgerCommand}" Style="{StaticResource ActionBtn}" Height="30"/>
                    <Button Grid.Column="5" Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource ClassicBtn}" Height="30"/>

                    <Border Grid.Column="7" Background="#E0FFFF" BorderBrush="#005555" BorderThickness="1" Padding="8,3" VerticalAlignment="Center">
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#003366" FontWeight="SemiBold" FontSize="11" TextTrimming="CharacterEllipsis" MaxWidth="260"/>
                    </Border>
                </Grid>
            </Border>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,8">
                <Border Style="{StaticResource KpiCard}" BorderBrush="#005555">
                    <StackPanel>
                        <TextBlock Text="TOTAL BILLED - GRN" FontSize="10" FontWeight="Bold" Foreground="#666666"/>
                        <TextBlock Text="{Binding TotalBilled, StringFormat=Rs. \{0:N2\}}" FontSize="18" FontWeight="Bold" Foreground="#003366" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}" BorderBrush="#A94044">
                    <StackPanel>
                        <TextBlock Text="SUPPLIER RETURNS" FontSize="10" FontWeight="Bold" Foreground="#666666"/>
                        <TextBlock Text="{Binding TotalCredits, StringFormat=- Rs. \{0:N2\}}" FontSize="18" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}" BorderBrush="#28A745">
                    <StackPanel>
                        <TextBlock Text="TOTAL PAID" FontSize="10" FontWeight="Bold" Foreground="#666666"/>
                        <TextBlock Text="{Binding TotalPaid, StringFormat=Rs. \{0:N2\}}" FontSize="18" FontWeight="Bold" Foreground="#28A745" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}" BorderBrush="#D97706" Background="#FFF8EB" Margin="0">
                    <StackPanel>
                        <TextBlock Text="NET OUTSTANDING" FontSize="10" FontWeight="Bold" Foreground="#D97706"/>
                        <TextBlock Text="{Binding NetOutstanding, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Grid.Row="2" Background="White" BorderBrush="#A0A0A0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TabControl Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}" Background="#F0F0F0" BorderThickness="0,0,0,1" BorderBrush="#A0A0A0">
                        <TabControl.Resources>
                            <Style TargetType="{x:Type TabItem}">
                                <Setter Property="Padding" Value="14,7"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Foreground" Value="#555555"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Foreground" Value="#003366"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TabControl.Resources>
                        <TabItem Header="Statement of Account"/>
                        <TabItem Header="GRN History"/>
                        <TabItem Header="Supplier Returns"/>
                        <TabItem Header="Payment History"/>
                    </TabControl>

                    <DataGrid Grid.Row="1"
                              x:Name="dgLedger"
                              ItemsSource="{Binding LedgerEntries}"
                              SelectedItem="{Binding SelectedLedgerEntry, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="All"
                              HorizontalGridLinesBrush="#E0E0E0"
                              VerticalGridLinesBrush="#E0E0E0"
                              RowHeight="30"
                              Background="White"
                              BorderThickness="0"
                              SelectionMode="Single"
                              RowStyle="{DynamicResource BoDataGridRow}"
                              AlternatingRowBackground="#F9F9F9"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.Resources>
                            <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource BaseCell}"/>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding EntryDate, StringFormat=dd-MM-yyyy}" Width="90" IsReadOnly="True" ElementStyle="{StaticResource CenterText}"/>

                            <DataGridTemplateColumn Header="Ref No." Width="115" CellStyle="{StaticResource FrozenCell}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding ReferenceNo}" Style="{StaticResource CopyableGridTextBox}" FontWeight="Bold" Foreground="#003366"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Type" Binding="{Binding DisplayType}" Width="125" IsReadOnly="True" CellStyle="{StaticResource FrozenCell}"/>
                            <DataGridTextColumn Header="Description / Remarks" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Charge" Binding="{Binding ChargeAmount, StringFormat=N2}" Width="105" IsReadOnly="True" ElementStyle="{StaticResource RightText}"/>
                            <DataGridTextColumn Header="Paid / Credit" Binding="{Binding PaidAmount, StringFormat=N2}" Width="105" IsReadOnly="True" ElementStyle="{StaticResource RightText}"/>
                            <DataGridTextColumn Header="Balance" Binding="{Binding RunningBalance, StringFormat=N2}" Width="115" IsReadOnly="True" ElementStyle="{StaticResource RightBoldText}"/>
                            <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=dd-MM-yyyy}" Width="95" IsReadOnly="True" ElementStyle="{StaticResource CenterText}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <GridSplitter Grid.Column="1"
                      Width="6"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource BorderBrush}"
                      ShowsPreview="True"/>

        <Border Grid.Column="2" Background="#E2EBE9" BorderBrush="#005555" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="RECORD SUPPLIER PAYMENT" FontSize="13" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,12" HorizontalAlignment="Center"/>

                <Border Grid.Row="1" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" Padding="10" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="Current Outstanding Amount" Foreground="#666666" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding NetOutstanding, StringFormat=Rs. \{0:N2\}}" FontWeight="Bold" FontSize="20" Foreground="#A94044" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Payment Date *" Style="{StaticResource InlineLabel}" Margin="0,0,0,3"/>
                        <DatePicker SelectedDate="{Binding PaymentDate, Mode=TwoWay}" Style="{StaticResource ClassicDate}" Margin="0,0,0,12"/>

                        <TextBlock Text="Payment Amount (Rs.) *" Style="{StaticResource InlineLabel}" Foreground="#005555" Margin="0,0,0,3"/>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="6"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="6"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding PaymentAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ClassicInput}" FontWeight="Bold" FontSize="15" Background="#FFFFE0" HorizontalContentAlignment="Right"/>
                            <Button Grid.Column="2" Content="HALF" Command="{Binding AutoFillHalfCommand}" Style="{StaticResource QuickFillBtn}"/>
                            <Button Grid.Column="4" Content="FULL" Command="{Binding AutoFillFullCommand}" Style="{StaticResource QuickFillBtn}"/>
                        </Grid>

                        <TextBlock Text="Payment Method *" Style="{StaticResource InlineLabel}" Margin="0,0,0,3"/>
                        <ComboBox ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding SelectedPaymentMethod, Mode=TwoWay}" Style="{StaticResource ClassicCombo}" Margin="0,0,0,12"/>

                        <Border Background="#F5F5F5" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Bank Name" Style="{StaticResource InlineLabel}" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding BankName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ClassicInput}" Margin="0,0,0,10"/>

                                <TextBlock Text="Cheque / Bank / Card Reference No." Style="{StaticResource InlineLabel}" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding ReferenceNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ClassicInput}" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="Payment Remarks" Style="{StaticResource InlineLabel}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding PaymentRemarks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="70" TextWrapping="Wrap" AcceptsReturn="True" Padding="5" BorderBrush="#808080" BorderThickness="1"/>

                        <Border Background="#FFF8EB" BorderBrush="#D97706" BorderThickness="1" Padding="8" Margin="0,12,0,0">
                            <TextBlock Text="Overpayment is blocked. Supplier advances can be added later as a separate feature." Foreground="#6B3F00" FontSize="11" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Grid Grid.Row="3" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="CLEAR" Command="{Binding ClearPaymentCommand}" Style="{StaticResource ClassicBtn}" Height="40"/>
                    <Button Grid.Column="2" Content="POST PAYMENT" Command="{Binding PostPaymentCommand}" Style="{StaticResource PayBtn}" Height="40"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
