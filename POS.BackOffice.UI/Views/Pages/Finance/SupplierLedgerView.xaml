<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Finance.SupplierLedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="TextBox" x:Key="ClassicInput">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox" x:Key="ClassicCombo">
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DatePicker" x:Key="ClassicDate">
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
        </Style>

        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PayBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1E7E34"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="QuickFillBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>

        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Border" x:Key="KpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#005555"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="RightElement" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="RightBoldElement" BasedOn="{StaticResource RightElement}">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="SELECT SUPPLIER ACCOUNT:" Style="{StaticResource HeaderLabel}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="CompanyName" Style="{StaticResource ClassicCombo}" Background="#FFFFE0" FontWeight="Bold" FontSize="14" Height="30"/>
                    <Button Grid.Column="2" Content="LOAD LEDGER" Command="{Binding LoadLedgerCommand}" Style="{StaticResource ClassicBtn}" Height="30" Margin="10,0,0,0" Padding="20,0"/>
                </Grid>
            </Border>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,8">
                <Border Style="{StaticResource KpiCard}" BorderBrush="#005555">
                    <StackPanel>
                        <TextBlock Text="TOTAL BILLED (GRNs)" FontSize="10" FontWeight="Bold" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalBilled, StringFormat=Rs. \{0:N2\}}" FontSize="18" FontWeight="Bold" Foreground="#003366" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}" BorderBrush="#A94044">
                    <StackPanel>
                        <TextBlock Text="TOTAL CREDITS (RETURNS)" FontSize="10" FontWeight="Bold" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalCredits, StringFormat=- Rs. \{0:N2\}}" FontSize="18" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}" BorderBrush="#28A745">
                    <StackPanel>
                        <TextBlock Text="TOTAL AMOUNT PAID" FontSize="10" FontWeight="Bold" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalPaid, StringFormat=Rs. \{0:N2\}}" FontSize="18" FontWeight="Bold" Foreground="#28A745" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}" BorderBrush="#D97706" Background="#FFF8EB" Margin="0">
                    <StackPanel>
                        <TextBlock Text="NET OUTSTANDING DEBT" FontSize="10" FontWeight="Bold" Foreground="#D97706"/>
                        <TextBlock Text="{Binding NetOutstanding, StringFormat=Rs. \{0:N2\}}" FontSize="20" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0" ToolTip="This is the total amount you currently owe this supplier."/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Grid.Row="2" Background="White" BorderBrush="#A0A0A0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TabControl Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex}" Background="#F0F0F0" BorderThickness="0,0,0,1" BorderBrush="#A0A0A0">
                        <TabItem Header="Statement of Account (All)" FontWeight="Bold"/>
                        <TabItem Header="GRN History"/>
                        <TabItem Header="Return History (Debit Notes)"/>
                        <TabItem Header="Payment History"/>
                    </TabControl>

                    <DataGrid Grid.Row="1" x:Name="dgLedger" ItemsSource="{Binding LedgerEntries}" SelectedItem="{Binding SelectedLedgerEntry}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" 
                              HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                              RowHeight="28" Background="White" BorderThickness="0" 
                              SelectionMode="Single" AlternatingRowBackground="#F9F9F9">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding EntryDate, StringFormat=dd-MM-yyyy}" Width="85" Foreground="#555"/>
                            <DataGridTextColumn Header="Ref No." Binding="{Binding ReferenceNo}" Width="100" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding EntryType}" Width="90"/>
                            <DataGridTextColumn Header="Description / Remarks" Binding="{Binding Description}" Width="*"/>

                            <DataGridTextColumn Header="Charge (Cr)" Binding="{Binding ChargeAmount, StringFormat=N2}" Width="95" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Paid/Ret (Dr)" Binding="{Binding PaidAmount, StringFormat=N2}" Width="95" ElementStyle="{StaticResource RightElement}" Foreground="#005500"/>
                            <DataGridTextColumn Header="Balance" Binding="{Binding RunningBalance, StringFormat=N2}" Width="110" ElementStyle="{StaticResource RightBoldElement}" Foreground="#A94044"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Column="2" Background="#E2EBE9" BorderBrush="#005555" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="💵 RECORD NEW PAYMENT" FontSize="16" FontWeight="Bold" Foreground="#003366" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                <Border Grid.Row="1" Background="White" BorderBrush="#A0A0A0" BorderThickness="1" Padding="10" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Current Outstanding Amount:" Foreground="#666" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding NetOutstanding, StringFormat=Rs. \{0:N2\}}" FontWeight="Bold" FontSize="24" Foreground="#A94044" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="Payment Date *" Style="{StaticResource InlineLabel}" Margin="0,0,0,2"/>
                    <DatePicker SelectedDate="{Binding PaymentDate}" Style="{StaticResource ClassicDate}" Margin="0,0,0,15"/>

                    <TextBlock Text="Payment Amount (Rs.) *" Style="{StaticResource InlineLabel}" Foreground="#005555" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding PaymentAmount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ClassicInput}" FontWeight="Bold" FontSize="16" Background="#FFFFE0" HorizontalContentAlignment="Right" Padding="5"/>
                        <Button Grid.Column="1" Content="50% HALF PAY" Command="{Binding AutoFillHalfCommand}" Style="{StaticResource QuickFillBtn}" Margin="5,0,0,0" ToolTip="Instantly fills exactly half of the outstanding balance"/>
                        <Button Grid.Column="2" Content="100% FULL PAY" Command="{Binding AutoFillFullCommand}" Style="{StaticResource QuickFillBtn}" Margin="5,0,0,0" ToolTip="Instantly fills the complete outstanding balance"/>
                    </Grid>

                    <TextBlock Text="Payment Method *" Style="{StaticResource InlineLabel}" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding SelectedPaymentMethod}" Style="{StaticResource ClassicCombo}" Margin="0,0,0,15"/>

                    <Border Background="#F5F5F5" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Bank Name:" Style="{StaticResource InlineLabel}" Margin="0,0,0,2"/>
                            <TextBox Text="{Binding BankName}" Style="{StaticResource ClassicInput}" Margin="0,0,0,10" ToolTip="e.g., Commercial Bank, BOC"/>

                            <TextBlock Text="Cheque / Card / Ref No:" Style="{StaticResource InlineLabel}" Margin="0,0,0,2"/>
                            <TextBox Text="{Binding ReferenceNumber}" Style="{StaticResource ClassicInput}" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Additional Remarks / Notes:" Style="{StaticResource InlineLabel}" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding PaymentRemarks}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Style="{StaticResource ClassicInput}"/>
                </StackPanel>

                <Grid Grid.Row="4" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="CLEAR" Command="{Binding ClearPaymentCommand}" Style="{StaticResource ClassicBtn}" Height="40" Width="80"/>
                    <Button Grid.Column="1" Content="POST SECURE PAYMENT" Command="{Binding PostPaymentCommand}" Style="{StaticResource PayBtn}" Height="40" Margin="10,0,0,0" ToolTip="This will instantly update the supplier's ledger and reduce the outstanding balance."/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>