<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Reports.SalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ExcelBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PrintBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#2B5B84"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C3F5E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="QuickDateBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#E2EDF8"/>
            <Setter Property="Foreground" Value="#2B5B84"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="BorderBrush" Value="#B6D4FE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B6D4FE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="Border" x:Key="KpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Sales &amp; Revenue Analytics Engine" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Select Intelligence Report *" Foreground="#28A745" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbReportType" Style="{StaticResource StandardInput}" Background="#F4FAF6">
                            <ComboBoxItem Content="Sales Summary &amp; Profit Analysis" IsSelected="True"/>
                            <ComboBoxItem Content="Sales by Representative (Commissions)"/>
                            <ComboBoxItem Content="Top Customers / Debtors"/>
                            <ComboBoxItem Content="Sales by Category / Brand"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Location / Branch" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbLocation" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Date From *" Foreground="#666666" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpFromDate" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Date To *" Foreground="#666666" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dpToDate" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <Button Grid.Column="8" x:Name="btnRunReport" Content="Generate Report" Style="{StaticResource StandardBtn}" Background="#28A745" Margin="0" VerticalAlignment="Bottom" Height="30" Width="130"/>
                </Grid>

                <Grid Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Quick Dates:" VerticalAlignment="Center" Foreground="#666666" Margin="0,0,10,0"/>
                        <Button Content="Today" Style="{StaticResource QuickDateBtn}"/>
                        <Button Content="This Week" Style="{StaticResource QuickDateBtn}"/>
                        <Button Content="This Month" Style="{StaticResource QuickDateBtn}"/>
                        <Button Content="This Year" Style="{StaticResource QuickDateBtn}"/>
                    </StackPanel>

                    <CheckBox Grid.Column="1" x:Name="chkMaskProfit" Content="Privacy Mode: Hide Cost &amp; Profit Data" Foreground="#A94044" FontWeight="SemiBold" VerticalAlignment="Center" Cursor="Hand" ToolTip="Hides sensitive company margins from the screen and printouts"/>
                </Grid>
            </StackPanel>
        </Border>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,-15,15">

            <Border Style="{StaticResource KpiCard}" Background="#F4FAF6" BorderBrush="#C3E6CB">
                <StackPanel>
                    <TextBlock Text="Total Net Revenue" Foreground="#28A745" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiRevenue" Text="Rs. 1,450,000" FontWeight="Bold" FontSize="26" Foreground="#28A745" Margin="0,5,0,0"/>
                    <TextBlock Text="After discounts &amp; returns" Foreground="#28A745" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

            <Border x:Name="cardGrossProfit" Style="{StaticResource KpiCard}" Background="#FFF8EB" BorderBrush="#FDE0A6">
                <StackPanel>
                    <TextBlock Text="Total Gross Profit" Foreground="#D97706" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiProfit" Text="Rs. 420,500" FontWeight="Bold" FontSize="26" Foreground="#D97706" Margin="0,5,0,0"/>
                    <TextBlock Text="Revenue minus COGS" Foreground="#D97706" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" Background="#E2EDF8" BorderBrush="#B6D4FE">
                <StackPanel>
                    <TextBlock Text="Average Invoice Value" Foreground="#2B5B84" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiAvgInvoice" Text="Rs. 12,500" FontWeight="Bold" FontSize="26" Foreground="#2B5B84" Margin="0,5,0,0"/>
                    <TextBlock Text="From 116 transactions" Foreground="#2B5B84" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" Background="#FDF3F3" BorderBrush="#F5C6CB">
                <StackPanel>
                    <TextBlock Text="Total Discounts Given" Foreground="#A94044" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiDiscounts" Text="Rs. 45,000" FontWeight="Bold" FontSize="26" Foreground="#A94044" Margin="0,5,0,0"/>
                    <TextBlock Text="Revenue sacrificed for sales" Foreground="#A94044" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

        </UniformGrid>

        <Border Grid.Row="2" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
            <DataGrid x:Name="dgSalesAnalytics" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      IsReadOnly="True" 
                      HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      BorderThickness="0"
                      RowHeight="35"
                      Background="Transparent"
                      SelectionMode="Single">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F8F8"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Invoice No." Binding="{Binding InvoiceNo}" Width="120" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Customer / Company" Binding="{Binding CustomerName}" Width="1.5*"/>

                    <DataGridTextColumn x:Name="colCost" Header="Total Cost (COGS)" Binding="{Binding TotalCost, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Net Revenue" Binding="{Binding NetRevenue, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#28A745"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Discount" Binding="{Binding TotalDiscount, StringFormat=N2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Foreground" Value="#A94044"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn x:Name="colProfit" Header="Gross Profit" Binding="{Binding GrossProfit, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#D97706"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn x:Name="colMargin" Header="Margin %" Binding="{Binding ProfitMargin, StringFormat={}{0:N2}%}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="Data Status:" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,5,0"/>
                <TextBlock Text="Includes all posted Wholesale Invoices, POS Receipts, and Customer Returns." Foreground="#666666"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Export to Excel" Style="{StaticResource ExcelBtn}" Width="140"/>
                <Button Content="Print PDF Report" Style="{StaticResource PrintBtn}" Width="140" Margin="0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>