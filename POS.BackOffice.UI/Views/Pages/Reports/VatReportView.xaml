<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Reports.VatReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="850"
             d:DesignWidth="1450"
             Background="#D4D0C8"
             FontFamily="Segoe UI"
             FontSize="12">

    <UserControl.Resources>
        <Style x:Key="ClassicButton" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#E6E6E6"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ClassicButton}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="KpiCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
        <Style x:Key="KpiTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HeadersVisibility" Value="All"/>
            <Setter Property="AlternatingRowBackground" Value="#F6F8FA"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="#7A7A7A"
                BorderThickness="1"
                Background="#F0F0F0"
                Padding="10"
                Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,25,0">
                    <TextBlock Text="VAT REPORTS &amp; RECONCILIATION"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#003366"/>
                    <TextBlock Text="Read-only operational report from saved transaction snapshots"
                               Foreground="#555555"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="From:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,5,0"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding StartDate}" Width="130" Height="28" Margin="0,0,15,0"/>
                <TextBlock Grid.Column="3" Text="To:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,5,0"/>
                <DatePicker Grid.Column="4" SelectedDate="{Binding EndDate}" Width="130" Height="28" Margin="0,0,15,0"/>
                <StackPanel Grid.Column="5" Orientation="Horizontal">
                    <Button Content="Generate Report"
                            Command="{Binding GenerateReportCommand}"
                            Style="{StaticResource PrimaryButton}"/>
                    <Button Content="Current Month"
                            Command="{Binding ResetFiltersCommand}"
                            Style="{StaticResource ClassicButton}"/>
                </StackPanel>

                <StackPanel Grid.Column="6" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Summary.StoreName}" FontWeight="Bold" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding Summary.CurrentRegistrationText}" Foreground="#555555" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                BorderBrush="#A0A0A0"
                BorderThickness="1"
                Background="#FFFBE6"
                Padding="8"
                Margin="0,0,0,8">
            <TextBlock Text="Operational VAT position only. This screen does not create, file, lock, amend or pay a statutory VAT return. LegacyUnknown transactions and unclassified freight are excluded from calculated VAT." TextWrapping="Wrap" Foreground="#5E4A00"/>
        </Border>

        <TabControl Grid.Row="2">
            <TabItem Header="VAT Summary">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <TextBlock Text="Output VAT — Sales less Customer Credit Notes" FontSize="14" FontWeight="Bold" Foreground="#003366" Margin="4,4,4,2"/>
                        <UniformGrid Columns="3">
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="GROSS OUTPUT VAT" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.GrossOutputVat, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}"/>
                                    <TextBlock Text="{Binding Summary.GrossSalesTaxableAmount, StringFormat=Taxable: Rs. \{0:N2\}}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="CUSTOMER RETURN VAT" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.CustomerReturnVat, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}" Foreground="#8A2B2B"/>
                                    <TextBlock Text="{Binding Summary.CustomerReturnTaxableAmount, StringFormat=Taxable reversal: Rs. \{0:N2\}}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="NET OUTPUT VAT" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.NetOutputVat, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}"/>
                                    <TextBlock Text="{Binding Summary.NetSalesTaxableAmount, StringFormat=Net taxable: Rs. \{0:N2\}}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <TextBlock Text="Input VAT — GRN Purchases less Supplier Debit Notes" FontSize="14" FontWeight="Bold" Foreground="#003366" Margin="4,14,4,2"/>
                        <UniformGrid Columns="3">
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="GROSS INPUT VAT" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.GrossInputVat, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}"/>
                                    <TextBlock Text="{Binding Summary.GrossPurchaseTaxableAmount, StringFormat=Taxable: Rs. \{0:N2\}}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="SUPPLIER RETURN VAT" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.SupplierReturnVat, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}" Foreground="#8A2B2B"/>
                                    <TextBlock Text="{Binding Summary.SupplierReturnTaxableAmount, StringFormat=Taxable reversal: Rs. \{0:N2\}}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="NET INPUT VAT" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.NetInputVat, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}"/>
                                    <TextBlock Text="{Binding Summary.NetPurchaseTaxableAmount, StringFormat=Net taxable: Rs. \{0:N2\}}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <TextBlock Text="Operational position and control counts" FontSize="14" FontWeight="Bold" Foreground="#003366" Margin="4,14,4,2"/>
                        <UniformGrid Columns="4">
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="OPERATIONAL VAT POSITION" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.OperationalVatPosition, StringFormat=Rs. \{0:N2\}}" Style="{StaticResource KpiValue}"/>
                                    <TextBlock Text="{Binding Summary.PositionLabel}" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="COMPLETE DOCUMENTS" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.CompleteDocumentCount}" Style="{StaticResource KpiValue}"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="LEGACY / UNKNOWN ITEMS" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.LegacyUnknownDocumentCount}" Style="{StaticResource KpiValue}" Foreground="#8A5A00"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="DISCREPANCIES" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.DiscrepancyCount}" Style="{StaticResource KpiValue}" Foreground="#8A2B2B"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <UniformGrid Columns="2" Margin="0,12,0,0">
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="NET SALES BY NON-STANDARD CATEGORY" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.SalesZeroRatedAmount, StringFormat=Zero Rated: Rs. \{0:N2\}}" Margin="0,6,0,0"/>
                                    <TextBlock Text="{Binding Summary.SalesExemptAmount, StringFormat=Exempt: Rs. \{0:N2\}}"/>
                                    <TextBlock Text="{Binding Summary.SalesOutOfScopeAmount, StringFormat=Out of Scope: Rs. \{0:N2\}}"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource KpiCard}">
                                <StackPanel>
                                    <TextBlock Text="NET PURCHASES BY NON-STANDARD CATEGORY" Style="{StaticResource KpiTitle}"/>
                                    <TextBlock Text="{Binding Summary.PurchaseZeroRatedAmount, StringFormat=Zero Rated: Rs. \{0:N2\}}" Margin="0,6,0,0"/>
                                    <TextBlock Text="{Binding Summary.PurchaseExemptAmount, StringFormat=Exempt: Rs. \{0:N2\}}"/>
                                    <TextBlock Text="{Binding Summary.PurchaseOutOfScopeAmount, StringFormat=Out of Scope: Rs. \{0:N2\}}"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Sales and Customer Returns">
                <DataGrid ItemsSource="{Binding SalesAndReturns}" Margin="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Source" Binding="{Binding SourceType}" Width="160"/>
                        <DataGridTextColumn Header="Document" Binding="{Binding DocumentNumber}" Width="130"/>
                        <DataGridTextColumn Header="Original Reference" Binding="{Binding RelatedDocumentNumber}" Width="140"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding DocumentDate, StringFormat=yyyy-MM-dd}" Width="95"/>
                        <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="85"/>
                        <DataGridTextColumn Header="Taxable" Binding="{Binding TaxableAmount, StringFormat=N2}" Width="105"/>
                        <DataGridTextColumn Header="VAT" Binding="{Binding VatAmount, StringFormat=N2}" Width="95"/>
                        <DataGridTextColumn Header="Inclusive" Binding="{Binding InclusiveAmount, StringFormat=N2}" Width="110"/>
                        <DataGridTextColumn Header="Net Taxable Effect" Binding="{Binding NetTaxableEffect, StringFormat=N2}" Width="125"/>
                        <DataGridTextColumn Header="Net VAT Effect" Binding="{Binding NetVatEffect, StringFormat=N2}" Width="110"/>
                        <DataGridTextColumn Header="Zero Rated" Binding="{Binding ZeroRatedAmount, StringFormat=N2}" Width="100"/>
                        <DataGridTextColumn Header="Exempt" Binding="{Binding ExemptAmount, StringFormat=N2}" Width="95"/>
                        <DataGridTextColumn Header="Out of Scope" Binding="{Binding OutOfScopeAmount, StringFormat=N2}" Width="105"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Purchases and Supplier Returns">
                <DataGrid ItemsSource="{Binding PurchasesAndReturns}" Margin="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Source" Binding="{Binding SourceType}" Width="160"/>
                        <DataGridTextColumn Header="Document" Binding="{Binding DocumentNumber}" Width="130"/>
                        <DataGridTextColumn Header="Supplier Invoice" Binding="{Binding RelatedDocumentNumber}" Width="140"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding DocumentDate, StringFormat=yyyy-MM-dd}" Width="95"/>
                        <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="85"/>
                        <DataGridTextColumn Header="Taxable" Binding="{Binding TaxableAmount, StringFormat=N2}" Width="105"/>
                        <DataGridTextColumn Header="VAT" Binding="{Binding VatAmount, StringFormat=N2}" Width="95"/>
                        <DataGridTextColumn Header="Product Inclusive" Binding="{Binding InclusiveAmount, StringFormat=N2}" Width="115"/>
                        <DataGridTextColumn Header="Net Taxable Effect" Binding="{Binding NetTaxableEffect, StringFormat=N2}" Width="125"/>
                        <DataGridTextColumn Header="Net VAT Effect" Binding="{Binding NetVatEffect, StringFormat=N2}" Width="110"/>
                        <DataGridTextColumn Header="Zero Rated" Binding="{Binding ZeroRatedAmount, StringFormat=N2}" Width="100"/>
                        <DataGridTextColumn Header="Exempt" Binding="{Binding ExemptAmount, StringFormat=N2}" Width="95"/>
                        <DataGridTextColumn Header="Out of Scope" Binding="{Binding OutOfScopeAmount, StringFormat=N2}" Width="105"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="VAT by Category and Rate">
                <DataGrid ItemsSource="{Binding CategoryRateRows}" Margin="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Document Source" Binding="{Binding SourceType}" Width="190"/>
                        <DataGridTextColumn Header="Tax Category" Binding="{Binding TaxCategoryCode}" Width="130"/>
                        <DataGridTextColumn Header="Saved VAT Rate" Binding="{Binding VatRateDisplay}" Width="110"/>
                        <DataGridTextColumn Header="Documents" Binding="{Binding DocumentCount}" Width="85"/>
                        <DataGridTextColumn Header="Taxable" Binding="{Binding TaxableAmount, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="VAT" Binding="{Binding VatAmount, StringFormat=N2}" Width="110"/>
                        <DataGridTextColumn Header="Inclusive" Binding="{Binding InclusiveAmount, StringFormat=N2}" Width="120"/>
                        <DataGridTextColumn Header="Net Taxable Effect" Binding="{Binding NetTaxableEffect, StringFormat=N2}" Width="135"/>
                        <DataGridTextColumn Header="Net VAT Effect" Binding="{Binding NetVatEffect, StringFormat=N2}" Width="125"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Reconciliation and LegacyUnknown">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="LegacyUnknown and Unclassified Register">
                        <DataGrid ItemsSource="{Binding LegacyUnknownRows}" Margin="5">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Source" Binding="{Binding SourceType}" Width="150"/>
                                <DataGridTextColumn Header="Document" Binding="{Binding DocumentNumber}" Width="120"/>
                                <DataGridTextColumn Header="Related" Binding="{Binding RelatedDocumentNumber}" Width="130"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding DocumentDate, StringFormat=yyyy-MM-dd}" Width="95"/>
                                <DataGridTextColumn Header="Financial Amount" Binding="{Binding FinancialAmount, StringFormat=N2}" Width="120"/>
                                <DataGridTextColumn Header="Snapshot Status" Binding="{Binding SnapshotStatus}" Width="120"/>
                                <DataGridTextColumn Header="Warning" Binding="{Binding Warning}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>

                    <GroupBox Grid.Row="2" Header="Snapshot Reconciliation Differences — tolerance above Rs. 0.01">
                        <DataGrid ItemsSource="{Binding ReconciliationRows}" Margin="5">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Source" Binding="{Binding SourceType}" Width="155"/>
                                <DataGridTextColumn Header="Document" Binding="{Binding DocumentNumber}" Width="120"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding DocumentDate, StringFormat=yyyy-MM-dd}" Width="95"/>
                                <DataGridTextColumn Header="Field Checked" Binding="{Binding FieldChecked}" Width="170"/>
                                <DataGridTextColumn Header="Header / Source" Binding="{Binding HeaderOrSourceAmount, StringFormat=N2}" Width="115"/>
                                <DataGridTextColumn Header="Lines / Returned" Binding="{Binding DetailOrReturnedAmount, StringFormat=N2}" Width="115"/>
                                <DataGridTextColumn Header="Difference" Binding="{Binding Difference, StringFormat=N2}" Width="100"/>
                                <DataGridTextColumn Header="Explanation" Binding="{Binding Explanation}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="3" Background="#EEEEEE" BorderBrush="#A0A0A0" BorderThickness="1" Padding="6" Margin="0,8,0,0">
            <DockPanel>
                <ProgressBar DockPanel.Dock="Right" Width="120" Height="12" IsIndeterminate="{Binding IsBusy}" Margin="10,0,0,0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#444444"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
