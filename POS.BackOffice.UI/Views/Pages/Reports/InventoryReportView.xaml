<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Reports.InventoryReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1250"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="StandardBtn">
            <Setter Property="Background" Value="#2B3C38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B524E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ExcelBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PrintBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#2B5B84"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C3F5E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SmartActionBtn" BasedOn="{StaticResource StandardBtn}">
            <Setter Property="Background" Value="#6F42C1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5936A2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Control" x:Key="StandardInput">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Border" x:Key="GroupBoxBorder">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="Border" x:Key="KpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource GroupBoxBorder}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Inventory Analytics &amp; Reporting Engine" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Select Intelligence Report *" Foreground="#2B5B84" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbReportType" Style="{StaticResource StandardInput}" Background="#F0F8FF">
                            <ComboBoxItem Content="Reorder Level / Low Stock Alert" IsSelected="True"/>
                            <ComboBoxItem Content="Dead Stock / Slow Moving Items"/>
                            <ComboBoxItem Content="Fast Moving / Top Performers"/>
                            <ComboBoxItem Content="Inventory Valuation Summary"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Filter by Location" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbLocation" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Filter by Supplier" Foreground="#666666" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbSupplier" Style="{StaticResource StandardInput}"/>
                    </StackPanel>

                    <Button Grid.Column="6" x:Name="btnRunReport" Content="Generate Report" Style="{StaticResource StandardBtn}" Background="#2B5B84" Margin="0" VerticalAlignment="Bottom" Height="30" Width="130"/>
                </Grid>
            </StackPanel>
        </Border>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,-15,15">

            <Border Style="{StaticResource KpiCard}" Background="#FDF3F3" BorderBrush="#F5C6CB">
                <StackPanel>
                    <TextBlock Text="Critical Low Stock Items" Foreground="#A94044" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiLowStock" Text="14 Items" FontWeight="Bold" FontSize="28" Foreground="#A94044" Margin="0,5,0,0"/>
                    <TextBlock Text="Requires immediate purchasing" Foreground="#A94044" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" Background="#FFF8EB" BorderBrush="#FDE0A6">
                <StackPanel>
                    <TextBlock Text="Dead Stock Value (>90 Days)" Foreground="#D97706" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiDeadStock" Text="Rs. 245,500" FontWeight="Bold" FontSize="28" Foreground="#D97706" Margin="0,5,0,0"/>
                    <TextBlock Text="Capital tied up in non-moving goods" Foreground="#D97706" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" Background="#F4FAF6" BorderBrush="#C3E6CB">
                <StackPanel>
                    <TextBlock Text="Most Active Category (Last 30 Days)" Foreground="#28A745" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtKpiTopCategory" Text="Electronics" FontWeight="Bold" FontSize="28" Foreground="#28A745" Margin="0,5,0,0"/>
                    <TextBlock Text="Generated highest revenue volume" Foreground="#28A745" FontSize="11" Margin="0,2,0,0" Opacity="0.8"/>
                </StackPanel>
            </Border>

        </UniformGrid>

        <Border Grid.Row="2" Style="{StaticResource GroupBoxBorder}" Padding="0" Margin="0,0,0,15">
            <DataGrid x:Name="dgAnalytics" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      IsReadOnly="True" 
                      HeadersVisibility="Column" 
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      BorderThickness="0"
                      RowHeight="35"
                      Background="Transparent"
                      SelectionMode="Single">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F8F8"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="100"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                    <DataGridTextColumn Header="Primary Supplier" Binding="{Binding SupplierName}" Width="1.5*"/>

                    <DataGridTextColumn Header="Current Stock" Binding="{Binding QtyOnHand}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#A94044"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Min Reorder Level" Binding="{Binding MinReorderLevel}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Suggested Order Qty" Binding="{Binding SuggestedOrderQty}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#2B5B84"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Last Sold Date" Binding="{Binding LastSoldDate, StringFormat=dd/MM/yyyy}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="Report generated based on live database metrics." Foreground="#666666" FontStyle="Italic"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Export to Excel" Style="{StaticResource ExcelBtn}" Width="140"/>
                <Button Content="Print PDF Report" Style="{StaticResource PrintBtn}" Width="140"/>
                <Button x:Name="btnAutoDraftPO" Content="Auto-Draft Purchase Order" Style="{StaticResource SmartActionBtn}" Width="210" Margin="0" ToolTip="Instantly converts the suggested order quantities into a pending Purchase Order"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>