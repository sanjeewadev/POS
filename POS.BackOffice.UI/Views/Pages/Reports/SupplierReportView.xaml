<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Reports.SupplierReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="SupplierCellStyle" TargetType="DataGridCell"><Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/><Setter Property="BorderThickness" Value="0"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource LightSelectionBrush}"/><Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/></Trigger></Style.Triggers></Style>
        <Style x:Key="SupplierMetric" TargetType="Border"><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="7"/><Setter Property="Margin" Value="0,0,6,0"/></Style>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,7"><StackPanel DockPanel.Dock="Left"><TextBlock Text="Supplier Reports" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}"/><TextBlock Text="Purchase volume, supplier returns and outstanding balances" Foreground="{DynamicResource SecondaryTextBrush}"/></StackPanel><TextBlock DockPanel.Dock="Right" Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/></DockPanel>
        <Border Grid.Row="1" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="7" Margin="0,0,0,7"><WrapPanel><TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,4,0"/><DatePicker SelectedDate="{Binding StartDate}" Width="125" Height="27"/><TextBlock Text="To" VerticalAlignment="Center" Margin="10,0,4,0"/><DatePicker SelectedDate="{Binding EndDate}" Width="125" Height="27"/><TextBlock Text="Supplier" VerticalAlignment="Center" Margin="10,0,4,0"/><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="220" Height="27" Padding="5,3"/><Button Content="Load" Command="{Binding LoadCommand}" Width="80" Height="27" Margin="8,0,0,0"/><Button Content="Reset" Command="{Binding ResetCommand}" Width="80" Height="27" Margin="5,0,0,0"/>
                <Button Content="Save PDF" Command="{Binding ExportPdfCommand}" Width="86" Height="27" Margin="5,0,0,0" ToolTip="Manager or Administrator access required."/>
                <Button Content="Export CSV" Command="{Binding ExportCsvCommand}" Width="90" Height="27" Margin="5,0,0,0" ToolTip="Manager or Administrator access required."/></WrapPanel></Border>
        <UniformGrid Grid.Row="2" Rows="1" Columns="6" Margin="0,0,0,7">
            <Border Style="{StaticResource SupplierMetric}"><StackPanel><TextBlock Text="Outstanding" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding TotalCompanyDebt, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SupplierMetric}"><StackPanel><TextBlock Text="Suppliers Owing" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding SupplierWithDebtCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SupplierMetric}"><StackPanel><TextBlock Text="Purchases" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding TotalPurchaseValue, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SupplierMetric}"><StackPanel><TextBlock Text="Suppliers Purchased" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding SupplierWithPurchaseCount}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SupplierMetric}"><StackPanel><TextBlock Text="Supplier Credits" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding TotalSupplierReturnValue, StringFormat={}Rs. {0:N2}}" FontWeight="Bold"/></StackPanel></Border>
            <Border Style="{StaticResource SupplierMetric}" Margin="0"><StackPanel><TextBlock Text="Returned Qty" Foreground="{DynamicResource SecondaryTextBrush}"/><TextBlock Text="{Binding TotalReturnedQty, StringFormat={}{0:#,##0.###}}" FontWeight="Bold"/></StackPanel></Border>
        </UniformGrid>
        <TabControl Grid.Row="3">
            <TabItem Header="Outstanding Balances"><DataGrid ItemsSource="{Binding OutstandingSummaries}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeaderWidth="0" GridLinesVisibility="Horizontal" CellStyle="{StaticResource SupplierCellStyle}"><DataGrid.Columns><DataGridTextColumn Header="Supplier" Binding="{Binding SupplierDisplayName}" Width="*" MinWidth="220"/><DataGridTextColumn Header="GRN Billed" Binding="{Binding TotalGrnBilled, StringFormat={}Rs. {0:N2}}" Width="115"/><DataGridTextColumn Header="Returns" Binding="{Binding TotalSupplierReturns, StringFormat={}Rs. {0:N2}}" Width="105"/><DataGridTextColumn Header="Paid" Binding="{Binding TotalPaid, StringFormat={}Rs. {0:N2}}" Width="105"/><DataGridTextColumn Header="Outstanding" Binding="{Binding NetOutstanding, StringFormat={}Rs. {0:N2}}" Width="120"/><DataGridTextColumn Header="Transactions" Binding="{Binding TransactionCount}" Width="90"/><DataGridTextColumn Header="Last Activity" Binding="{Binding LastTransactionDate, StringFormat={}{0:dd-MMM-yyyy}}" Width="105"/></DataGrid.Columns></DataGrid></TabItem>
            <TabItem Header="Purchase Volume"><DataGrid ItemsSource="{Binding PurchasingVolumes}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeaderWidth="0" GridLinesVisibility="Horizontal" CellStyle="{StaticResource SupplierCellStyle}"><DataGrid.Columns><DataGridTextColumn Header="Supplier" Binding="{Binding SupplierDisplayName}" Width="*" MinWidth="220"/><DataGridTextColumn Header="GRNs" Binding="{Binding GrnCount}" Width="75"/><DataGridTextColumn Header="Purchase Value" Binding="{Binding TotalGrnValue, StringFormat={}Rs. {0:N2}}" Width="125"/><DataGridTextColumn Header="Share" Binding="{Binding PercentageOfTotalPurchases, StringFormat={}{0:N2}%}" Width="85"/><DataGridTextColumn Header="Last GRN" Binding="{Binding LastGrnDate, StringFormat={}{0:dd-MMM-yyyy}}" Width="105"/></DataGrid.Columns></DataGrid></TabItem>
            <TabItem Header="Supplier Returns"><DataGrid ItemsSource="{Binding SupplierReturns}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" RowHeaderWidth="0" GridLinesVisibility="Horizontal" CellStyle="{StaticResource SupplierCellStyle}"><DataGrid.Columns><DataGridTextColumn Header="Supplier" Binding="{Binding SupplierDisplayName}" Width="*" MinWidth="220"/><DataGridTextColumn Header="Documents" Binding="{Binding ReturnDocumentCount}" Width="85"/><DataGridTextColumn Header="Quantity" Binding="{Binding TotalReturnedQty, StringFormat={}{0:#,##0.###}}" Width="90"/><DataGridTextColumn Header="Gross Return" Binding="{Binding GrossReturnValue, StringFormat={}Rs. {0:N2}}" Width="115"/><DataGridTextColumn Header="Restocking Fee" Binding="{Binding RestockingFee, StringFormat={}Rs. {0:N2}}" Width="110"/><DataGridTextColumn Header="Supplier Credit" Binding="{Binding NetSupplierCredit, StringFormat={}Rs. {0:N2}}" Width="120"/><DataGridTextColumn Header="Return %" Binding="{Binding ReturnValuePercentage, StringFormat={}{0:N2}%}" Width="85"/><DataGridTextColumn Header="Last Return" Binding="{Binding LastReturnDate, StringFormat={}{0:dd-MMM-yyyy}}" Width="105"/></DataGrid.Columns></DataGrid></TabItem>
        </TabControl>
        <Grid Grid.Row="4"><TextBlock Text="No supplier report data matches the selected filters." HorizontalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/><ProgressBar Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/></Grid>
    </Grid>
</UserControl>
