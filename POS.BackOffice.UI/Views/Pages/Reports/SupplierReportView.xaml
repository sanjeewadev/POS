<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Reports.SupplierReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="#D4D0C8" FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#003366"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="InlineLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>

        <Style TargetType="DatePicker" x:Key="ClassicDate">
            <Setter Property="Height" Value="26"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Width" Value="130"/>
        </Style>

        <Style TargetType="Button" x:Key="ClassicBtn">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ClassicBtn}">
            <Setter Property="Background" Value="#005555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#003333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007777"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="ClassicGroupBox">
            <Setter Property="BorderBrush" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Border" x:Key="KpiCard">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DataGridCell" x:Key="BaseCell">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#005555"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="RightElement">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="RightBoldElement" BasedOn="{StaticResource RightElement}">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="DangerElement" BasedOn="{StaticResource RightBoldElement}">
            <Setter Property="Foreground" Value="#A94044"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ClassicGroupBox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="REPORTING PERIOD:" Style="{StaticResource HeaderLabel}" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1" Text="From Date:" Style="{StaticResource InlineLabel}"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding StartDate}" Style="{StaticResource ClassicDate}" Margin="0,0,15,0"/>

                <TextBlock Grid.Column="3" Text="To Date:" Style="{StaticResource InlineLabel}"/>
                <DatePicker Grid.Column="4" SelectedDate="{Binding EndDate}" Style="{StaticResource ClassicDate}" Margin="0,0,20,0"/>

                <Button Grid.Column="5" Content="⚡ GENERATE REPORT" Command="{Binding GenerateReportCommand}" Style="{StaticResource ActionBtn}" HorizontalAlignment="Left" Padding="30,0" FontSize="13"/>

                <StackPanel Grid.Column="6" Orientation="Horizontal">
                    <Button Content="📊 Export to Excel" Command="{Binding ExportToExcelCommand}" Style="{StaticResource ClassicBtn}" Margin="0,0,10,0"/>
                    <Button Content="🖨️ Export PDF" Command="{Binding ExportToPdfCommand}" Style="{StaticResource ClassicBtn}"/>
                </StackPanel>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,8">
            <Border Style="{StaticResource KpiCard}" BorderBrush="#D97706" Background="#FFF8EB">
                <StackPanel>
                    <TextBlock Text="TOTAL COMPANY OUTSTANDING DEBT (A/P)" FontSize="11" FontWeight="Bold" Foreground="#D97706"/>
                    <TextBlock Text="{Binding TotalCompanyDebt, StringFormat=Rs. \{0:N2\}}" FontSize="26" FontWeight="Bold" Foreground="#A94044" Margin="0,5,0,0" ToolTip="Total amount owed across all suppliers"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" BorderBrush="#005555">
                <StackPanel>
                    <TextBlock Text="REPORTING TIMEFRAME" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding StartDate, StringFormat=dd-MMM-yyyy}" FontSize="18" FontWeight="Bold" Foreground="#003366"/>
                        <TextBlock Text="  to  " FontSize="18" Foreground="#666"/>
                        <TextBlock Text="{Binding EndDate, StringFormat=dd-MMM-yyyy}" FontSize="18" FontWeight="Bold" Foreground="#003366"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource KpiCard}" BorderBrush="#A0A0A0" Margin="0">
                <StackPanel>
                    <TextBlock Text="DATA STATUS" FontSize="11" FontWeight="Bold" Foreground="#666"/>
                    <TextBlock Text="Live Read-Only Ledger" FontSize="18" FontWeight="SemiBold" Foreground="#28A745" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="White" BorderBrush="#A0A0A0" BorderThickness="1">
            <TabControl Background="#F0F0F0" BorderThickness="0,0,0,1" BorderBrush="#A0A0A0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Padding" Value="15,8"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Foreground" Value="#555"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#003366"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="Aged Payables Summary (Who Do We Owe?)">
                    <DataGrid ItemsSource="{Binding AgedPayables}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                              RowHeight="30" Background="White" BorderThickness="0" SelectionMode="Single" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="6"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Supplier Name" Binding="{Binding SupplierName}" Width="350" FontWeight="Bold"/>
                            <DataGridTextColumn Header="0 - 30 Days" Binding="{Binding CurrentTo30Days, StringFormat=N2}" Width="*" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="31 - 60 Days" Binding="{Binding Days31To60, StringFormat=N2}" Width="*" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="61 - 90 Days" Binding="{Binding Days61To90, StringFormat=N2}" Width="*" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Over 90 Days" Binding="{Binding Over90Days, StringFormat=N2}" Width="*" ElementStyle="{StaticResource DangerElement}"/>
                            <DataGridTextColumn Header="Total Owed (Rs.)" Binding="{Binding TotalOwed, StringFormat=N2}" Width="150" ElementStyle="{StaticResource RightBoldElement}" Foreground="#003366"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Purchasing Volume (Top 20 Partners)">
                    <DataGrid ItemsSource="{Binding PurchasingVolumes}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                              RowHeight="30" Background="White" BorderThickness="0" SelectionMode="Single" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="6"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Supplier Name" Binding="{Binding SupplierName}" Width="350" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Total GRN Value (Rs.)" Binding="{Binding TotalGrnValue, StringFormat=N2}" Width="250" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="% of Total Store Purchases" Binding="{Binding PercentageOfTotalStore, StringFormat=\{0:N2\}%}" Width="*" ElementStyle="{StaticResource RightBoldElement}" Foreground="#005555"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

                <TabItem Header="Return &amp; Defect Rates">
                    <DataGrid ItemsSource="{Binding ReturnRates}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All" HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="#E0E0E0" 
                              RowHeight="30" Background="White" BorderThickness="0" SelectionMode="Single" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="6"/>
                                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                <Setter Property="BorderBrush" Value="#A0A0A0"/>
                            </Style>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource BaseCell}"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Supplier Name" Binding="{Binding SupplierName}" Width="350" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Total Units Bought" Binding="{Binding TotalItemsBought, StringFormat=N2}" Width="200" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Total Units Returned" Binding="{Binding TotalItemsReturned, StringFormat=N2}" Width="200" ElementStyle="{StaticResource RightElement}"/>
                            <DataGridTextColumn Header="Defect / Return Rate (%)" Binding="{Binding DefectPercentage, StringFormat=\{0:N2\}%}" Width="*" ElementStyle="{StaticResource DangerElement}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>

            </TabControl>
        </Border>
    </Grid>
</UserControl>