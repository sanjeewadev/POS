<UserControl x:Class="POS.BackOffice.UI.Views.Pages.Reports.ShopCostFreeIssueReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="850" d:DesignWidth="1400"
             Background="{DynamicResource WindowBackgroundBrush}"
             FontFamily="Segoe UI"
             FontSize="12">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,7">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Shop Cost Free Issue Report" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource HeaderTextBrush}"/>
                <TextBlock Text="Detailed tracking and cost analysis of free issues under shop cost rules" Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>
            <TextBlock DockPanel.Dock="Right" Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </DockPanel>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Style="{DynamicResource BoFilterBar}">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,12,4">
                    <TextBlock Text="From" Style="{DynamicResource BoInlineLabel}"/>
                    <DatePicker SelectedDate="{Binding FromDate}" Style="{DynamicResource BoInputDatePicker}" Width="125"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,12,4">
                    <TextBlock Text="To" Style="{DynamicResource BoInlineLabel}"/>
                    <DatePicker SelectedDate="{Binding ToDate}" Style="{DynamicResource BoInputDatePicker}" Width="125"/>
                </StackPanel>

                <Button Content="LOAD REPORT" Command="{Binding LoadReportCommand}" Style="{DynamicResource BoPrimaryButton}" Margin="0,0,6,4"/>
                <Button Content="CLEAR" Command="{Binding ClearCommand}" Style="{DynamicResource BoButton}" Margin="0,0,0,4"/>
            </WrapPanel>
        </Border>

        <!-- Summary KPI Cards -->
        <UniformGrid Grid.Row="2" Columns="2" Margin="0,0,0,7">
            <Border Style="{DynamicResource BoKpiCard}">
                <StackPanel>
                    <TextBlock Text="TOTAL QUANTITY ISSUED" Style="{DynamicResource BoKpiLabel}"/>
                    <TextBlock Text="{Binding TotalQuantity}" Style="{DynamicResource BoKpiValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource BoKpiCard}" BorderBrush="#A94044" Margin="0">
                <StackPanel>
                    <TextBlock Text="TOTAL COST" Style="{DynamicResource BoKpiLabel}"/>
                    <TextBlock Text="{Binding TotalCost, StringFormat=Rs. \{0:N2\}}" Style="{DynamicResource BoKpiValue}" Foreground="#A94044"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Main Data Grid -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding ReportItems}"
                  Style="{DynamicResource BoDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date &amp; Time" Binding="{Binding TransactionDate, StringFormat=yyyy-MM-dd HH:mm}" Width="135"/>
                <DataGridTextColumn Header="Invoice No." Binding="{Binding InvoiceNo}" Width="115"/>
                <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="110"/>
                <DataGridTextColumn Header="Item Description" Binding="{Binding ItemDescription}" Width="*" MinWidth="200"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity, StringFormat=N2}" Width="90"/>
                <DataGridTextColumn Header="Unit Cost (Rs.)" Binding="{Binding UnitCost, StringFormat=N2}" Width="110"/>
                <DataGridTextColumn Header="Total Cost (Rs.)" Binding="{Binding TotalCost, StringFormat=N2}" Width="115"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="130"/>
                <DataGridTextColumn Header="Applied By" Binding="{Binding AppliedBy}" Width="110"/>
                <DataGridTextColumn Header="Approved By" Binding="{Binding ApprovedBy}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status / Progress Footer -->
        <ProgressBar Grid.Row="4" Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,6,0,0"/>
    </Grid>
</UserControl>